#!/usr/bin/env python3
import os
import sys

sys.path.insert(0, os.path.join(
    os.path.dirname(os.path.dirname(os.path.realpath(__file__))), "share"))
from rbench.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
