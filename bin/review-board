#!/bin/bash
# Regenerate the leg board from the live corpus and open it in the default browser.
set -e
out="${1:-$HOME/.cache/llm-legs/review-board.html}"
mkdir -p "$(dirname "$out")"
"$(cd "$(dirname "$0")" && pwd)/review-board-html" "$out"
open "$out"
