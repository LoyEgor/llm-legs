# Hand-off: judge-confirmed review findings in the Light edit and worker-run paths

From the chat "Review-bench improvements phase 4", review round 20260920T021015Z-6e4c657 (T2 double, behavior hunt), judge-confirmed.
The files below are another chat's live work, so nothing here is fixed: the owner of each decides what to do with its finding.
Severity, path and the run's own wording are kept verbatim; the full text is `review-bench findings 20260920T021015Z-6e4c657`.

P2 llm-legs/bin/worker-run:1689 (opus-high#2)
`light_start_abandon`'s `rm -rf "$abandon_directory"` deletes the run directory on every post-worktree refusal, but the EXIT trap armed at line 2185 stays armed past the side effects at 2333 (`statusline-place add --kind worker-start`), 2365 and 2369 (`anchors_why run-start --run "$run_id"` for the workdir and for every repo of the launcher's journal). A refusal in that window — `fail 'setsid is unavailable'` at 2406, or a TERM/INT delivered while `snapshot_other_families` walks the chat's repos — removes the only artifact those rows are ever closed from: `fold_run_anchors`/`complete_run` need `$directory/families/*` to emit `run-fold`. The anchors/debt store keeps an open `run-start` for a run id whose directory no longer exists, so the launching chat's debt verdict stays `?run` forever and the statusline keeps a worker-start with no worker-end. Before this commit the same refusal left the directory in place and readers resolved the run as dead.

P2 llm-legs/bin/worker-run:1759 (opus-high#2)
`[ -n "$base" ] || base=HEAD` reintroduces exactly the loss the comment three lines below guards against. Run shape: a Light edit whose meta.json carries no `light_base` (a run launched by the previous worker-run and completed after this in-place upgrade, or `rev-parse` at 2186 returning empty) whose worker committed its edits inside the worktree. base=HEAD is then the worker's own commit, so `light_touched_paths` reports nothing, `scope_state=ok`, `git add -A` stages nothing and `git diff --cached --binary HEAD` is empty; the `[ ! -s "$patch" ]` branch then runs `light_worktree_remove`, whose `git worktree remove --force` plus `git branch -D light-<run-id>` destroy the only copy of the work, while the verdict prints `SCOPE: ok` / `LANDED: no` and the reader concludes the run produced nothing.

P1 llm-legs/bin/worker-run:2187 (opus-high)
`workdir=$light_worktree${light_relative:+/$light_relative}` now puts a Light edit's worker in a subdirectory of the worktree, but the gate that judges it still works at the worktree root: `light_verify_run "$worktree" "$verify"` (line 1768) runs VERIFY with cwd = worktree root, and `light_touched_paths "$worktree" "$base"` (lines 1762, 1773) yields repo-root-relative paths for the SCOPE fence. Run shape: `worker-run start light --workdir /repo/sub` with the shipped recipe shape (`share/light-recipes/test-scaffold.md`: `SCOPE: {{target}}` / `VERIFY: bash {{target}}`) where the caller writes paths as the worker sees them from its cwd — VERIFY's `bash tests/foo.sh` does not exist at the worktree root, exits nonzero, verify_state=fail; if the caller instead writes them repo-relative to satisfy VERIFY, the brief hands the worker paths that do not resolve from its cwd. Before this commit workdir was forced to the worktree root so the two cwds coincided. Result: a green, in-scope edit is judged `VERIFIED: fail` / `LANDED: no`, the diff is discarded and the worktree and branch are left behind, while the account was spent.

P1 llm-legs/bin/light-research:193 (opus-high)
`if units_load "$attach"; then ... fi` silently falls through to the legacy single-run attach path (lines 197-212) whenever the unit table is gone, and `units_finish` deletes it with `rm -rf "$units_dir"` (line 187) the moment the batch lands — as does `units_persist`'s `rm -rf "$units_dir"` (line 117) for a relaunch reusing the same `--out`. Run shape: a two-unit batch prints `RUN: A` and `RUN: B`, `--attach A` collects both and lands the combined answer, then `--attach B --out <same>` finds no table, collects run B alone and calls `land_answer` on that one part — overwriting the complete answer file with one unit's text and printing `CITATIONS:`/`ANSWER:`/`LINES:` as if it were the whole batch. The caller then reads a silently truncated answer.

P3 llm-legs/share/light-research.sh:41 (sol-high)
Relative citations are now accepted when the quote matches any repository in the overall call, without retaining which unit produced the citation. In Grok fan-out, a unit reviewing repo A can cite `shared.txt` with text present only in repo B; assembly keeps it under repo A's heading, but this loop marks it verified and the printed citation total claims success.

P1 llm-legs/bin/worker-run:1726 (sol-high)
The new scope fence observes only Git changes inside the throwaway worktree. Light workers run with unrestricted write modes, so a successful cell can write directly to the shared checkout, `$HOME/.claude`, or through a worktree symlink while leaving no worktree diff; `light_scope_state` then returns `ok`, the gate removes the empty worktree, and `worker-run report` prints a passing scope although the external mutation remains.
