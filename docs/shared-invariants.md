# Shared invariants

Some values are duplicated across scripts written in different languages (bash,
jq, Lua, prose) because each implementation is independent — no shared library
spans `bin/claudeb`, `bin/statusline.sh`, `bin/worker-pick`, `llm-limits.sh`, the
Hammerspoon renderer, and the docs. When such a value drifts in one place, the
system misbehaves silently. `tests/test_consistency.sh` re-extracts each value
from the live files and fails if any site disagrees with the canonical value
below. This table is the canonical source; the test names it in every failure.

| # | Invariant | Canonical value | Implementation sites |
|---|---|---|---|
| a | Staleness / dim thresholds | five_hour `1800`s, weekly `21600`s, fable `21600`s | the numbers live once in `share/limits-view.sh` (`LIMITS_STALE_FIVE_HOUR`/`_WEEKLY`/`_FABLE`); `bin/claudeb` and `llm-limits.sh` pass them into jq as `$thr5`/`$thrw`/`$thrf` and must carry no local literal; `bin/statusline.sh` sources the same module and passes `$thr5`/`$thrw` into its bucket jq and `$LIMITS_STALE_FABLE` into the limits-file age backstop, with no local literal; `docs/statusline-contract.md` usage-table prose (`as_of` >1800s / >21600s, mtime >21600s); `hammerspoon/llm-limits.lua` consumes the collector's `.stale` flag rather than a local threshold (`bucket.stale == true`) — it must stay flag-driven, never hardcode a number |
| b | Keychain service formula | `Claude Code-credentials-` + first 8 hex of `shasum -a 256` of the profile path | `bin/claudeb` `keychain_service`; `llm-limits.sh` `claude_subscription_type`; `bin/claude-session-driver` `keychain_service` (Python `hashlib.sha256(...).hexdigest()[:8]`, same prefix) |
| c | worker-pick cache line format | `cx<mark><acct>·<model>·<eff> cb<~|@><acct>·<model>·<eff> gx<mark><acct>·<model>·<eff>` (producer printf `cx%s%s·<model>·%s %s·%s·%s gx%s%s·%s·%s`; Codex model comes from `~/.codex/config.toml`, and each vendor account is dynamic) | producer `bin/worker-pick` `write_cache`; consumer `bin/statusline.sh` reads `worker-pick.line.<acct>` verbatim |
| d | Weather HTTP classes | `000`, `429`, `5xx` (`5[0-9][0-9]`) are weather — they never write an auth verdict (`expired`/`revoked`/`auth-failed`) | `bin/claudeb` `probe_weather_failed` case pattern; `bin/claudeb` `oauth_refresh` outcome classification |
| e | OAuth token-endpoint 429 cooldown | `900 * 2^(max(1, strikes)-1)`, capped at `14400`s (4h); a longer vendor `Retry-After` wins | `bin/claudeb` `oauth_backoff_until`; `llm-limits.sh` `claude_stale_cause` |
| f | Token-freeze file semantics | file at `<store>/token-freeze` present = freeze active; a numeric `until` in the past → inactive (auto-resume); a missing / non-numeric `until` → active while the file exists. The active freeze blocks robot paths only; `CLAUDEB_WARM_USER_EXPLICIT=true` exempts the full user invocation and marks its journal entries with `"user":true` | `bin/claudeb` `token_freeze_active` plus user guards and `token_journal`; `llm-limits.sh` `token_freeze_active_at` plus the user-explicit stale-cause guard |
| g | Codex/Gemini base-profile priority | literal profile `main` never outranks a usable non-main account of the same vendor that spends no more quota: in `worker-pick` it is the tiebreak after the spending bucket and the five-hour reading (`docs/routing-contract.md` rule 2), in `codexb` it sorts last outright. Display-only account lines sort the selected account first, then the remaining candidates, then pool members that cannot serve, then everything unreachable, preserving selection order within each band | `bin/worker-pick` shared `rank` and `display_band` definitions; `bin/codexb` `pick_account` ranking; `share/worker-policy.md` vendor-fit prose |
| h | Antigravity review cell invocation mapping | Every cell omits `--effort`. `agy-pro-low` → `--model gemini-3.1-pro-low`; `agy-pro-high` → `--model "Gemini 3.1 Pro (High)"` because v1.1.9 still serves the high slug as Flash; `agy-flash37-<effort>` → `--model gemini-3.7-flash-<effort>`; `agy-flash36-<effort>` → `--model gemini-3.6-flash-<effort>`; `agy-flash35-<effort>` → `--model gemini-3.5-flash-<effort>`. A resolved label that disagrees with the requested cell is recorded as `model_mismatch` and warned about; every Antigravity cell is a `-skill` cell — diff-only agy review is refused in `parse_rater` | `share/rbench/{catalog,launch}.py` `AGY_EFFORTS`, `AGY_MODEL_IDS`, and `agy_model_id`; `bin/worker-run` knob-to-agy model case for worker-supported models; `docs/DIAGNOSTICS.md` review-bench system-map entry |
| i | Gemini worker knobs | Required defaults are `gemini_model=pro`, `gemini_effort=high`; valid pairs are pro high/low, flash36 high/medium/low, and flash35 high/medium/low (bare `flash` aliases `flash36`). Invocation follows invariant h: effort is encoded in the versioned slug except the `pro` high label workaround, and no pair passes `--effort` | accepted knobs/default-writing semantics in `~/.claude/commands/worker.md`; routing defaults in `bin/worker-pick`; statusline reads and renders both knobs independently; policy pointer in `share/worker-policy.md` |
| j | Worker spawn pressure gate | `worker-pick --account <vendor>` owns selection: its selected account is allowed below the hard `100`% wall and warned from `85`%; exit `3` and an effective `100`% account are blocked. `95`% is the protective block only when worker-pick is unavailable or fails, and that fallback is always reported | shared `~/.claude/hooks/worker-limit-gate.sh` for claudeb, Codex, and Gemini worker spawns; `bin/worker-pick` account query; `~/.claude/settings.json` has one Agent registration |
| k | Worker account resolution | resolution lives only in `worker-run`: an explicit `--account` (relays forward the brief's `ACCOUNT: <name>` line) wins and never reroutes; otherwise `worker-pick --account claudeb\|codex\|gemini`; exit `3` aborts the launch with `OUTCOME: <VENDOR>_USAGE_LIMIT`, or with `OUTCOME: <VENDOR>_UNAVAILABLE` when the picker reports an empty worker pool rather than a wall; an account outside the pool is refused outright unless it is the vendor pin (row q); only an unavailable/failed query falls back to the vendor pin (recorded as pinned), then `main` for Codex/Gemini, and the resolved account is visible in the printed `TAG:` line. On a mid-run usage wall an unpinned run re-resolves with the burnt accounts excluded and relaunches, reporting each hop as a `REROUTE:` line and rewriting the tag; `OUTCOME: <VENDOR>_USAGE_LIMIT` means the whole pool walled. Claude refuses when its fallback pin is empty because `claudeb` requires an explicit profile | `~/.claude/commands/worker.md` pin toggles; `bin/worker-pick` pin routing; `bin/worker-spawn-hook.sh` pending tag; `bin/worker-run` account resolution; `~/.claude/agents/claudeb-worker.md`, `codex-worker.md`, and `gemini-worker.md` `--account` forwarding |
| l | Gemini quota group matching | case-insensitive group label contains `gemini`; exact display-name equality is forbidden | `llm-limits.sh` raw group collection/validation; `bin/worker-pick` normalized account eligibility |
| m | Gemini profile discovery and HOME mapping | account `main` maps to the process base HOME; named accounts are sorted profile directories and map to `<profiles-dir>/<name>` | `share/gemini-accounts.sh`, sourced by `bin/geminib`, `llm-limits.sh` and `bin/worker-run` (which resolves a run's agy transcript under that HOME) |
| n | Weekly bucket provenance | rate-limit response headers carry NO weekly percentage, so no writer may stamp `origin: "headers"` on `seven_day`; every reader treats such a bucket as unknown (rendered `?`/`-`, never a number) | writers: `bin/claudeb` `merge_headers` (5h only), `bin/statusline.sh` `rl_merge` stamps `origin: "session"` and drops a cached header-origin week; readers: `bin/claudeb` `account_data`, `llm-limits.sh` both claudeb-store and single-account snapshot reads, `bin/statusline.sh` render gate, `bin/worker-pick` cache normalization |
| o | Claude rotation usability presence | `llm-limits.sh` always emits boolean `rotation.usable.general`, `rotation.usable.fable`, and `blocked == (rotation.usable.general \| not)` for every emitted Claude store account, including incomplete, disabled, and unusable snapshots; consumers may rely on those keys being present. `bin/worker-pick` defaults a missing rotation key to `true` only for compatibility with pre-change caches. Two documented weak spots, both fail-open on `fable` and both bounded by the worker's own `CLAUDEB_USAGE_LIMIT` reroute: the Pro-plan gate needs `claude_subscription_type`, which returns an empty string when the keychain is locked or absent (unknown plan is then treated as non-Pro). | producer: `llm-limits.sh` Claude account projection; permissive fallback: `bin/worker-pick` |
| p | Reserved profile names | the same set in both guards, so a name that cannot be a profile cannot be offered as a switch target either: `help add enable disable remove use accounts status st profile p run warm token-upkeep`, plus `main` and any name starting with `.` or containing `/` | `bin/claudeb` `reserved_name` (with `main` checked separately by `add_account`); `bin/claude-chat-switch` `is_reserved` |
| q | Worker-pool membership | one mechanism for all three vendors, so a toggle means the same thing everywhere: the excluded names live one per line in `<profiles-dir>/.<tool>/disabled` (`.claudeb`, `.codexb`, `.geminib`), an absent file means everything is in, and the dot-directory keeps the file outside the profile enumerators' `*` glob. Read and written only through `share/worker-pool.sh`, which also owns the per-vendor directory formula (`worker_pool_dir`, used by the three CLIs, `worker-run`, `codex-image` and `llm-limits.sh`; Claude reaches it as its store root `CLAUDEB_DIR`) and the wall itself (`worker_pool_refuse_headless`). Exclusion IS unreachability for every headless run — `claudeb … -p`, `codexb <name> exec`, `geminib profile <name> --print`, and any `worker-run start` naming the account — however the account is named; the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`) is the one override. Spend consent is ALL it is: nothing that only reads an account may consult it — `bin/llm-refresh` refreshes an excluded account like any other and `claudeb revive` takes one. An interactive launch is the user, never a worker, and is never gated, which is why every member including the last one may go out: an empty pool means no worker may run, and `worker-pick` says so in its own words (`every <vendor> account is out of the worker pool`) so `worker-run` reports `OUTCOME: <VENDOR>_UNAVAILABLE` instead of a usage limit. One rule the helper itself enforces: a file that exists but cannot be read means "everything is out" for readers and is never rewritten by writers | `share/worker-pool.sh`; `bin/claudeb`, `bin/codexb`, `bin/geminib` (`is_disabled`/`set_disabled`, `enable`/`disable`); `llm-limits.sh` (`enabled` per account, `ROT off`); `bin/worker-run` (the wall on `--account` and the empty-pool outcome); `bin/codex-image` (the same wall, since image runs launch `codex` directly); `bin/worker-pick` (excluded from every vendor's eligible set but not its pin resolution; per-vendor empty-pool reason); `hammerspoon/llm-limits.lua` ("In worker pool" checkbox on every account row, sole rows included) |
| r | Claude leg selection announcement | the stderr literal `claudeb: worker-pick selected <name>` is a parsed contract, not free prose: claudeb prints it on every routed headless run, `ask_claude.sh` seds it into the audit row's `account` field, and the legs test stub reproduces it byte-identically | `bin/claudeb` headless routing announcement; `ask_claude.sh` audit account extraction; `tests/test_legs_routing.sh` claudeb stub |
| s | User-entry refresh classification | `needs_user_entry:true` is the structured marker for a refresh outcome that requires human account entry; a vendor error carries it only when every `"; "`-joined entry has that class, and the affected account carries it independently. Every composite cause, including multiple Claude auth failures, uses `"; "` between entries; individual stale-cause text must never contain that separator | producer and classifier `llm-limits.sh`; consumer `hammerspoon/llm-limits.lua`; guards `tests/test_llm_limits.sh`, `tests/e2e_surfaces.sh`, and `tests/test_consistency.sh` |
| t | Review receipt path and schema | `<state_dir>/receipts/<repoName>__<repoHash>.json`, directly under `receipts`, where `repoName` is the basename returned by the repository identity rule and `repoHash` is the first 8 lowercase hex characters of SHA-1 over the canonical full repository path; one JSON object with string fields `repo`, `tree`, `commit`, `run_id`, and `ts`, non-negative integer `errored`, an optional positive integer `panel` naming how many cells the run launched (absent on receipts written before it existed), and `tree` is the reviewed commit's Git tree object. A lens run writes a sibling `<repoName>__<repoHash>__lens-<slug>.json` with an extra `lens` field and never the plain name, so a review run by a methodology the tool did not write can never be read as the repository's own. A scoped worktree run — one narrowed to named paths with `--paths` — writes a sibling `<repoName>__<repoHash>__scope-<slug>.json` with an extra `scope` array field, where `<slug>` is the first 8 lowercase hex characters of SHA-1 over its normalized paths joined with NUL; it likewise never writes or advances the plain name, so `receipt` cannot read a review of part of the tree as a review of the repository. The record is keyed on the repository and a chat is not, so no surface outside `bin/review-bench` reads it: what a chat owes is answered per session by the gate's verdict (row `ah`), and a badge rendered off this file would answer for reviews other chats ran | writer and reader `share/rbench/store.py` |
| u | Late review threshold | `3` × the base cell's recorded median duration, with a `120`s (`120000`ms) floor | writer/reporting `share/rbench/report.py`; reader/coloring `bin/statusline.sh`; live-progress prose `docs/statusline-contract.md` |
| v | Account data age | `as_of` is the oldest valid timestamp among `five_hour`, `weekly`, and `fable` windows whose `used_pct` is numeric; `stale_seconds` is its non-negative age. Absent and null-valued windows do not participate, and an account with no data-carrying window has neither field. Vendor aggregate age applies the same rule to its hoisted windows. Metadata with separate freshness retains its own timestamp: Codex reset credits use their capture time. | primary producer `llm-limits.sh` `data_as_of`/`set_data_age`; independent routing freshness `bin/worker-pick` `account_as_of`; consumers `llm-limits.sh` table/plain renderers and `hammerspoon/llm-limits.lua` |
| w | Owner-only review panels | tier `T3` and any tier's `--max` run only once Egor has named one himself; nothing in the tool ever proposes a panel, so a passing mention can never become what runs. The permission is an empty marker file `<state_dir>/review-grants/<panel>`, one per panel (`t3`, `max`), whose mtime is the whole state: a marker younger than `1800`s unblocks, nothing is spent, and a stale or absent one blocks. Both sides refuse independently and fail open on anything they cannot read. The tool side's terminal exemption is Egor's own shell only: a terminal on both ends AND no agent runtime in the environment (`CLAUDECODE`) | `share/rbench/{store,cli}.py` `OWNER_TIERS`/`OWNER_GRANT_DIR`/`OWNER_GRANT_TTL_S`, `owner_named`, `owner_at_keyboard`, `guard_tier_owner`; `bin/review-owner-gate.sh` `prompt` (writer) and `bash` (reader) modes; registrations in `~/.claude/settings.json`; phrase mapping in `~/.claude/docs/review-tiers.md` |
| x | Claude account existence | an account exists when ANY claudeb store knows it: `tokens/<name>`, `limits/<name>.json`, or the `~/.claude-profiles/<name>` profile directory; `main` and `-` are reserved non-accounts on every surface. `bin/claudeb` `account_names` is the single enumerator (status, enable/disable, warm, probes, and pin validation all consult it); the limits menu lists strictly from the vendor cache, which those probes populate for the same set; account birth (add, first-launch profile create) and death (remove) fire the announce hooks so the cache follows without a manual refresh | `bin/claudeb` `account_names`/`account_exists` and the `main`/`-` filter; `llm-limits.sh` claude-section `main`/`-` skip; `share/account-announce.sh` `announce_account_added`/`announce_account_removed` wired into `bin/claudeb`, `bin/codexb`, `bin/geminib` |
| y | One limits view for every surface | bucket display semantics are defined once: a bucket is `expired` when its reset epoch is real (≥ `31536000`) and ≤ now, and a reset over `86400`s past is dropped outright — the collector nulls the field and `limits_reset_text` prints `-` — because the window it named has rolled over unseen and every surface reads the field as a schedule; the drop is read AFTER expiry, so a dead reading never loses its `expired` verdict by losing its date, `stale` per row a's thresholds (or auth expired / cached origin), and its `effective` value is `0` when expired, else the raw pct; a text cell shows the EFFECTIVE value with `~` (stale) and `!` (expired) markers, a bare `-` when unmeasured; reset cells and age text share one formatter; per-account age is the oldest measured bucket (row v). Every surface renders these semantics from the same defs or from fields the collector computed with them — no surface re-derives its own. Any probe/warm that rewrites snapshots announces a passive collect (`announce_limits_probed`) so the merged cache and menu move with the snapshots; the collector suppresses announces in its own children (`LLM_LIMITS_ANNOUNCE_SUPPRESS`) to avoid recursion and double writes; a pressed `r` in the status TUI carries `CLAUDEB_WARM_USER_EXPLICIT=true` so the token-freeze experiment cannot silently eat a user-explicit refresh | definitions `share/limits-view.sh` (`LIMITS_VIEW_JQ`: `limits_bucket_expired`/`limits_reset_ancient`/`limits_bucket_stale`/`limits_effective_pct`/`limits_pct_text`/`limits_reset_text`/`limits_age_text`); consumers `bin/claudeb` `account_data`+renderers and `llm-limits.sh` `meta`/`mark`/table/plain; `hammerspoon/llm-limits.lua` renders the collector's `effective_pct`/`stale`/`expired` fields only; `bin/statusline.sh` judges its raw account snapshot (`5h`/`wk`) with `limits_bucket_expired`/`limits_bucket_stale`/`limits_effective_pct`/`limits_reset_ancient` and renders the `fb` row off the collector's fields exactly as the menu does (dim = stale or expired, value = effective); live parity guard `tests/e2e_surfaces.sh` store-consistency check; announce plumbing `share/account-announce.sh` + `bin/claudeb` `probe_accounts`/warm dispatch + `llm-limits.sh` child env |
| z | Lens registry location | `*.md` directly under `lenses/` beside `bin/`, which `REVIEW_BENCH_LENS_DIR` overrides whole (tests only); a lens is identified by its frontmatter `name` slug, never by its filename | `share/rbench/prompts.py` `LENS_DIR`/`lens_dir()`; registration prose in the `review-bench` skill (`~/.claude/skills/review-bench/SKILL.md`) |
| aa | Hammerspoon launchd agent identity | label `com.egor.hammerspoon`, wrapper `/Users/egorloy/.local/libexec/hammerspoon`, log `/Users/egorloy/Library/Logs/com.egor.hammerspoon.log`; renaming any of these must move every site together or the env guard's ownership probe and log cap silently miss | `launchd/com.egor.hammerspoon.plist` `Label`/`ProgramArguments`/`StandardOutPath`/`StandardErrorPath`; `hammerspoon/config/env_guard.lua` `launchctl print` probe and log-cap path; prose `docs/DIAGNOSTICS.md` |
| ab | Review report frame | every report block a hook or the bench emits is its word centered in `=` to a total line width of `50` with one space each side, closed by a bare rule of `50` `=`; the generic shape consumers locate a block by is `^=+ [a-z]+ =+$` / `^={10,}$`, and the review consumers narrow the header to its own word WITH the ROUND it carries and the state suffixes of row `as` — `^=+ review(?: · round <N>)?(?: · (?:NOT FINISHED|NO PANEL|STALE · <D Mon>))? =+$` — so a commit or push report framed identically is never taken for one, while the `bench` word (an untiered `--raters` panel, which settles no debt, owes no fixes and is no round of anything) stays deliberately outside that narrowing; the round segment is optional in the regex and mandatory in what the tool emits, because a consumer that requires it withholds every block recorded before it existed while one that forbids it finds none of the blocks recorded since; the width and those single spaces are what all of it rests on, and a suffix spelled on the emitting side alone is a report that stops being found the moment it starts mattering | emitters: `share/rbench/{round,report}.py` `REPORT_FRAME_WIDTH`/`REPORT_FRAME_WORD`/`round_frame_word()`/`report_frame_header()`/`report_frame_word()` and `../claude-setup/hooks/commit-report.sh` `FRAME_WIDTH`/`frame_header`/`rule`; consumers, both of which PRINT the block they locate rather than asking the model for it: `../claude-setup/hooks/review-report-nudge.sh` and its Stop net `../claude-setup/hooks/stop.d/notice-review-report-delivery.sh`, each keyed on `HEADER_SPAN_RE`/`FOOTER_SPAN_RE` |
| ae | Account pin ownership | the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`) is Egor's own override above the pool, and a session moves it only after he names it: naming an account to work on is not naming the pin. The permission is an empty marker file `<state_dir>/pin-grants/pin` whose mtime is the whole state — younger than `1800`s unblocks, nothing is spent, a stale or absent one blocks — and it opens BOTH directions, since a pin he set is not a session's to clear either. Three doors refuse independently and fail open: the command door inside `worker_model_pin_account`, which every spelling of `claudeb\|codexb\|geminib use` reaches, and — because a door on one way to the file is a door around the other — an `Edit\|Write` door and a `Bash` door for a session writing `~/.claude/worker-model` by hand. What the file doors deny is the PIN, not the file: the same file carries `worker=`, `*_model=` and `*_effort=`, which `/worker` rewrites directly, so a write leaving every `*_profile=` line as it found it passes ungated. Every door compares the resolved file, never the path text — `//`, a `..` hop and `~` all name the one pin. The command door applies only to a session (`CLAUDECODE` in the environment) writing the REAL file — the menubar shells out from Hammerspoon carrying neither, and a fixture named through `WORKER_PICK_CONFIG_FILE` is a test's own. Reading the pin is never gated. The one write no grant gates is the account ending its own pin: `worker-pick` removes a pin whose account hit its USAGE wall (`worker_model_clear_walled_pin`, the exact name it measured and only on data it calls fresh), because nobody chose that and a pin exists to be spent — every other lapse, dead auth included, leaves it standing for the window to reset | `share/worker-model.sh` `WORKER_MODEL_PIN_TTL_MIN`/`worker_model_pin_grant`/`worker_model_canonical_path`/`worker_model_pin_allowed` and its refusal in `worker_model_pin_account`; `bin/worker-pin-gate.sh` `prompt` (writer) plus `write` and `bash` (readers); registrations in `~/.claude/settings.json`; guard `tests/test_worker_pin_gate.sh` |
| af | Round bands have one voice | what a triaged round earned — the three bands and the dials between them (`ROUND_FIX_MAX = 8`, `ROUND_HARD_MIN = 20`, `HANDOFF_P1_STOP = 3`) and the four words a decision may be (`DECISION_WORDS` = fix, simplify, cut, redesign, spelled once and used identically by `fork --choice`, the `fixes:` and `decision:` rows and the Russian decision text) — is decided only inside `share/rbench/round.py` (`round_band`, `round_decision_owed`, `fork_missing`), and every other site asks it there. Two numbers and nothing else, counted over the WHOLE round and never per repository: a merged panel is priced exactly as a solo one, and pricing it per member let three findings spread over three repositories stop a pass no member's threshold reached. Both dials count CODE findings alone (`share/rbench/{panel,round}.py` `docs_finding`, and `severity_tallies` over it): a confirmed finding whose file ends in `.md` is fixed like every other and prices nothing, because documentation is prose an LLM may never read while code executes on every run — so a round of nine confirmed findings of which two are docs is a round of seven, inside the fix band and owing no decision. The same split is on the report receipt every later reader prices a round from (`confirmed` and `confirmed_by_severity` code-only, a `docs` integer beside them) and in the `confirmed:` row's ` · <n> in docs` tail, which is where the fixing pass reads that its own two counts still answer for all of them. Every speaker is handed the answer there and nowhere else: the `fixes:` and `next:` rows of every report block, `fork --check` and the three gates that only relay its exit code, and `round_covers_its_fixes`, which lets a commit close a round only in the fix band, on a decision naming `fix`, or on a round 2 — a caller pricing that decision itself would be the second copy of the dials this row forbids. No other module of the package spells a dial or one of the four words — `share/rbench/debt.py` spells none at all — and the only second spelling anywhere is the gate's own pair of literals, which words the ask and decides nothing (row `ap`); a copy beyond those is the failure this row exists to prevent. There is no third band and no fifth word | `share/rbench/round.py` `ROUND_FIX_MAX`/`ROUND_HARD_MIN`/`HANDOFF_P1_STOP`/`DECISION_WORDS`/`round_band`/`round_covers_its_fixes`, the report `fixes:`/`next:` rows; `../claude-setup/hooks/review-flow-gate.sh` and `../claude-setup/hooks/stop.d/ask-review-report.sh` relaying `fork --check`; guards `tests/test_review_bench.sh` and `tests/test_consistency.sh` |
| ah | The statusline speaks the gate's verdict | one question, three implementations in a row, and a word renamed on any of them goes silent rather than loud. `review-bench debt --repo <top> [--session <sid>] [--paths <p>...]` prints exactly one line and always exits `0`: `none`, `debt <n> mine|other|unknown [<owned>] [(+<f> foreign)]`, or `timed-out <run-id>`; `--list` prints the debt paths themselves instead of that line. The owner word is the third field and the only one a reader switches on: `mine` iff the asking session owns at least one debt path, `other` where it owns none, and `unknown` where no session was named at all, since a reader that computed no ownership must not assert one. `<n>` is ALWAYS the whole debt and never a share of it, whichever words follow: it is the field every reader prices the repository by, and a line answering `0` over work nobody has read is the one thing it may never say. `<owned>` follows `mine` alone and only where the session owns a STRICT subset, so a chat owning one path of ten no longer reads the same as one owning all ten; `(+<f> foreign)` stands where `n` holds debt a `--debt` review will not read — another chat's, which is the one set `debt_foreign_skipped` answers for on both surfaces — and is withheld where the owner word already says it (`other`, every path foreign) and wherever no session was named at all, since with no asking chat that review skips nothing and a count of an exclusion that never happens names a scope no review will take; where no session was named and EVERY debt path is recorded to some chat the ownership word is dropped instead — `debt 2` — because `unknown` over debt whose owner is on record states the one thing the records contradict, so a positional reader must treat a third field that is not one of the three words as no word at all. No state word ever follows it: what a ROUND owes is asked of that round (`fork --check`, row `af`) and is never readable off a repository's count, and a trailing word here is a second answer to that question on every surface that reads this line. Nothing may parse positionally past the owner word. `timed-out` outranks the debt underneath it but never an empty repository: over content every artifact settles, the answer is `none` and the kill is history, or one dead review would hold a red statusline no artifact could ever clear. `debt --split` answers the same question for a machine, in DIFF LINES and in one line of its own: `split <own> <foreign> <orphaned>`, the lines between each debt path's covering artifact and the content standing there now — counted by `diff_numstat`, the differ the review target header prints `N file(s) · M line(s)` with, over the two contents as files, since working-tree bytes are in no object store and hashing them into one would make a render write into the repository it is reading. A path whose recorded side cannot be READ — nothing ever held it, or the blob its artifact named is gone from this store — is priced by where its unreviewed work stands: dirty in the working tree, against the content HEAD holds (whole where HEAD holds no such path); clean, against the parent of the oldest commit reaching HEAD at or after the earliest epoch either journal stamped for it, less the width of the post-commit hook that writes that stamp (`JOURNAL_STAMP_GRACE_S`), since with the tree clean HEAD IS that work; and with no epoch stamped or no commit under one, against HEAD, which owes no lines while the path count still names it, because nothing on record says which commits went unreviewed. A held path that is gone counts what it lost, one side being empty. The review BASE is that same left side and no other: `debt_base_blobs` answers both, so the panel reads exactly the lines this prices. Only a path HEAD does not hold is dropped from the base and shown whole, and only a reopened round's paths carry an empty side of their own (`unread_artifact`), since the fork's ruling is that the second pass rereads the full original scope plus the fixes. Spelled twice the two drifted apart, and an 84-line edit reached the raters as 11.9k lines of untouched code with twenty findings in it (live case 2026-08-24). An artifact recording working-tree shas writes those objects into the repository (`stored_path_blob_shas`, a command only), or its own next pricing reads what it recorded as that absence. A path this repository's attributes take out of diffing (`-diff`) counts the lines the header gives it, which is none: the comparison below is of two files OUTSIDE the repository, which no `.gitattributes` pattern can name. `<own>` folds the asking chat's journal entries and its own runs exactly as the owner word does; `<orphaned>` is the debt of paths NO journal entry names and no run record claims, separated because it belongs to nobody and counted as the asker's it reports another chat's work as theirs to answer for. The counts are cached at `<state dir>/debt-lines.json` keyed by the two contents ACTUALLY compared and by nothing else — never by a run id, which moves while the content does not; never by a sha read before the bytes were, which the file may have moved past since; and never with the sha of a blob this checkout could not produce, or the number measured without it would answer for the checkout that holds it, the file being one machine's and not one repository's. The attribute case above is for that reason not cached at all. Fresh measurements are written out in batches rather than at the end alone, or a first pass killed by the gate's timeout persists nothing and the cache never warms. So the statusline's ~3s budget survives a repository whose debt is hundreds of paths. `review-flow-gate.sh verdict <repo> [session]` is the only translator of those into a statusline line — `off`, `bright rev <own>`, `dim rev <foreign>`, or `split rev <own>/<foreign>` when both stand — and it asks about the whole repository with no paths, since debt outlives the commit that landed it. It folds `<orphaned>` into the foreign side, which is the one place that fold is made. Nothing it prints is red: `bright` is normal weight (this chat's own debt), `dim` is everyone else's, and `split` carries both weights in one segment, bright up to the `/` and dim from it. The watchdog's kill has NO statusline consumer — a killed run settles nothing, so its paths stand in these numbers like any others, and the kill is seen through the report flow and `review-bench doctor`'s untriaged class — while `debt`'s own `timed-out <run-id>` line stays for its other readers. Like `escalation-verdict` (row `af`) it must answer from argv BEFORE the hook reads its payload, or a caller holding no pipe open hangs on it, and it is read-only by construction — it launches no panel and writes nothing, or a statusline render would spend a review. `bin/statusline.sh` renders that line subject only to truncation, the style word, and one label deduplication: when a classified verdict shares one repository with a live counter that already says `rev`, the statusline strips that leading `rev ` and leaves the numbers beside the counter; alone or under a foreign-repository anchor it keeps the word. It computes no class of its own, splits `split` on the first `/` and nowhere else, and shows a style word it does not know loud and whole rather than swallowing it. A second implementation of this question renders a label that disagrees with the gate that acts on it, and one of the two is always lying (Egor, 2026-08-09) | producer `share/rbench/debt.py` `cmd_debt`/`repo_debt`/`debt_split`/`debt_line_counts`/`debt_foreign_skipped`; translator `../claude-setup/hooks/review-flow-gate.sh` `verdict_mode`/`review_debt`; consumer `bin/statusline.sh` `review_gate_verdict`/`review_verdict_line`; segment row `docs/statusline-contract.md`; guards `../claude-setup/tests/test_review_flow_gate.sh`, `tests/test_review_bench.sh` and `tests/test_statusline_hooks.sh` |
| ai | Usage wall record | `${WORKER_STATS_DIR:-${CLAUDEB_DIR:-$HOME/.claude-profiles/.claudeb}/worker-stats}/walls.jsonl` plus, for OpenCode, the served-call stamp `<same dir>/opencode-seen/<account>`. JSONL records share fields `side`, `account`, `bucket`, `detected_at`, `reset_at`, and `window`. `reset_at` and `window` are optional. OpenCode keeps `bucket: "general"` for every quota window, so any active wall blocks that key for every request; `window` is one of `5-hour`, `weekly`, or `monthly` when known. The file holds RAW observations and nothing else: a row says one request was refused, never that the refusal still stands, which is row `al`'s question. It has two writers in two languages — `bin/opencode-go` at the 429 it sees (a `jq` append, so a probe or a worker walls the plan as visibly as a bench cell) and `bin/review-bench` for the `opencode` and `agy` sides — and TWO rules they must spell alike. The first is the ceiling: a `reset_at` the provider stated is capped once, by the writer, to the horizon its own window can reach (`5-hour` 21600s, `weekly` 691200s, `monthly` 2764800s, unnamed 604800s), so a garbled date cannot retire an account until someone notices by hand, and every reader takes the recorded value as final rather than re-clamping it. The second is the grammar that reads the stated horizon out of the refusal body, because the gateway states it in wordings both writers must accept whole: abbreviated units (`hr`/`hrs`, `min`/`mins`, `sec`/`secs`) alongside the spelled-out `second|minute|hour|day|week`, an optional space between number and unit, and COMPOUND values (`Resets in 9hr 30min`, `Resets in 2 hours 30 minutes`) whose components are SUMMED rather than read down to the first one. The unit alternation is longest-first and ends on a word boundary, so both `3 mins` and `3 minutes` are consumed whole rather than `min` matching inside the longer spelling and the remainder of the word cutting the run of components short. A body stating no reset still yields no `reset_at` — none is invented — and the two grammars are pinned against one shared corpus of real 429 bodies, so a wording taught to one side alone fails the guard. NO writer ever retires an OpenCode row by the clock — `bin/review-bench`'s compaction may collapse superseded duplicates into one row per account and window (newest detection, longest horizon) but never drops the account — and no reader may treat a row as spent because its `reset_at` passed: the gateway dates a reset to the day, so a window is routinely shut hours past the horizon it named. The one counter-evidence is a completion the plan SERVED — a 2xx from `/models` or any other path is not one, since the plan answers those while completions stay walled — and `bin/opencode-go` records it by writing that account's `opencode-seen/<account>` stamp, which row `al` reads against the rows. Both writes are single-file and independent, so no reader-then-writer window exists in which a 429 landing beside them could be discarded. The account a row is filed under is that plan's keychain service verbatim: `opencode-go` for the default profile and `opencode-go-<profile>` otherwise. `bin/review-bench` reads the same rows for its own launch question through its own clock horizon (`WALL_WINDOW_MAX_TTL_S`) and is not bound by this row's retirement rule — it asks whether a cell may be launched now, not whether the plan is spent — but the served stamp binds BOTH readers, and row `al` spells the one comparison they share | writers `bin/opencode-go` (`wall_row`/`record_call`/`record_wall`/`mark_served`) and `share/rbench/accounts.py` (`mark_walled`, `WALL_WINDOW_MAX_TTL_S`); pool reader `share/rbench/accounts.py`; display reader `llm-limits.sh` (row `al`); guard `tests/test_consistency.sh` |
| aj | Per-vendor role switches | a role is a per-vendor wall over the pool, spelled `<vendor>_<role>=off` in `~/.claude/worker-model` for `claudeb`/`codex`/`gemini` × `workers`/`reviewers`. The literal `off` is the only veto and an ABSENT key is the open state, so opening a role deletes the line rather than inventing an `=on` spelling; a hand-edited duplicate resolves FIRST-wins on every reader, or the menu shows one value while the routers follow another. `share/worker-model.sh` `worker_model_set_role <vendor> <role> on\|off` is the one writer — under the pin's own lock (row `ae`), and refusing a session (`CLAUDECODE`) outright, since closing a role redirects every worker and rater after it. The ladder is **pin > roles > pool** (`docs/routing-contract.md` Roles): a usable vendor pin answers over `<vendor>_workers=off` the way it answers over pool exclusion, while a reviewers query never sees a pin, so `<vendor>_reviewers=off` is final. The refusals are parsed contracts: `worker-pick: <vendor> is switched off for <role>` at exit `3`, which `worker-run` and `review-bench` both match to word their own, and `worker-pick: --role only means something with --account` at exit `2`. A workers wall reports `OUTCOME: <VENDOR>_UNAVAILABLE` and never `_USAGE_LIMIT` — a switch Egor flipped is not quota to hunt — and it holds over an explicit `--account`, a resumed session and the pin fallback alike, the vendor pin being the one override. In the cache line (row `c`) a workers-off vendor arrives as its own shape, `cb⏸off`/`cx⏸off`/`gx⏸off`, distinct from every account name | writer `share/worker-model.sh` `worker_model_set_role`; readers `bin/worker-pick` `role_off`/`--role` flag/`query_role_wall` and the per-vendor `*_role_off` flags, `bin/worker-run` `PICKED_ROLE_OFF` plus its own `${vendor}_workers` wall, `share/rbench/accounts.py` `reviewers_role_off`/`role_closed_note` and its `--role reviewers` query, `hammerspoon/llm-limits.lua` `WORKER_MODEL_PREFIX`/`WORKER_ROLES`/`readWorkerModel` with the For workers / For reviewers items writing through `M.setWorkerRole`/`WORKER_ROLE_SCRIPT`; render `bin/statusline.sh` `⏸off`; prose `docs/routing-contract.md` Roles, `docs/statusline-contract.md` worker row, `~/.claude/commands/worker.md` (keys `/worker` preserves and never writes) |
| ak | Auto-refresh vendor roster | the daemon refreshes exactly `claude`, `codex`, `gemini`, `opencode`, and the four are spelled together in the seed, the tick loop and the state validator, so a state file written before a vendor existed migrates into the default rung rather than failing validation. Three of them refresh by reading a free usage endpoint; `opencode` has none, so its cadence is INVERTED and no other vendor may be given that shape: with no walled account in `~/.llm-limits.json` it is dormant — it sends nothing, and that state stays true for free because every real call records its own outcome (row `ai`) — and only a standing wall is probed, one `opencode-go wall-check` completion per walled account, which the gateway answers with a free 429 while the window is shut. The daemon never passes `--probe-clear`: that flag belongs to the explicit `Hard refresh` of row `al`, and on a heartbeat it would spend a completion per clear account per tick. It asks the collector, never the wall record: a second implementation of whether a wall stands (row `al`) would spend the plan on its own say-so. The probe's answer is read as one of three states and never two: `walled` (a refusal this probe itself recorded), `served` (a completion the plan answered) and `dormant` (nothing was sent at all, the wall having gone between the read and the request) — counting the third as a reopening would report a leg recovered on a request nobody made. The tick lock is handed back for the duration of the probe, because a wedged gateway must not hold the other three vendors' ticks behind it. A probe that answered re-collects, or the row it just retired keeps standing on every surface until the next tick. The stated reset is day-granular and therefore a hint: inside `OPENCODE_TIGHTEN_WITHIN_S` (`3600`s) of it, or past it, the existing ladder tightens and keeps probing, because only a served request retires a wall. No surface may invent a percentage for this vendor | `bin/llm-refresh` `normalized_state` seed, the `for vendor in` tick loop, the state validator, `opencode_tick`/`opencode_walls_json`/`opencode_probe`/`opencode_next_interval`; probe `bin/opencode-go` `wall-check` and `wall_check_all`; guard `tests/test_consistency.sh` |
| al | OpenCode rows in the limits store | the Go plan publishes no usage endpoint — `usage`, `limits`, `me` and `account` all 404 — so `.vendors.opencode` carries no percentage and never will, and whether a recorded wall (row `ai`) still stands is computed HERE, once, by `llm-limits.sh`. One row per profile listed in `~/.config/opencode-go/profiles` (absent file = the single default profile `-`), in file order: `{account, walled, windows: [{window, resets_at}], as_of, stale_seconds}`. `account` is the PROFILE, mapped to the wall record's account name by row `ai`'s formula; `window` is `5h`/`wk`/`mo`, or `?` for a pre-window record, and a named window suppresses `?` beside it; `resets_at` is the recorded horizon as an ISO-8601 instant, `null` when the gateway never stated one, and it is DISPLAY only — a horizon already past retires nothing. The rule is one comparison and holds no clock: a window stands while the account carries a wall row `detected_at` no earlier than (`>=`) its served stamp, so a refusal outlives its own stated reset and only a completion the plan served ever ends it — and a tie goes to the wall, because only a standing wall is ever probed by the scheduled refresh, so a same-second pair read as served would freeze a walled account clean forever. That comparison has TWO implementations and they must stay spelled alike: `llm-limits.sh`'s `.detected_at >= $served` for every surface, and `share/rbench/accounts.py`'s `read_wall_rows`, which drops an account's rows below its stamp BEFORE aggregating a standing wall — the same order, or a stale long-horizon row shadows the 429 recorded after the completion and answers for the account anyway. A missing or unparsable stamp is no evidence and keeps every row, so an account nobody ever served stays walled on both. The bench answering this question its own way is what refused a whole OpenCode leg for a day (2026-08-15) on an account whose plan was serving other models fine. `as_of` is that served stamp alone — a 429 is not evidence the account is alive, and dating the row by one would show a walled account getting fresher the more often it refuses — so an account never served carries no age at all. The vendor is assigned LAST in the collector pipeline, past every pass that reads a percentage — staleness, expiry, `usable_now`, and the empty-vendor verdict that decides the exit code — because each of those would be inventing a reading; `--plain` and `--table` skip it for the same reason. Every other surface RENDERS these rows and re-derives nothing: `hammerspoon/llm-limits.lua` reads them like any vendor's, `bin/llm-refresh` picks its probe targets from them, and `opencode-go wall-check` asks them before spending a completion — unless `--probe-clear` makes the asking itself the point: the menubar's `Hard refresh` runs `wall-check --all --probe-clear`, which walks the roster file instead of the walled rows and spends one small completion on every clear account, since `as_of` is the served stamp and nothing but a served completion moves it. Anything scheduled (row `ak`) stays walled-only | producer `llm-limits.sh` `opencode_profiles`/`opencode_seen_tsv`/the `opencode=` jq; consumers `hammerspoon/llm-limits.lua` `appendOpenCode`, `bin/llm-refresh` `opencode_walls_json`, `bin/opencode-go` `wall_check`/`wall_check_all`, `share/rbench/accounts.py` `opencode_served_at`/`read_wall_rows`; guards `tests/test_consistency.sh` and `tests/test_review_bench.sh` |
| am | Worker files reach the launching chat | a worker a chat spawned IS that chat, so the files it authored are that session's own work for review coverage — the alternative is a run of hundreds of worker-authored files priced as the one edit the chat made by hand (Egor, 2026-08-17). The carrier is the RUN RECORD, not a report anyone read: at launch `worker-run` writes `<run-dir>/launcher` holding the `CLAUDE_CODE_SESSION_ID` that spawned it (nothing when there is none, or when the id is not a plain name), and after EVERY attempt — a walled one, a failed one, and one that never reached its workdir included — `<run-dir>/files`, whose first line is `WORKDIR: <dir>` and whose remaining lines are that run's paths, workdir-relative or absolute when they fall outside it, unioned across attempts because rerouting restamps the run's start and takes a new session. Every vendor answers out of its OWN transcript — claudeb's session jsonl, codex's rollout (`patch_apply_end` changes unioned with the `*** ... File:` targets in the patch text, since each is missing where the other is present), agy's `transcript_full.jsonl` under its conversation id (never the `transcript.jsonl` beside it, which drops the tool_call arguments) — and the answer is exact only where EVERY mutating action named its target: a write with no target, a shell command matching `worker-run`'s one write-pattern list, or a call the reader cannot classify leaves the whole run unanswerable rather than half-listed. `UNKNOWN: <reason>` replaces the paths when the list cannot be established at all (one of those refusals, no workdir, transcript unreadable) and `PARTIAL: <reason>` stands beside real paths for either of two reasons — the run also ran shell commands, whose edits no transcript records, or it named a write target the transcript cannot resolve, a token carrying an unexpanded `$name` or a backtick, which is text and not a path anybody can attribute (`classify_tool_rows`; the patch text of a run editing this suite's own codex fixtures nests their `*** Update File: $cx_workdir/...` headers and one reached a live run's file list as a file, 2026-08-24 — the same rule as row `ao`'s `rj_command_target`); a record is always written, because no record is what an unfinished run looks like and both readers take that absence for "this run wrote nothing". Beside that list stands `<run-dir>/worker-session`, the run's OWN session ids one per line — claudeb alone, since no other vendor's sessions run this machine's journal hooks and an id lifted from one of those could match a journal record only by accident — written only where the vendor's output names one and never guessed at, and unioned across attempts for the reason the paths are. Beside them stands `<run-dir>/workdir-escape`, written only where the run's own calls named NO path inside its workdir — the silent agy failure of working in the first `--add-dir` instead (live 2026-08-24), which every report says out loud and which is not the ordinary case of a run that also wrote outside its workdir. Beside both stands `<run-dir>/dirty` — the paths the run's REPOSITORY gained uncommitted content on while it ran, spelled against that repository's top under a `WORKDIR:` line of its own — written only where the listing carries `UNKNOWN:`/`PARTIAL:`, which is the run saying it cannot name its own files, and bounded twice: by `<run-dir>/dirty-before`, the paths already uncommitted when the run was launched — written only where git could answer at all, since a floor that is missing and a tree that was clean are the same empty set to every reader, and no floor on record means no snapshot is taken, and carrying each of those paths' CONTENT beside its name in `<run-dir>/dirty-before-shas` (`<blob-sha>\t<path>`, `-` where git could not hash one), because subtracted by name alone a path that was already open is invisible however far the run moved it: a fixing pass whose every edit landed in files a co-tenant had open reached the record as nothing at all (live case 2026-08-22), so a floor path whose sha MOVED is gained too and one standing at its floor sha is not — and by the listing's own paths, which carry an owner already and are spelled against the repository top for that subtraction, the listing answering against the WORKDIR. It closes what `worker-session` still left open: an edit made through the shell that the worker's own hooks never journaled either reaches no store at all, so `review-bench debt` answered `none` over two rewritten files and a merged `--debt` panel scoped straight past them (live case 2026-08-21). It is evidence about CONTENT and never about authorship — `git status` answers for a shared checkout — so `share/rbench/store.py` `run_dirty_paths` folds these paths into the debt UNIVERSE owned by NOBODY and never into `run_record_claims`, which names a launcher: misattributed debt is answered by the wrong chat or by none, while anonymous debt is answerable by whoever looks at it. NO reader reads it as ownership, not even the chat that launched the run: a chat answers for a path only through a record that NAMES the path for it, and the dirt of a shared checkout names nobody — read as the launcher's own, three co-tenants' commits came back as one chat's debt on its statusline (live case 2026-08-25). The same records answer for a fixing pass a codex or gemini worker ran, which journals nothing and lists no files at all: inside the round's window its launcher, its workdir and its dirt are the only evidence that pass wrote anything, and a co-tenant's blind run over the same path takes the answer back. Read only from a record carrying an `exit_code`, since the snapshot of a run still writing is still moving, and read past the `journaled` marker, which retires the LISTING alone — no sweep will ever name a path the listing never held. The run's own listing is part of that universe too, for the same reason: a path a worker named that no journal has swept and no artifact ever held was in no count, no `debt --list` and no `--debt` scope until some later commit swept the run. A record whose `WORKDIR:` sits in a CHECKOUT OF THE FAMILY — the in-repo worktree convention `<repo>/.claude/worktrees/<name>` first — is spelled against that checkout's own top and so names the same repository-relative files the asking checkout holds, which is the family rule `review-bench debt` reads every artifact under (`docs/review-contract.md` §Debt): a worker a chat launched in its worktree is that chat's own work in the main checkout too. Neither reading takes a record whose workdir is a working tree of its own that is NOT of this family, since those paths read as this repository's by spelling alone while its git does not track one of them. It is what closes the shell-only blind spot: a run that edited through the shell names no file at all, while the worker session's own `commit-journal.sh` journaled every one of those edits under ITS id, which no chat in the repository answers for — so the gate priced the work as nobody's and the launching chat committed it unreviewed twice (live case 2026-08-20). `review-flow-gate.sh` folds the commit-journal entries owned by those ids into the launcher's pending set, priced whether or not the listing was ever swept — the `journaled` marker retires the LISTING, and nothing ever renames what the worker journaled — and keyed only where no run of this chat is still writing under that session, `--resume` handing one id to several runs; never for a run another chat launched, whose launcher answers for it. `share/chat_names.py` `worker_session_launchers` reads the same file — one walk, since `bin/review-bench` and the chat-name resolver of row `aw` fold a worker to its launcher for two different questions and must not answer them differently, so `debt --session <launcher>` counts those paths as the launcher's: the launcher JOINS the record's authors and the worker id stays beside it, since the gate inside the live worker asks this same question under that id and answered `other` it would be refused a waiver over the work it had just done. `commit-journal.sh` sweeps the store on EVERY tool call — every `*/launcher`, the launching chat read off the file itself — judges every record through ONE predicate, `rj_run_final` in `hooks/lib/review-journal.sh`, which `commit-report.sh` reads too so the two hooks cannot disagree about whether a run is over: an `exit_code` on record (the run's own statement that it is over, whoever launched it — held for a liveness probe instead, a co-tenant's finished record with no launch stamp reads `unknown` and parks for the stale release while nothing else names its files and the edit notice skips it for having an exit code), a supervisor `rj_run_liveness` reads DEAD, or the age release below. A run that is not final is left alone entirely — not swept, and NOT retired: a record retired for an unusable launcher name while its run may still be writing takes the edit notice's warning about those files with it. It skips a run with no `exit_code` whose supervisor pid is still alive (its list is not final) while sweeping one whose supervisor is GONE, since nothing will ever write that exit code and the list is as final as it will ever be, journals what it read under the LAUNCHER's session id — stamping the debt of a listed path even where it already matches HEAD, since the listing is itself the record of who wrote the file and a worker that committed in its own shell lists nothing else — and marks `<run-dir>/journaled` only over a record it actually read, a path the record format can never hold (a control character in its name) counting as read since no later call can change it. Three more answers are TERMINAL for the same reason — no later call can make them come out differently, and counted as unrecorded each re-swept its run on every tool call of every chat for as long as the record sat on disk: a launcher naming no usable session id, a listing line that no `WORKDIR:` and no `meta.json` workdir can anchor, and a record whose listing never arrives. Each is said once on stderr AND once to the model as `additionalContext` — a PostToolUse stderr on exit 0 is read by nobody, so the one reader who could review these files, waive them or hand them to whoever owns them never learned they exist — behind a per-key marker in `$HOME/.cache/claude/review-journal/`, the same scheme the library notes use, and then retired. The sweep also consumes `<run-dir>/deferred-paths`, the rows row `ao`'s claim scan leaves for paths a commit carried under this run's workdir while it could still name its own files, and journals each under the run's OWNER only where the run's own `files` listing NAMES that path: the workdir the commit was passed over on is a promise that this sweep will name the path, never evidence that the run wrote it, and taken as evidence it makes every path a co-tenant committed under a repository-wide workdir that launcher's work — the same directory guess this row refuses everywhere else. What the listing does not name — and everything of a run whose launcher names nobody a journal entry can carry — is stamped with an EMPTY session, the journals' own spelling for a path in debt no record answers for (row `ao`), and said once to the one reader present. Read on every call and not only at retirement, since the `journaled` marker closes this sweep to a record while a commit may defer a path against it long after — but never before the record is FINAL (`rj_run_final`) or already `journaled`, since the listing that decides all this is written at the run's end and a path the run is about to name would read as nobody's for good; consumed by the same move-aside the note spool uses, so a report appending mid-read loses nothing, and a row the journal could not take is written back rather than dropped — exactly as the listing sweep declines to retire a run whose paths it could not record, since a row consumed on a failed write leaves that path owed by nobody with nothing ever looking at it again. The key and the delivery are the run's OWNER's, never the sweeping chat's: where the two differ the sentence waits in `<owner>.commit-journal.notes` for that chat's own next call to collect, since printed here it reaches a reader who can do nothing about it while the chat whose files are named nowhere never hears they exist. A record that will never gain a listing at all — an exit code on record, the supervisor gone, or a liveness nothing can ever establish released by age, and `files` never written — is retired unread, since there is nothing left to import and every reader of it holds that record's whole WORKDIR as pending work on the strength of the missing file. Retired is not resolved, and nothing inherits what the record never named: a run whose files were never named leaves no claim for a later commit to be read against, and the paths it wrote are nobody's until some record names them (row `ao`). A run of ANOTHER chat is swept on that same evidence and nothing weaker — dead, or an `unknown` record past `RUN_RECORD_STALE_AFTER` — whether or not it carries a launch stamp, since a `ps` that answers about no process at all keeps a record unknown on every call there will ever be — because the launching chat may never take another tool call and its dead worker's files would then belong to nobody for good; live and unknown foreign runs are left alone, and the `journaled` marker settles two sweepers racing over one record. `review-flow-gate.sh` reads the same records for its pending set alone: the paths of a run still going, since nothing else prices them while it has no `exit_code`, and those of a finished run the journal has not swept yet (a worker that finished between the chat's last tool call and its commit reaches the commit through no journal entry at all), plus the journal entries its `worker-session` ids own — folded ahead of the `journaled` marker, which retires the LISTING alone. It names no run to anybody and never refuses a commit (`docs/review-contract.md`). What a reader is told about a record that has not finished naming its own files is said once and AFTER the commit, by `commit-report.sh` `scope_note`, over the paths that commit actually carried and no others: a run whose supervisor is still running reads differently from one held live by a `ps` that answered nothing, so the line carries the record's own liveness and the age behind it. A record naming no tree at all is named wherever the chat is committing rather than filtered out of every repository. `share/rbench/debt.py` `cmd_waive` reads the same records for the opposite question — a run of ANOTHER session, not yet `journaled`, owns the paths its `files` record names, and a waiver naming no path drops them instead of signing for work a co-tenant's worker is still writing. A listing the run could not complete is NAMED afterwards rather than left orphaned: `wait` ends on an `UNNAMED:` line spelling out every `dirty` path exactly as a claim takes it while they are few enough to read, and past that cap naming the exact count and the `dirty` record holding the list instead — the 887-path case printed multiple kilobytes on one line at the end of every wait, and `worker-run claim <run-id> --paths <p>… [--complete]` writes the launching chat's answer into `files` as ordinary rows — that chat alone (`launcher`), only once `exit_code` is on disk, only for paths under the run's workdir or elsewhere in the REPOSITORY that workdir sits in — the tree the dirt record is spelled against, since a run launched in a subdirectory has that record named against the top and the workdir alone refuses the very command `wait` prints — refused whole rather than half applied, dropping each claimed path from `dirty` and retiring the `PARTIAL:`/`UNKNOWN:` caveat on `--complete` alone; the hole itself is closed at the source by `../claude-setup/hooks/worker-edit-guard.sh`, which denies a worker session's shell write into its own repository so the listing has the Edit/Write calls it is built from. The three readers spell the store one way, `WORKER_RUN_DIR` over `$HOME/.cache/claude-worker-runs`, or one of them answers about a directory nobody writes. A change to this machinery is reviewed with the `parallel-sessions` lens (`review-bench lens list`): its defects appear only beside another working chat, which a default review never imagines | producer `bin/worker-run` `persist_run_files`/`compute_run_files`/`record_worker_session`/`record_workdir_dirt`/`record_workdir_escape`/`workdir_escape_line`/`unnamed_line`/`claim_run` and the `launcher` and `dirty-before`/`dirty-before-shas` writes in `start_run`; consumers `../claude-setup/hooks/commit-journal.sh` `journal_worker_runs`, `../claude-setup/hooks/review-flow-gate.sh` `scan_run_records`, `share/rbench/{store,round,debt}.py` `foreign_run_claims`/`run_record_paths`/`run_dirty_paths`/`blind_fix_runs`/`repo_debt` and `share/chat_names.py` `worker_run_root`/`worker_run_launchers`/`worker_session_launchers`; policy `docs/review-contract.md`; guards `tests/test_worker_run.sh`, `../claude-setup/tests/test_worker_edit_guard.sh`, `../claude-setup/tests/test_commit_journal.sh`, `../claude-setup/tests/test_review_flow_gate.sh`, `tests/test_review_bench.sh` and `tests/test_consistency.sh` |
| an | Launching-chat pid walk | the harness registers a live chat as `<registry>/<pid>.json` with a string `.sessionId`, registry `~/.claude/sessions` (`share/rbench/store.py` takes `REVIEW_BENCH_SESSION_DIR` in its place for tests). A run is that chat's grandchild several execs down, so the launcher is found by walking `ps -o ppid=` up from the process's own pid, at most `15` hops and stopping at pid 1, taking the first entry whose `.sessionId` is a string and naming nobody where none is. TWO implementations, in two languages and at two moments: `bin/review-bench` walks ONCE at run start and records the answer as the progress document's `session`, because the chain is alive only then — a backgrounded run outlives its launcher and reparents to pid 1 — and `bin/statusline.sh` walks at render time ONLY for a document written before the writer recorded one. Which is also the precedence every reader owes: the recorded `session` first, the walk as the fallback, or a run the render attributes to nobody is dropped from the statusline of the very chat that started it | writer `share/rbench/store.py` `walk_launching_session`/`SESSION_WALK_HOPS`/`SESSION_REGISTRY_DIR_ENV`; reader `bin/statusline.sh` `review_run_session`/`review_run_owner`; segment row `docs/statusline-contract.md`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh`, `tests/test_statusline_hooks.sh` |
| ao | Review debt journal | whose debt a commit landed is recorded in `<journal-dir>/claude-review-debt` (row `bd`: the git family's COMMON dir), in the SAME record format as the commit journal beside it (row `am`'s carrier): `<session-id>\t<epoch>\t<repo-relative-path>`, NUL-terminated, entries with no TAB owned by nobody. Three hooks write it, all by APPENDING one record through `hooks/lib/review-journal.sh` `rj_append`: `commit-journal.sh` at the EDIT, which is where ownership is known and the only write a commit arming no notice cannot skip; `review-flow-gate.sh` at the commit that goes through, appending a FRESH record for whatever the notice named; and `commit-report.sh` at a commit it saw LAND, for every path carried by every commit that ONE call landed, in every repository it landed one. WHICH commits those were is EVIDENCE, never inference: `review-flow-gate.sh`, PreToolUse on the same Bash call, writes `<repo-top>\t<HEAD>` for every repository the call may reach, under a name keyed on the call's own `tool_use_id` (the same id rides on the PostToolUse payload that consumes it; one name per session let two concurrent Bash calls of one chat overwrite each other's snapshot, and the report then measured this call's commit against the other call's tree — a payload carrying no id falls back to the session-only name, and both names are aged out of the cache on the same bound — swept under the per-call glob alone, the session-only file a refused call left behind was read by the next call carrying no id as its own evidence), behind a first line `KIND\t<kind>` naming what the call was armed for — every configured home AND every directory the command itself names (`git -C <dir>`, `cd <dir>` resolved against the `cd` before it, skipping tokens a shell would have expanded), through `rj_journal_homes`/`rj_command_dirs` so both hooks scope it identically — into `$HOME/.cache/claude/review-journal/<session>.heads`, and the report reads that file, REMOVES it, and takes `pre..HEAD` first-parent per repository independently. The one repository each hook then SPEAKS for — the block the report renders, the notice the gate prints — is derived by both through `rj_command_target`, never by an extractor either carries of its own; where the command NAMES a target neither can resolve (a token carrying an unexpanded `$` or a backtick, a path that is no directory, or MORE THAN ONE REPOSITORY, since `git -C a commit && git -C b status` landed in a and either one taken by position is the same confident misreport, while two directories inside ONE tree are the one target that tree is) the session cwd is not the answer to it, and both price every repository `rj_target_repos` enumerates — the journal homes, that cwd, and every directory the command itself named (`rj_command_dirs`) — instead: the report names the one whose HEAD actually moved against the snapshot and renders no block at all where none did, the gate arms one notice per repository. Nothing expands or executes any part of the command text to resolve it. The snapshot is armed for every kind that CREATES commits — `commit`, `merge`, `cherry-pick`, `revert` — and for NOTHING else, which is the whole of the stamping rule: `git pull`, `git pull --rebase` and `git fetch` move HEAD over commits other people wrote, and a range stamped for them puts an upstream author's paths into this chat's review scope. Only the commit kind reaches the gate's own notice; the others take their evidence and leave, since that notice is the commit's and armed for a merge it would block a call this flow has never gated. The report stamps only where the KIND line names a commit-creating call, and a snapshot naming no kind at all is read as a commit. A `--dry-run` arms none, whatever the kind it would otherwise be: it lands nothing, the report exits on that same flag before it consumes anything, and the file left standing under the session-only name is read by this chat's next call carrying no id as its own evidence. A `merge` call is narrowed once more, to the commits in its range carrying a SECOND PARENT: a merge that fast-forwards creates nothing at all, so its range is the same run of other people's commits a pull brings, and stamped whole it put an upstream author's paths into this chat's scope through the one kind that was allowed to stamp them. Nothing weaker answers it: git's `[branch sha]` summary lines are silent for a `-q` commit, for a redirect and for a repository the command never printed (a call mixing a printed commit with a quiet one lost the quiet one entirely), and a clock over a bare HEAD reads a co-tenant's commit, a pull or a merge as this call's. A repository absent from the snapshot takes NO debt row, and a repository whose HEAD resolved to nothing when the snapshot was taken is named by the `unborn` sentinel and read as "every first-parent commit this tip reaches" — dropped for failing `rev-parse` instead, the first commit of a fresh repository is the one commit no reader can ever measure. A pre-HEAD this tip does not descend from is still enumerated where the two share history — `--amend` and a rebase REPLACE the commit the snapshot named, and required to descend from it the hook answered for neither — while a pre-HEAD sharing no history at all (a checkout of an unrelated line) answers for nothing. What an earlier call landed is behind this call's pre-HEAD, so nothing is landed twice and no marker records what was reported. The sweep is gated on the SNAPSHOT and never on the block this hook renders: that block reads the one repository the command parsed to, so `cd a && git commit -q && cd b && npm test` left a's commit unstamped. A landing larger than `LANDED_CAP` first-parent commits is swept newest-first to that cap and the count it could not read is named in the report, since silence there reads as a scope that covered everything the call carried. A repository still admits a sha only where its own HEAD DESCENDS from it, never on `rev-parse` alone: a linked worktree shares the parent's object store, so a commit made in one resolves in the main checkout too and would take debt rows in a tree whose content never landed there). The paths a commit carried are read against its FIRST PARENT (`log -1 --name-only --first-parent`): a merge commit answers a plain `--name-only` with nothing at all, so everything a merge brought to the line it landed on went into no debt row — and a call whose git block this hook could not parse still prints those journal lines, unframed, since a frame is a report of a commit while the scope the sweep stamped is named nowhere else — an edit and the commit carrying it inside ONE Bash call are seen by neither of the others (the gate saw nothing pending, the edit hook sees a path already at HEAD), and that debt would be recorded under no chat at all. It passes over a path on a commit-journal entry, and never on a debt row alone: our own entry says the edit was recorded when it happened — and only where a debt row of OURS actually stands, since the append that should have followed that entry can have failed and passing over it then leaves the content this commit carried owed to nobody — a co-tenant's alone says the pending work `git commit -a` swept in beside ours is that chat's (where both stand, our name is already in the debt journal from the edit that wrote ours), while a debt row from an earlier episode is exactly what a review has since settled and may never stand in for the content this commit landed — and it passes over a path an unswept worker run of ANOTHER chat has listed — unswept meaning no `journaled` marker. Where that run's listing cannot answer for its files — never written, naming none, or saying `UNKNOWN:`/`PARTIAL:` — the scope on record is its whole WORKDIR, and what happens to it turns on `rj_run_final` (row `am`'s predicate, shared by both hooks): while the run can still change, the workdir is passed over, since its own sweep is still to come, and the report says on what liveness and age it was left; once the record is FINAL it claims nothing at all and emits no scope, because no record NAMES those paths and a chat answers for a path only through a record that names it. Never its dirt and never its WORKDIR, which is normally the whole repository: inherited by the launcher, one listless record of 2026-08-21 went on taking the paths of every commit anybody made in that checkout for its whole seven-day life, four legacy files nobody had touched among them (live 2026-08-24), and the same inference read three co-tenants' commits back as one chat's own debt (live 2026-08-25). What such a run wrote is nobody's until some record names it, which is what the run's terminal note (row `am`) tells its owner to do by hand. A path passed over on the LIVE reading is written to `<run-dir>/deferred-paths` as `<repo-top>\t<path>`, append-only and duplicate-tolerant, and row `am`'s sweep journals these rows under the run's owner where that run's own listing names the path and under NOBODY where it does not. The report says which run took the paths and prints `worker-run report <id>`. Every commit additionally passes over any path whose standing debt row of ANOTHER name does not PREDATE that commit's own `%ct`: a worker that committed in its own shell went through no hook of this flow, and its sweep's stamp is better evidence of authorship than the range this call happens to span — a stamp landing in the commit's own second is that same sweep, and read as older it handed the worker's file to whoever was committing beside it, while a row of OUR own says nothing of the kind and is answered by the entry rule above, and neither does a row naming NOBODY, which is a record of a path no record answers for rather than a co-tenant's sweep — the reader's epoch floor makes a fresh stamp per EDIT the writers' duty (a record at or below the epoch of the artifact that covered its path is invisible to the reader, so no spent record may stand in for a new write), and the compaction, not the writer, bounds the growth. Never a read-modify-write: two chats rewriting this file in the same second lose whichever ownership landed first. Appends take the SAME per-journal `<journal>.lock` the rewriters hold across their whole snapshot-to-`mv` transaction (`rj_append` waits briefly, then appends raw rather than drop the record), and every rewriter finishes through `rj_swap`, which abandons the swap when the journal grew since its snapshot — an append landing mid-rewrite survives as the compaction that yields to it, shrinking the lost-append window to the size-check-to-`mv` instant. Readers collapse duplicates, the newest write of a (session, path) winning; the entries of paths no longer in debt are dropped by a compaction that runs only while the gate actually holds `claude-review-debt.lock` and is skipped when it is busy, since a journal nothing prunes grows a chat's name onto files it settled months ago. `review-bench debt` reads it and the commit journal together and asks nothing else about authorship; a debt record standing AT OR BELOW the epoch of the artifact covering its path is a settled episode's leftover and never answers for the work done since (`debt_path_authors` epoch floor, absolute — a path whose every record is spent that way has no author and is nobody's rather than its oldest claimant's), so a compaction that has not run yet cannot hold a chat answerable for another chat's later work on the same file | writers `../claude-setup/hooks/commit-journal.sh` `stamp_debt_owner` and `../claude-setup/hooks/review-flow-gate.sh` `record_debt`/`prune_debt`, both over `../claude-setup/hooks/lib/review-journal.sh`; reader `share/rbench/{store,debt}.py` `journal_entries`/`debt_authors`/`debt_path_authors`; policy `docs/review-contract.md`; guards `../claude-setup/tests/test_review_flow_gate.sh` and `tests/test_review_bench.sh` |
| ap | Both round dials are one number across the two repositories | the numbers a round is priced on live in `share/rbench/round.py` (row `af`) and are spelled a second time in exactly one other place: `review-flow-gate.sh` `SECOND_REVIEW_P1S` = `HANDOFF_P1_STOP = 3`, `SECOND_REVIEW_FINDINGS` = `ROUND_FIX_MAX = 8` and `SECOND_REVIEW_HARD_FINDINGS` = `ROUND_HARD_MIN = 20`, which are the sentence the gate PRINTS when it hands a chat the decision menu and the word its `band <p1> <total>` hands every hook that BRANCHES on the band instead of printing it. All three held equal here, because the gate that words the ask and the tool that refuses the fixing pass would otherwise disagree about which round owes a decision — the count arm short by one prices a 25-finding round as the middle band and lets `fix` past unargued while the report beside it prints `round 2 required` — and the gate's own VERDICT is still never computed from them: it relays `review-bench fork <id> --check` and its exit code, so a drift costs a wrong sentence and never a wrong gate. A negative guard rejects literal round-threshold assignments under `hooks/stop.d/` and `hooks/lib/`, including the retired `HARD_CONFIRMED`, `HARD_P1S` and `FIX_ONLY_CONFIRMED` names, so the stop asks and journal cannot become a fourth pricing implementation and ask `band` instead. `share/rbench/debt.py` spells no dial at all: a waiver answers for work no review is owed over, never for a round that owes a decision, so no band is its business | `share/rbench/round.py` `HANDOFF_P1_STOP`/`ROUND_FIX_MAX`/`ROUND_HARD_MIN`; `../claude-setup/hooks/review-flow-gate.sh` `SECOND_REVIEW_P1S`/`SECOND_REVIEW_FINDINGS`/`SECOND_REVIEW_HARD_FINDINGS`/`band`; guard `tests/test_consistency.sh` |
| ar | Worker run liveness identity | a worker run record's `meta.json` stamps the supervisor pid beside `pid_started_at`, the launch instant of that pid, written ONCE and never restamped — unlike `started_at`, which `bin/worker-run` moves on every walled reroute to budget `run_overdue` per attempt. Liveness is the pid's own age (`ps -o etime=`, never `kill -0`, which reads a foreign-owned live process as dead via EPERM) against `pid_started_at` within `RJ_PID_SLACK=30` seconds; a record with no `pid_started_at` answers `unknown` in the hooks and keeps the legacy signal probe in `worker-run` (its runs predate the field and must not start reading dead), never a verdict computed from the restampable `started_at`. Pid `0` is refused as a pid by BOTH readers before any probe: it is the pre-launch placeholder `worker-run` stamps before the supervisor exists, `ps -p 0` answers nothing while pid 1 answers, and read as a pid it says a run that has not STARTED is gone — which retires its record and sends everything it later writes to no journal, no debt row and no notice. An empty `ps` answer is read as death only once a probe of a pid that must exist (`ps -p $$ -o etime=`) shows `ps` can answer at all — the same bytes otherwise mean a sandboxed or failing `ps`, and both readers act on death destructively. `worker-run`'s own readers — `wait`, `report`, `busy_accounts` — go through the same test, or a chat is told to wait forever for a run the hooks have already retired A bench run's `<run-dir>/delegated` stamp is read by the same pid rule (`share/rbench/round.py` `pid_still_running`, the one place the Python side spells it — asked by `triage_delegated` of a `delegated` stamp, by `live_worker_run` of a run record's own `pid`/`pid_started_at`, which is how the settle demand stays silent while a worker of the asking chat is still writing in the repository, and by `panel_still_running` of `<run-dir>/panel.pid`, which is how `review-bench wait` tells a panel that is working from one that died without handing anything over), with ONE earlier form: `claimed <session> <epoch>`, which `../claude-setup/hooks/worker-limit-gate.sh` writes at the Agent spawn for every `review-bench record <id>` the brief (or the brief file it names) carries — a worker takes about a minute to reach `worker-run`, whose pid stamp then overwrites the claim, and the Stop gate fired in that minute. A claim is live for `DELEGATED_CLAIM_SECONDS = 600` from its epoch and dead after, exactly as a gone pid; the hook never writes over a pid stamp | writer `bin/worker-run` (jq `.pid_started_at = $began` at supervisor launch); readers `bin/worker-run` `supervisor_running`/`PID_START_SLACK` and `../claude-setup/hooks/lib/review-journal.sh` `rj_run_liveness`/`RJ_PID_SLACK`; guards `tests/test_consistency.sh` and `tests/test_worker_run.sh` |
| as | Review report header words | the review frame of row `ab` opens on `review · round <N>` — the block's own identity, since a reader who cannot see which of a chain's two rounds he is holding has to reach the `before:` row for it — and the STATE hangs off that as a suffix, the ONLY place a block states one, with no row below it repeating it: the bare `review · round <N>`; `· NOT FINISHED` for a round whose fix status is `blocked`; `· NO PANEL` where no cell completed; `· STALE · <D Mon>` — the run's own finish date in the READER's zone — for a run that finished older than three hours ago (`REPORT_STALE_HOURS = 3`, in `share/rbench/round.py` alone) — a clock and nothing else: content and delivery criteria were tried and called a block stale the moment its own fixing pass moved the tree; and `bench` — carrying no round, since an untiered explicit-`--raters` panel is no review round at all. `report_frame_word` picks one for every block `record` and `report` print, in a precedence that runs the list backwards — `bench` first, because a panel no tier launched is no round to state a state of; then NOT FINISHED, NO PANEL, STALE, and the plain word last. A watchdog kill has NO word here: the cell it killed carries `killed · cap` / `killed · stalled` on its own `failed:` row, and how much of the diff the survivors covered is the triage receipt's answer, not the frame's — a run where the kill left nothing behind is already NO PANEL — off `round_state` — `fix_status`'s own three-way answer (`done`, `blocked`, `pending`) — with `pending` wearing the PLAIN word at any age and only the clock adding STALE: a round nobody has answered for yet is not a round that stopped, and reading the loud word off "no fixes recorded" is what put NOT FINISHED in front of Egor over a round whose fixes were landing at that moment (2026-08-20). The `fixes:` row is printed ONLY where something is owed, and says what is owed rather than that nothing happened: `stopped — <reason>` from a `--blocked` receipt; `<fixed> of <confirmed>` for a done receipt answering for fewer findings than the triage confirmed, or for one — `done` and `blocked` alike — recorded against a different triage than the run now carries; `decision first — fix / simplify / cut / redesign` while the band owes a decision nobody has recorded; and `fix — the commit closes`, or `fix — the commit closes both rounds` on a round 2, where the fixing pass is simply still to land. `done` and `nothing to fix` print no row, so an absent row is a round that owes nothing rather than one nobody answered. `debt:` stands directly under `confirmed:` on every round — `<N> lines · <M> files`, the size the target line announced at LAUNCH and the record kept (`scope_price`), never re-priced at render time, where the tree the fixing pass moved would give a different number every time the same report is drawn. A round 2 adds `before:` (round 1's own `confirmed:` value, rendered by the call that wrote it) and `decision:` (`<choice> (round 1)`) under it, and a round 1 prints neither; `next:` speaks ONLY where something follows (`round 2 by decision`, or the hard band's `round 2 required (P1 ≥ 3, confirmed ≥ 20)`) and prints no row at all otherwise — a `next: none` row spends a line repeating that the `fixes:` row above it was the whole answer; and `id:` is LAST and carries the CHAIN's id, so the two rounds of one piece of work hand the reader one id rather than two to pick between. The fork that frame owes — fix as it stands, rewrite the block they cluster in, cut the scope, Egor's decision reaching him with the P1 list before anyone acts on it — is NOT in the block: `review-bench fork <run-id>` prints it (`REPORT_BLOCKED_FORK`, and the round-budget refusal where the budget of two is spent) for the report hook to hand the model as context, because the model is who acts on it and the block is what Egor reads. BOTH nets ask it of every run they deliver and neither infers it from the block's rows: a budget-spent round prints no `next:` row at all, so a net reading rows escalates nothing on exactly the round the budget already refused. No prose analysis demand rides it: the decision is a RECORD, `review-bench fork <id> --choice --why` → `<run-dir>/fork.json`, which `fork --check` answers for (exit 3 while owed and missing — crossed either dial of row af, no record) and which the Bash gate (`review-flow-gate.sh`, a `fixes <id> --done|--blocked` command), the Agent gate (`worker-limit-gate.sh`, a brief or brief file carrying one) and the Stop gate (`stop.d/ask-review-report.sh`, through `pending-report` naming the `fork` command) all relay rather than re-price. Because ONE word is worn by two rounds, the consumers cannot decide deliverability from the header: their `HEADER_SPAN_RE` is the narrowed review header of row `ab` with every state suffix optional (a hook that keeps the bare-word regex silently stops delivering exactly the reports that still owe work), which is also the whole of what opens a block — a frame carrying any OTHER word, `bench` above all, is not a review header and opens nothing, so a panel sharing every row label with a review can never be delivered as a round on the labels alone — and BOTH then apply the same second rule, `deliverable`, over the block: a block whose `fixes:` row says the pass `stopped` (`UNANSWERED_ROW_RE`) is withheld unless it also carries the loud header (`STOPPED_HEADER_RE`). There is no `stopped:` row any more — the fork left the block — so a block whose header was cut carries no evidence of its own state; nothing rebuilds one, because nothing reads a report out of captured text at all. Both regexes are spelled identically in the two hooks — a rule one net applies and the other does not is a report withheld at one stop and delivered at the next. Delivering a round mid-pass is what put a report reading 0 P1 in front of Egor while its fixes were being applied (2026-08-20). THE SOURCE IS NEVER THE CAPTURE: a block reaches Egor only as `review-bench report --session <id>` renders it for a run id read out of the COMMAND, and the run-id regex below is the only thing either net recognises a report by. Every recovery that read a block out of a tool result — a row fallback rebuilding a head-cut copy, a foreign-header walk over blocks belonging to other hooks, a tail key deduping the copy, a `sed` range the model was told to re-read with — delivered blocks nobody could attribute, and their removal is what makes `blocks()` a closed frame and the `deliverable` rule and nothing else, spelled identically in both files. The same pair of hooks reads one more answer of the tool's: `review-bench pending-delivery --session <id>` prints `<run-id> <state>` per line, one of exactly `done`, `blocked`, `triaged` and `fork`, and prints nothing and exits `0` where there are none, so the Stop net can render a report a headless worker recorded and this chat never printed. The same ownership question is asked of ONE run by id, because a run reached by id says nothing about who launched it: `review-bench report <id> --session <id>` renders only a run that chat launched — its `meta.json` `session`, or the chat that launched the worker whose session that field names — and otherwise prints no block and exits 2 with `review-bench: run <id> belongs to chat <launcher>` on stderr, which both hooks read the launcher out of through an identically spelled `FOREIGN_RE`. A foreign run is then delivered by neither net — no frame, no ledger key, and not from the capture its own command left in the turn either — and the nudge says one line naming the chat instead. Passing no `--session`, which is what a payload naming no chat gets, filters nothing, as in `pending-report`. A framed block IS Egor's indicator that HIS review finished, so one chat framing another chat's review as its own is not a duplicate but a wrong answer (2026-08-22). `pending` is named by no line at ANY age; while its triage is younger than the triage-gate window the same round is named `triaged` instead — delivered ONCE as the WHOLE BLOCK, the moment the triage is on record and whoever ran `record`, because that block IS the report and the one-line herald only made Egor wait for it; `done` after that reaches nobody, the round having already been read. A recorded fork decision keeps the one-line form (`report <id> --line fork`: `review <id> · fork: <choice> — <why>`), inside the same window off the record's own `at`, and it is the whole of the net's `LINE_STATES` — `LINE_STATES = ("fork",)`, `triaged` having left it for the frame; a round that decision reopened is delivered again on its NOT FINISHED word — and past that window it reaches nobody: a window that promoted aged rounds handed the Stop gate 39 pre-receipt-mechanism rounds in one message (2026-08-20), so no `queued` mark ever exempts a `triaged` round and `settle-delivery` never queues one (`DELIVERY_STATES` stays the two FINAL states it may queue). The STATE is half of it and load-bearing on both sides: the ledger is keyed on `run:<id>:<state>`, so a round delivers one report per state it reaches, and `DELIVERY_LINE_RE` discards outright any line whose state is not one of those four. That ledger is ONE FILE both hooks write and read (`$XDG_CACHE_HOME/claude/review-delivery/<session>.emitted`), because both of them can print a frame to Egor and one report per run-state means once ACROSS the channels, not once per hook: they share `ledger_keys(run_id, state, block)` — the block's sha256 and `run:<id>:<state>` — written after emitting and consulted before, so whichever fires first retires that state for the other. Spelled identically in both files. The nudge derives the state from the block it is about to print (`delivery_state`, `blocked` where the loud header stands) since no delivery line names one there, and adds a state-blind `run:<id>` of its own that the Stop net must NOT consult — that net owes one report per state, and a state-blind key from the other channel would swallow the finished report of a round it saw blocked. Missing this, the PostToolUse path printed a finished report the moment the panel closed and the Stop net printed the same bytes again at the end of that turn (2026-08-21) — a third spelling added on the emitter's side reaches nobody. Those two key spellings are a COMPATIBILITY SURFACE, not an implementation detail: re-spelling a state re-keys the ledger and re-delivers rounds Egor has already read (2026-08-20), so a state may be REMOVED from the vocabulary but never renamed. A round whose fixing pass answered is named only while it is inside the `pending-report` freshness window; a `triaged` one only while its triage is inside the same window, read off its report receipt's own instant; a `fork` one only while its record's own `at` is inside that window, and a fork record over a round that crossed neither dial is refused by `fork --choice` itself, so no such line is ever queued. | emitter `share/rbench/{round,report}.py` `round_frame_word`/`report_frame_word`/`report_frame_header`/`report_block_lines`, `REPORT_BLOCKED_FORK` with `round_fork_text`/`cmd_fork`, `fix_status`/`round_state` with `FIX_RECEIPT` (`share/rbench/store.py`), plus `DELIVERY_STATES`, `delivery_state`, `cmd_pending_delivery` and `cmd_report`'s `--session` filter; consumers `../claude-setup/hooks/review-report-nudge.sh` and `../claude-setup/hooks/stop.d/notice-review-report-delivery.sh` (`HEADER_SPAN_RE`, `deliverable` with `STOPPED_HEADER_RE`/`UNANSWERED_ROW_RE`, and `recorded_runs` in the Stop net); guards `tests/test_consistency.sh`, `tests/test_review_bench.sh`, `../claude-setup/tests/test_review_report_delivery_gate.sh` and `../claude-setup/tests/test_review_report_nudge.sh` |
| at | The review that answers a debt | `REVIEW_ASKED=1 review-bench review --debt --tier <T0|T1|T2 — choose, see review-tiers.md>` — the one command a chat is handed when a review is owed, spelled once in each repository and equal between them. The gate's `escalation-verdict` words (row `af`) end on it, and `bin/review-bench` prints the same words itself when the fixing pass stops at the decision bands. The literal is load-bearing because the mode is: `--debt` takes NO path list, computing its own scope from the debt and widening it to every surviving path of the round a decision reopened (row `ap`), which is the only scope round 2 can answer over. A gate that reverts to naming paths hands over a hand-picked scope that answers no round — the failure is silent, because the narrow review passes and the debt it settles looks answered. The tier is a PLACEHOLDER (`DEBT_REVIEW_TIER`) and never a printed tier: a real one was read as the answer and pasted unread, so every debt was reviewed at T1 whatever it held. The chat about to run it chooses by `../claude-setup/global/docs/review-tiers.md`, and the `<`, `|` and `>` are there so a blind paste fails instead of choosing silently; only a caller already holding a tier — a refusal reprinting the command the user typed — substitutes it | `share/rbench/store.py` `DEBT_REVIEW_COMMAND` (built once in `debt.py`'s `debt_review_command`/`debt_chat_review_command`, quoted by `round.py`'s round-2 text); `../claude-setup/hooks/review-flow-gate.sh` `escalation-verdict` round-2 line; prose `docs/review-contract.md`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh` and `../claude-setup/tests/test_review_flow_gate.sh` |
| au | The delivery ledger is one file with one key shape | the record that a review round's report reached Egor lives at `$XDG_CACHE_HOME/claude/review-delivery/<session>.emitted` (`~/.cache` where that variable is unset), one key per line, and the key a run is looked up by is `run:<id>:<state>` with `state` one of the four of row `as` (`done`, `blocked`, `triaged`, `fork`). Three parties now spell it: the two claude-setup hooks that WRITE it — either of which can print a frame, so one report per run-state means once across both channels (row `as`) — and `share/rbench/debt.py`, which only READS it, for `review-bench doctor` (whether a deliverable round ever reached anybody) and for `pending-delivery`/the statusline review anchor (`round.py` `pending_delivery_rows` through `ledger_delivered` — a `(run, state)` the ledger names is pending nowhere, or a delivered round anchors the statusline forever). The reader's side is the fragile one: a diagnostic looking under a path or a key shape the hooks do not write would report every delivered round as undelivered, and one that wrote a key would retire a report nobody has seen — so the scan is read-only by construction, and the `run:<id>` key the nudge adds for itself is no answer here either, because a state-blind key cannot say which of a round's two reports was delivered. `settle-delivery`'s `<run-dir>/delivery.json` is NOT a second ledger and never a key here: it records that a round was put back in front of its launching chat (`queued`) or written off (`lapsed`), against the STATE it answers for, and only the two hooks may ever record an ARRIVAL. A round is written off for either of two reasons and never a third: the transcript that chat's stop reads is gone, or its own queue has stood longer than `share/rbench/debt.py` `DELIVERY_QUEUE_LAPSE_S` (7 days) without being delivered — a chat that closed but kept its transcript can never stop again either, and 24 rounds of a nine-day span sat `queued` and undelivered through the whole `DOCTOR_WINDOW_S` fortnight on the strength of that file still existing (live 2026-08-24). A lapse KEEPS the queued instant beside `lapsed`, because how long the round was owed is readable nowhere else, so every reader of the mark asks for `queued` AND not `lapsed`: read on `queued` alone, `pending_delivery_rows` went on exempting that round from its window for ever. That instant is the ROUND's and is written once: `settle-delivery` runs from every stop, so a pass that restamped the queue it found would hold every queue permanently seconds old and nothing would ever reach the lapse at all — it is rewritten only where the state or the launching session it answers for has changed | readers/writers `../claude-setup/hooks/review-report-nudge.sh` and `../claude-setup/hooks/stop.d/notice-review-report-delivery.sh` (`ledger_keys`, `ledger_path`/`ledger`); reader `share/rbench/debt.py` `DELIVERY_LEDGER_DIR`/`DELIVERY_LEDGER_SUFFIX`/`DELIVERY_LEDGER_KEY` and `doctor_ledger_keys`, with `share/rbench/{store,round,debt}.py` `DELIVERY_MARK`/`delivery_mark`/`cmd_settle_delivery` its own store; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh` |
| av | The doctor snapshot is the menubar's whole vocabulary | `<state_dir>/doctor-snapshot.json` holds exactly `{"as_of": <epoch int>, "anomalies": {<class>: <int>, ...}, "total": <int>}` over exactly the five classes `untriaged`, `undelivered`, `stuck_fixes`, `orphan_debt`, `kill_asymmetry`, and `<state_dir>` is resolved the same way on both sides (`WORKER_STATS_DIR`, else `CLAUDEB_DIR`, else `~/.claude-profiles/.claudeb`, plus `worker-stats`). The renderer has no other way to learn a class name: it scans no store of its own and computes no threshold, so a class added on the writing side and not in the Lua list is a count nobody ever sees, a renamed key is a row reading `OK` over a system that is not, and a menu pointed at another state dir reports on records nobody writes. The ages behind the counts are deliberately NOT here — they live in one Python dict and the snapshot carries only what it found. `rounds_past_two`, which `doctor` prints beside the five, is deliberately NOT a class and never reaches the snapshot: it is the launcher's refusal of a third round having failed, which no age and no count can qualify, and a menubar row counting it would read as a backlog somebody is late on | writer `share/rbench/debt.py` `DOCTOR_SNAPSHOT`/`DOCTOR_CLASSES`/`doctor_snapshot_document` over `state_dir()`; reader `hammerspoon/llm-limits.lua` `DOCTOR_CLASSES`/`doctorSnapshotPath`/`appendDoctor`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh`, `tests/llm_limits_renderer_harness.lua` |
| aw | One resolver names every chat | A chat surfaces under the name Claude Code gave it — the `ai-title` event its transcript records, or a `custom-title` Egor typed over that — and under nothing else. The `name` in `~/.claude-profiles/<profile>/sessions/<pid>.json` marked `nameSource: "derived"` is a placeholder the harness makes up from the working directory (`arbostar-frs-frontend-59`): he has never seen one, so it is refused and a chat holding nothing else surfaces as the first 8 characters of its session id. A record the harness did NOT derive carries a real name and is read. A worker run is not a chat — it surfaces as the chat that LAUNCHED it, off the run records of row `am`, and stays an id where two chats resumed one worker session or no record names a launcher at all, because a name of its own answers a question about a conversation with the errand it sent. Every surface that prints a session id prints that name beside it — a bare id is a chat Egor cannot find in his own window list — in one of two spellings, both `share/rbench/store.py`'s and both reading nothing of their own: `chat_display`, the name where the surface prints no id, and `chat_suffix`, ` (<name>)` after an id the reader needs machine-readable. This is DISPLAY only: session ids stay the on-disk grammar of every journal, artifact and receipt, and nothing here renames a stored key. The transcript ROOTS are this reading too — `~/.claude/projects` plus every `~/.claude-profiles/*/projects`, `CHAT_NAME_ROOTS` over both — because every chat on this machine runs under a profile, and a consumer globbing the default root for itself reads all of them as gone. A second reader of the title events, of the session records or of the launcher walk is how a name he has never seen reaches a surface that swears it is his | one implementation, `share/chat_names.py` (`chat_name`/`chat_label`, `NAME_PATTERNS`, `store_names`/`DERIVED_NAME_SOURCE`, `SHORT_ID`, `worker_run_launchers`, `transcript_roots`/`transcript_path`, env `CHAT_NAME_ROOTS`/`CHAT_NAMES_CACHE`); consumers `bin/chat-find` (`resolved_names`, `report_recent`) — and `bin/chats` through its JSON — `share/rbench/{store,debt,round,report}.py` (`chat_display`/`chat_suffix`, `cmd_waive`, `session_transcript_exists`), and `bin/chat-name`, the one-line entry point the shell surfaces that hold no Python go through (`../claude-setup/hooks/stop.d/notice-chat-names.sh`, which names every id this turn printed as `<original title> (<short id>)` and drops in silence the ones the resolver cannot name); none of them may read those stores itself; guards `tests/test_chat_names.sh`, `tests/test_chat_find.sh`, `tests/test_review_bench.sh`, `tests/test_consistency.sh` |
| ax | A diff too big for one cell is split, not the panel | chunking is OFF unless `--chunk` asks for it or the whole diff is past `DIFF_CHUNK_THRESHOLD_BYTES = 800000`, and then a run cuts the commit's diff at FILE boundaries into chunks packed to `DIFF_CHUNK_TARGET_LINES = 800` and gives every cell all of them, read one after another; the run's own line says which of the two reasons turned them on, and a run chunked by the flag spells it on the rerun line it prints. It never multiplies the panel: the cell count is the tier's own whatever the diff's size, one rater is one cell with one findings file and one verdict namespace, and the `#N` repeat suffix stays the tier's word for a rater it deliberately runs twice — a cell per (rater, chunk) made a 13-cell tier over a 25-chunk commit 325 concurrent cells and hundreds of processes (live, 2026-08-22). The gate counts the DIFF's own BYTES and the target its own lines — headers and context included, never numstat's changed-line tally, since what kills a cell is the text it is handed. A file is NEVER cut inside: one whose own diff is over the target is a chunk of its own read whole, a commit that IS one such file is handed out unsplit, and the target bounds every chunk holding more than one file. Sub-hunk slicing was tried and removed (2026-08-22): the halves of one rewrite went to cells that could not see each other's text and its deletion-only pieces were not valid patches. So a chunk's paths say the whole of what it holds, which is all the cells reading the repository are told. A rename reaches its chunk as a rename, its source in the pathspec beside the destination — asked about the destination alone git prints a brand-new file and the deletion lands in no chunk at all. It stays ONE run — one receipt, one handoff, one finding-index space — and the target line names the chunk count. A chunk NO cell's pass came back from is the one way a dead pass still costs coverage: its paths are dropped from the run's snapshot and stay in debt, since a receipt may only attest content somebody read, while every other chunk the cell read is kept. Only RECORDED cells attest: a cell the run discards as errored reached no adjudicator, and a pass counts as read on its ANSWER rather than its exit code, since prose or an empty reply joined into the cell's other chunks is invisible; unchunked, the run is byte for byte the one it always was. Both numbers are duplicated in prose (`docs/review-contract.md`), so a threshold moved in one place and not the other documents a panel nobody runs. The gate is measured rather than chosen (665 runs / 7197 cells, 2026-08-27): LINE count predicts no death at all — 19313 lines at 36 bytes/line were reviewed whole by every leg — while the one deterministic size death on record is a BYTE wall, one commit of 13263 lines and 2397508 bytes that killed every diff-fed leg in under two seconds (argv past ARG_MAX on Claude, the 1048576-character input ceiling on Codex, a context length on OpenCode), so the threshold sits under both ceilings with room for the environment and the review template the diff shares ARG_MAX with, stays above the 687 KB largest diff any unchunked cell ever completed, and everything below it is the caller's call, a chunked Claude cell costing ~4.7x the wall clock of an unchunked one | `share/rbench/{scope,launch}.py` `DIFF_CHUNK_THRESHOLD_BYTES`/`DIFF_CHUNK_TARGET_LINES`/`diff_chunks`/`diff_chunk_groups`/`diff_file_body`/`run_rater_chunks`/`chunk_pass_failure`/`unread_chunk_paths`; prose `docs/review-contract.md`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh` |
| ay | Sanctioned headless launchers | a vendor launched as a bare headless CLI call from a chat's Bash (`claude -p`, `claudeb … -p`, `codex exec`, `codexb … exec`, `gemini -p`, `geminib … --print`, `agy … --print`, `opencode run`) is a worker nobody can see: no `worker-run` record, no statusline tag, no journal ownership, no pool refusal, no limit signature, no stall watch. Every headless run reaches a launcher that owns it, and the list is one list — `worker-run`, `review-bench`, `llm-limits`, `claudeb revive`, `claudeb warm`, `claude-session-driver`, `codex-image`, `gemini-image`, `opencode-go`. The gate denies a command spelling a bare launch unless the same command names one of those launchers; it collapses quoted text into one word, then strips quotes and backslashes, then matches the vendor word only in command position — that order is what keeps a separator inside a quote (`echo "x; codex exec"`) from opening a command position the shell never had (string start or after `;`, `|`, `&`, `(`, a backtick or a newline, past any env assignments and the wrapper words `env`/`command`/`exec`/`nohup`/`nice`/`time`/`xargs`), so `'claude' -p`, `X="a b" claude -p` and `env claude -p` are denied while `mkdir claude -p` or a path operand is not; it fails open on its own errors. Interactive launches — no `-p`/`--print`/`--prompt`, no `exec`, no `run` — are the user and are never gated | policy `docs/routing-contract.md` § Sanctioned launchers; enforcement `bin/worker-launch-gate.sh` `LAUNCH_RES`/`SANCTIONED_RE`; registration in `~/.claude/settings.json`; guard `../claude-setup/tests/test_worker_launch_gate.sh` (the hook is registered in that repo, so its suite lives beside the registration) |
| az | A computed sweep is named before it is read | `--debt` is the one review target nobody spells, so its SIZE is the one thing a chat never sees until the panel has already read it. Every `--debt` round prices its computed scope with `debt_line_counts` — the differ row `ah` counts the same debt with — and prints `scope: <files> file(s) · <lines> line(s)` BEFORE anything is sealed or launched, since a snapshot commit is an object written into the caller's repository. Above `DEBT_SCOPE_LINES_MAX = 3000` lines it launches nothing: it prints the per-path lines largest first, capped at `DEBT_SCOPE_ROWS_SHOWN = 20` rows with the remainder counted rather than dropped silently, and the exact command to proceed — the same one plus `--scope-lines <lines>`, whose number must EQUAL what it printed. A flag that merely disabled the ceiling would be a bare override; a number that has to match is a size somebody read, which is the waive pattern: a decision on record. The ceiling is not a ceiling on what may be reviewed and never silently narrows a scope — the only two outcomes are the whole computed scope reviewed or nothing launched at all | `share/rbench/debt.py` `DEBT_SCOPE_LINES_MAX`/`DEBT_SCOPE_ROWS_SHOWN`/`debt_scope_gate`/`debt_scope_price`; caller `share/rbench/cli.py` `cmd_run`; flag help `DEBT_SCOPE_LINES_HELP`; prose `docs/review-contract.md`; guards `tests/test_review_bench.sh`, `tests/test_consistency.sh` |
| ba | One gateway-failure class, read only as a STATUS | a gateway failure is the whole 5xx family plus the curl-level `000` (no HTTP status reached at all), and both the client that retries it and the bench that classifies it read the same class. The bench reads it out of TEXT, so it is anchored to a status context (`HTTP 504`, `HTTP/1.1 503`, `status: 502`) and never to a bare three-digit number: a rater's prose cites line numbers, and `line 512` read as a dead gateway retried a cell that had already answered. Narrower on the other side and a 504 or a dropped connection fails a cell that used to cost one retry — the bench asks opencode-go for one attempt per gate hold, so the client's retries are the bench's to make | `bin/opencode-go` `transient_status`; `share/rbench/catalog.py` `HTTP_SERVER_STATUS`, read by `share/rbench/accounts.py` `OPENCODE_TRANSIENT_RE`/`OPENCODE_PROVIDER_OUTAGE_RE` and `share/rbench/panel.py` `FAILURE_REASONS`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh` |
| bb | Review cap rules | the numbers of the duration cap, the stall cap and the per-pass launch budget are spelled once in code and once in the contract prose the reader acts on: `CAP_WINDOW_DAYS = 21`, `DURATION_CAP_GRACE_S = 180`, `DURATION_CAP_THIN_SAMPLES = 5`, `DURATION_CAP_DEFAULT_S = 900`, `STALL_CAP_GRACE_S = 120`, `STALL_CAP_FLOOR_S = 240`, the agy ceiling of `AGY_DURATION_CEILING_S` (480s at T0/T1, 600s at T2/T3), and `CELL_ATTEMPTS_MAX = 2` | `share/rbench/catalog.py` (every constant but the last) and `share/rbench/launch.py` (`CELL_ATTEMPTS_MAX`, the last); `docs/review-contract.md` § Watchdog spells each as `` `NAME = value` `` and the ceiling as its four tier values |
| bc | Gemini main's removal marker | ONE file, `~/.llm-limits-gemini.json.removed` — main's legacy cache path with `.removed` appended (`LLM_LIMITS_GEMINI_CACHE` moves the cache and the marker with it; `LLM_LIMITS_GEMINI_REMOVED` overrides the marker alone). `geminib remove main` and the menubar's `--gemini-remove` are the same operation on the same file, and its presence means main is ABSENT from the roster — no row, not even a removed one — undone only by deleting the file, never self-cleared by a later refresh. The per-account directory `~/.llm-limits-gemini/<name>.json.removed` holds NAMED accounts' markers and never main's | resolved once in `share/gemini-accounts.sh` `gemini_removal_marker`, which `bin/geminib` (`remove`, `gemini_main_removed`), `llm-limits.sh` (`gemini_legacy_removed`, `gemini_account_marker`) and `bin/worker-run` all reach through the shared module — neither script may spell the path itself |
| bd | Journal ledger resolver | both review journals live in ONE directory per git FAMILY — the common git dir, resolved by the SAME command on both sides: `rev-parse --path-format=absolute --git-common-dir`, spelled literally in `share/rbench/store.py` `journal_dir`, in `../claude-setup/hooks/lib/review-journal.sh` `rj_journal_dir` and in `bin/statusline.sh`'s review-class cache key. Per-worktree git dirs gave one project two ledgers: a waiver from the main checkout cleared 33 paths while 12 stayed owed in a worktree of it, and the statusline and `review-bench debt` answered from different files (Egor, 2026-08-26). The two file names are canonical once per side — `DEBT_JOURNAL`/`COMMIT_JOURNAL` in `store.py`, `RJ_DEBT_JOURNAL`/`RJ_COMMIT_JOURNAL` in `review-journal.sh` — and no other file of either repo's `hooks/`, `bin/` or `share/` may spell `claude-review-debt` or `claude-commit-journal` at all, since a private path is exactly how a second ledger grows back. A worktree's own ledger is folded into the family's (records appended unless already held, then unlinked) inside the resolver itself, so readers and writers migrate it alike — EVERY worktree's on the Python side (`<common>/worktrees/*/`), since a ledger nobody reads from goes whole with `git worktree remove` — and each fold holds BOTH files under ONE lock primitive spelled the same on both sides: `mkdir "<journal>.lock"` beside the file it guards, destination taken before source, waited on for at most a second and never proceeded through — a busy lock skips the fold and the next reader retries. One primitive because the two languages fold and rewrite the same two files: a `flock` on one side and a lock directory on the other exclude nothing, and unlocked the fold deleted records appended between its read and its unlink | `share/rbench/store.py` `journal_dir`/`fold_journal`/`journal_lock_taken`; `../claude-setup/hooks/lib/review-journal.sh` `rj_journal_dir`/`rj_lock`/`rj_absorb_journal`; `bin/statusline.sh`; policy `docs/review-contract.md`; guards `tests/test_consistency.sh` and `tests/test_review_bench.sh` |
| be | Commit-free repository families | the whitelist is ONE file, `~/.claude/commit-free` — one repository root per line, `~` expanded, `#` comments and blank lines ignored, missing file = nobody is free — and `COMMIT_FREE_FILE` is the only override, so a test fixture and the live list are the same reader. Membership is by git FAMILY (row `bd`), not by path, so every worktree of a listed repository is listed too. Read only through `rj_commit_free`: the review door in `review-flow-gate.sh` and the commit door beside it (`price_repo`, which REFUSES a commit whose target the command NAMED here while it carries the committing chat's own unreviewed paths — the one refusal this gate has, since a family Egor asks no word for is a family where nothing else would trigger the review; it fires only where that chat has no round of its own standing READY TO CLOSE in the repository, which is `share/rbench/round.py` `round_next_step` asked through `review-bench round-open` and never a predicate of the hook's own — ready|decide|round2, the same three words the launcher's `round_open_guard` refuses a new panel on, so the door that lets a commit through and the door that lets a review start agree on when a round is finished; a target nothing resolves is priced over every repository this flow knows and is never refused, or calls that commit nothing would be walled), which also answers the question on argv as `commit-free [dir]`, the Stop ask `stop.d/ask-round-uncommitted.sh`, and the SessionStart line `hooks/commit-policy.sh` — a second reader of this file is a second definition of who may commit without asking | `../claude-setup/hooks/lib/review-journal.sh` `rj_commit_free` (the only site spelling the path and the env name); `../claude-setup/hooks/review-flow-gate.sh` review door, commit door and `commit-free` subcommand; `../claude-setup/hooks/stop.d/ask-round-uncommitted.sh` reason wording; `../claude-setup/hooks/commit-policy.sh` SessionStart line; `docs/DIAGNOSTICS.md` and `docs/review-contract.md` prose about the `REVIEW_ASKED` trigger |
| bf | Chat repository index | the file is `$HOME/.cache/claude/review-journal/<session>.repos` — one absolute repository top per line, the same spelling a `.heads` row carries, no duplicates, written only for a session id that is a plain name (`''|.|..|*[!A-Za-z0-9._-]*` is refused) — and it is swept by NOTHING: the 120-minute `find` in `rj_snapshot_heads` matches `<session>.heads` and `<session>.*.heads` only, and outliving the calls that wrote it is the whole of what this file adds over them. It is what the one-panel rule enumerates candidates from beside the snapshots, so a suffix that drifts on either side leaves a repository invisible to `debt_owed_repos` and the chat launches a panel per repository | writer `hooks/lib/review-journal.sh` `rj_session_repos`/`rj_register_repo`, called at every journal append under an owner (`hooks/commit-journal.sh` `stamp_debt_owner`, the commit-journal append and `stamp_deferred`; `hooks/review-flow-gate.sh` `record_debt`; `hooks/commit-report.sh` landed-debt stamp); reader `share/rbench/store.py` `CHAT_REPOS_SUFFIX` in `chat_call_repos`; prose `docs/review-contract.md` §Debt one-panel paragraph |
