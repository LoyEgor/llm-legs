# Shared invariants

Some values are duplicated across scripts written in different languages (bash,
jq, Lua, prose) because each implementation is independent — no shared library
spans `bin/claudeb`, `bin/statusline.sh`, `bin/worker-pick`, `llm-limits.sh`, the
Hammerspoon renderer, and the docs. When such a value drifts in one place, the
system misbehaves silently. `tests/test_consistency.sh` re-extracts each value
from the live files and fails if any site disagrees with the canonical value
below. This table is the canonical source; the test names it in every failure. Review-owned rows live in
`../review-bench/docs/shared-invariants.md`, cited here by repo name.

| # | Invariant | Canonical value | Implementation sites |
|---|---|---|---|
| a | Staleness / dim thresholds | five_hour `1800`s, weekly `21600`s, fable `21600`s, routing `7200`s | the numbers live once in `share/limits-view.sh` (`LIMITS_STALE_FIVE_HOUR`/`_WEEKLY`/`_FABLE`/`_ROUTING`); `bin/claudeb` and `llm-limits.sh` pass them into jq as `$thr5`/`$thrw`/`$thrf` and must carry no local literal; `bin/statusline.sh` sources the same module and passes `$thr5`/`$thrw` into its bucket jq and `$LIMITS_STALE_FABLE` into the limits-file age backstop, with no local literal; `docs/statusline-contract.md` usage-table prose (`as_of` >1800s / >21600s, mtime >21600s); `hammerspoon/llm-limits.lua` consumes the collector's `.stale` flag rather than a local threshold (`bucket.stale == true`) — it must stay flag-driven, never hardcode a number; `LIMITS_STALE_ROUTING` is the routing half of the same rule and coarser on purpose, since a router reads whatever the last collector pass merged rather than one live bucket: `bin/worker-pick` passes it into jq as `$stale_thr` and carries no literal, and it is what decides both the `DATA:` verdict and WHICH vendor/account rows that line names |
| b | Keychain service formula | `Claude Code-credentials-` + first 8 hex of `shasum -a 256` of the profile path | `bin/claudeb` `keychain_service`; `llm-limits.sh` `claude_subscription_type`; `bin/claude-session-driver` `keychain_service` and `share/claude_resets.py` `keychain_service` (Python `hashlib.sha256(...).hexdigest()[:8]`, same prefix) |
| d | Weather HTTP classes | `000`, `429`, `5xx` (`5[0-9][0-9]`) are weather — they never write an auth verdict (`expired`/`revoked`/`auth-failed`) | `bin/claudeb` `probe_weather_failed` case pattern; `bin/claudeb` `oauth_refresh` outcome classification |
| e | OAuth token-endpoint 429 cooldown | `900 * 2^(max(1, strikes)-1)`, capped at `14400`s (4h); a longer vendor `Retry-After` wins | `bin/claudeb` `oauth_backoff_until`; `llm-limits.sh` `claude_stale_cause` |
| f | Robot curl refresh is off, permanently and unconditionally | the direct curl POST to the OAuth token endpoint runs only inside an invocation the user asked for: `CLAUDEB_WARM_USER_EXPLICIT=true` (menu `Refresh`, `Refresh + Start Windows`, per-account `Hard refresh`, `r` in the status TUI) exempts that whole invocation — warm, heal and the POST itself — and marks its journal entries with `"user":true`. Every other path short-circuits before the endpoint (`oauth_refresh` rc `76`, journal outcome `robot-skip`) and rotates through `claudeb revive` instead. No file, env var or config may re-enable the robot POST: the predicate is `user_refresh_explicit` alone, so no site may reintroduce a marker file, a second predicate, or an `\|\|` that widens it | `bin/claudeb` `user_refresh_explicit` plus the guards in `oauth_refresh`/`heal_refresh_verdict`/`heal_one`/`warm_accounts` and `token_journal`; `llm-limits.sh` `claude_stale_cause`'s user-explicit guard over the `robot curl refresh off (manual refresh only) — revive path active` banner |
| g | Worker rank contract | one vector ranks every vendor's accounts, written once as `worker-pick`'s `rank_keys`: `[five-hour deferral, fresh claim, late auth, −budget, name]` (`docs/routing-contract.md` rule 2). The NEXT table applies that same vector across vendors — the pin tier first — and caps the result at five rows, so one vendor may hold several. A five-hour reading at or above `FIVE_HOUR_DEFER_PCT` (`80`) ranks an account behind every candidate below 80% whatever the budgets say; a claim fresher than the TTL (row `bm`) defers it one step further; `auth_late` ranks a grok account whose access token has `expired` behind every signed-in one without walling it — its own CLI refreshes it silently — and is set on grok rows alone, null elsewhere, which jq reads as false, so the key is a no-op for the other three rather than a ranking function of its own. Then the largest daily budget (row `bl`), then the name. Literal profile `main` is NOT a key on any vendor: the budget decides and the main-account shield (row `bn`) is what keeps a base account from being spent by workers, so no site may reintroduce a `main_last`. `codexb` ranks its own profiles by the same budget, largest first, name breaking the tie. Display-only account lines sort the selected account first, then the remaining candidates, then pool members that cannot serve, then everything unreachable, preserving selection order within each band | `bin/worker-pick` shared `rank_keys`/`rank`/`display_band`/`display_sort`; `bin/codexb` `pick_account` ranking; `docs/routing-contract.md` rule 2; `share/worker-policy.md` vendor-fit prose |
| h | Antigravity review cell invocation mapping | Every cell omits `--effort`. `agy-pro-low` → `--model gemini-3.1-pro-low`; `agy-pro-high` → `--model "Gemini 3.1 Pro (High)"` because v1.1.9 still serves the high slug as Flash; `agy-flash38-<effort>` → `--model gemini-3.8-flash-<effort>`; `agy-flash37-<effort>` → `--model gemini-3.7-flash-<effort>`; `agy-flash36-<effort>` → `--model gemini-3.6-flash-<effort>`; A family `agy models` no longer lists is retired from the roster whole rather than repointed, cells and tiers with it — an id the server has dropped is answered `invalid model selection <id>` instead of being substituted, so every cell of that family dies; `agy-flash35`/`gemini-3.5-flash` went that way (absent from `agy models` on CLI 1.1.24, 2026-09-02) and no cell, tier, short name or rater spelling may name it again, while its historical rows are keyed `flash35-<effort>` so Gemini aggregates retain them without restoring a launch spelling. A resolved label that disagrees with the requested cell is recorded as `model_mismatch` and warned about; an Antigravity cell runs the `/code-review` extension or a lens's text, decided by the lens's `skills:` and never by the cell name; a text-fed agy cell with no lens and no pool task is refused in `refuse_retired_cells` | `../review-bench/share/rbench/{catalog,launch}.py` `AGY_EFFORTS`, `AGY_MODEL_IDS`, and `agy_model_id`; `../review-bench/share/rbench/raters.py` `normalize_legacy_rater`/`set_cell_skill`, `../review-bench/share/rbench/accounts.py` `refuse_retired_cells`; `bin/worker-run` knob-to-agy model case for worker-supported models; `../review-bench/docs/DIAGNOSTICS.md` system-map entry for `bin/review-bench` |
| i | Gemini worker knobs | Table defaults are the newest Flash family `geminib families` prints and `gemini_effort=high`; the model names are the slugs `geminib families` prints (row `cr`), and `high` is the ONLY effort any of them runs — any other tier word from a brief or a knob (`low`, `medium`, `xhigh`, `max`) is replaced by `high` with one stderr note `gemini effort forced to high`, never refused; only a word no vendor table knows (`ultra`, a typo) still yields `OUTCOME: EFFORT_REFUSED`. Invocation follows invariant h: the effort is encoded in the versioned slug (`<agy_prefix>-high`) and no pair passes `--effort`; `pro` alone launches under its `<label> (High)` label (row h), because the `-high` Pro slug is still served as Flash. Capacity fallback is not a knob and not per-consumer: `bin/geminib` owns it for every Gemini launch (row `cn`) | accepted knobs/default-writing semantics in `~/.claude/commands/worker.md`; routing defaults in `bin/worker-pick`; statusline reads and renders both knobs independently; policy pointer in `share/worker-policy.md` |
| j | Worker spawn pressure gate | `worker-pick --account <vendor>` owns selection: its selected account is allowed below the hard `100`% wall and warned from `85`%; exit `3` and an effective `100`% account are blocked. `95`% is the protective block only when worker-pick is unavailable or fails, and that fallback is always reported | shared `~/.claude/hooks/worker-limit-gate.sh` for claudeb, Codex, Gemini, and Grok worker spawns (Grok is judged on its weekly bucket alone, the only window it measures); `bin/worker-pick` account query; `~/.claude/settings.json` has one Agent registration |
| k | Worker account resolution | resolution lives only in `worker-run`: an explicit `--account` (relays forward the brief's `ACCOUNT: <name>` line) wins at launch; a vendor pin is tried first even when limits say 100% (only a met run-observed wall record skips it, and that wall removes that one name from the pin list); otherwise `worker-pick --account claudeb\|codex\|gemini\|grok`; exit `3` aborts the launch with `OUTCOME: <VENDOR>_USAGE_LIMIT`, or with `OUTCOME: <VENDOR>_UNAVAILABLE` when the picker reports an empty worker pool, a role switched off, or no usage data for that vendor at all — three decisions and one absence, none of them a wall to hunt; an account outside the pool is refused outright unless it is the vendor pin (row q); only an unavailable/failed query falls back to `worker_model_pin_first` of that vendor, then `main` for Codex/Gemini — never for claudeb or grok, whose `main` carries no login at all, so both refuse instead of launching a run that can only fail at authentication — and the resolved account is visible in the printed `TAG:` line. On a mid-run usage wall every run except a resumed session (`--resume`) re-resolves with the burnt accounts excluded and relaunches, reporting each hop as a `REROUTE:` line and rewriting the tag — `--account` and the pin fallback spend that account first, then the rest of the ranking; a resumed session stays (`WALL: resumed session stays on <account>`). `OUTCOME: <VENDOR>_USAGE_LIMIT` means the whole pool walled. Claude refuses when its fallback pin is empty because `claudeb` requires an explicit profile, and grok refuses for the same reason. A failure outcome carries a `REASON:` line where the vendor's own text says which kind it was — grok's `auth` (a human login is owed, no reroute helps), `transient` (capacity weather, relaunch) and `max-turns` (a turn cap ended a run the vendor was serving) — and no `REASON:` at all where the text names neither. A hit turn cap is its own outcome, `OUTCOME: GROK_MAX_TURNS`, never `_UNAVAILABLE` and never `_USAGE_LIMIT`: the vendor served every turn asked of it, so nothing is walled and no reroute helps — relaunching the brief elsewhere outruns the same cap again, and only resuming the session (`-r`) does not. A session the vendor cancels BEFORE its first turn is its own outcome too, `OUTCOME: GROK_CANCELLED <run-id>` with `STATUS: failed`: the stream ends with `stopReason: cancelled` behind nothing but the handshake, and exit `0` alone reported that empty run as done. It is weather like `transient` — no wall is recorded, no pin lapses, and the relay pauses and relaunches once. Grok is capped at all only when `WORKER_RUN_GROK_MAX_TURNS` sets one; unset, no `--max-turns` reaches the CLI and row `bu`'s idle watchdog is the runaway guard, as it is for every other vendor | `~/.claude/commands/worker.md` pin toggles; `bin/worker-pick` pin routing; `bin/worker-spawn-hook.sh` pending tag; `bin/worker-run` account resolution; `~/.claude/agents/claudeb-worker.md`, `codex-worker.md`, `gemini-worker.md`, and `grok-worker.md` `--account` forwarding |
| l | Gemini quota group matching | case-insensitive group label contains `gemini`; exact display-name equality is forbidden | `llm-limits.sh` raw group collection/validation; `bin/worker-pick` normalized account eligibility |
| m | Gemini profile discovery and HOME mapping | account `main` maps to the process base HOME; named accounts are sorted profile directories and map to `<profiles-dir>/<name>` | `share/gemini-accounts.sh`, sourced by `bin/geminib`, `llm-limits.sh` and `bin/worker-run` (which resolves a run's agy transcript under that HOME) |
| n | Weekly bucket provenance | The `headers` weekly strip covers Claude only; Codex rate-limit headers are measured and Codex writers stamp `origin: "usage"`. Claude rate-limit response headers carry NO weekly percentage, so no writer may stamp `origin: "headers"` on `seven_day`; every reader treats such a bucket as unknown (rendered `?`/`-`, never a number) | writers: `bin/claudeb` `merge_headers` (5h only), `bin/statusline.sh` `rl_merge` stamps `origin: "session"` and drops a cached header-origin week; readers: `bin/claudeb` `account_data`, `llm-limits.sh` both claudeb-store and single-account snapshot reads, `bin/statusline.sh` render gate, `bin/worker-pick` cache normalization |
| o | Claude rotation usability presence | `llm-limits.sh` always emits boolean `rotation.usable.general`, `rotation.usable.fable`, and `blocked == ((enabled and rotation.usable.general) \| not)` for every emitted Claude store account, including incomplete, disabled, and unusable snapshots; consumers may rely on those keys being present. The two meanings stay separable and neither field may swallow the other: `rotation.usable.*` is CAPABILITY (auth alive, plus a fable bucket on a non-Pro plan for `fable`) and says nothing about consent, `enabled` is the worker-pool toggle, and `blocked` is the combination for a reader that wants one field. Folding the toggle into `usable.general` is what left `bin/worker-pick` unable to honour the pin over an out-of-pool account, the override `docs/routing-contract.md` rules 2 and 4 give it. `bin/worker-pick` defaults a missing rotation key to `true` only for compatibility with pre-change caches. Two documented weak spots, both fail-open on `fable` and both bounded by the worker's own `CLAUDEB_USAGE_LIMIT` reroute: the Pro-plan gate needs `claude_subscription_type`, which returns an empty string when the keychain is locked or absent (unknown plan is then treated as non-Pro). | producer: `llm-limits.sh` Claude account projection; permissive fallback: `bin/worker-pick` |
| p | Reserved profile names | the same set in both guards, so a name that cannot be a profile cannot be offered as a switch target either: `help add enable disable remove use accounts status st profile p run warm export-token token-upkeep`, plus `main` and any name starting with `.` or containing `/` | `bin/claudeb` `reserved_name` (with `main` checked separately by `add_account`); `bin/claude-chat-switch` `is_reserved` |
| q | Worker-pool membership | one mechanism for all four vendors, so a toggle means the same thing everywhere: the excluded names live one per line in `<profiles-dir>/.<tool>/disabled` (`.claudeb`, `.codexb`, `.geminib`, `.grokb`), an absent file means everything is in, and the dot-directory keeps the file outside the profile enumerators' `*` glob. Read and written only through `share/worker-pool.sh`, which also owns the per-vendor directory formula (`worker_pool_dir`, used by the four CLIs, `worker-run`, `codex-image` and `llm-limits.sh`; Claude reaches it as its store root `CLAUDEB_DIR`) and the wall itself (`worker_pool_refuse_headless`). Exclusion IS unreachability for every headless run — `claudeb … -p`, `codexb <name> exec`, `geminib profile <name> --print`, `grokb profile <name> --prompt-file`, and any `worker-run start` naming the account — however the account is named; the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`, `grok_profile`) is the one override. What it overrides is CONSENT, never capability: `worker-pick` accepts a pinned account only while it is not `unavailable` — a state that is dead auth OR a usage wall, kept as two separate words on the rows themselves, since a wall is a window to wait out and a dead login is a thing for Egor to fix, and `WALLED` over an account whose quota nobody spent sends him hunting a limit that does not exist. Spend consent is ALL it is: nothing that only reads an account may consult it — `bin/llm-refresh` refreshes an excluded account like any other and `claudeb revive` takes one. An interactive launch is the user, never a worker, and is never gated, which is why every member including the last one may go out: an empty pool means no worker may run, and `worker-pick` says so in its own words (`every <vendor> account is out of the worker pool`) so `worker-run` reports `OUTCOME: <VENDOR>_UNAVAILABLE` instead of a usage limit. One rule the helper itself enforces: a file that exists but cannot be read means "everything is out" for readers and is never rewritten by writers | `share/worker-pool.sh`; `bin/claudeb`, `bin/codexb`, `bin/geminib`, `bin/grokb` (`is_disabled`/`set_disabled`, `enable`/`disable`); `llm-limits.sh` (`enabled` per account, `ROT off`); `bin/worker-run` (the wall on `--account` and the empty-pool outcome); `bin/codex-image` (the same wall, since image runs launch `codex` directly); `bin/worker-pick` (excluded from every vendor's eligible set but not its pin resolution; per-vendor empty-pool reason); `hammerspoon/llm-limits.lua` ("In pool" checkbox on every account row, sole rows included) |
| r | Claude leg selection announcement | the stderr literal `claudeb: worker-pick selected <name>` is a parsed contract, not free prose: claudeb prints it on every routed headless run, `ask_claude.sh` seds it into the audit row's `account` field, and the legs test stub reproduces it byte-identically | `bin/claudeb` headless routing announcement; `ask_claude.sh` audit account extraction; `tests/test_legs_routing.sh` claudeb stub |
| s | User-entry refresh classification | `needs_user_entry:true` is the structured marker for a refresh outcome that requires human account entry; a vendor error carries it only when every `refresh_errors[]` entry has that class, and the affected account carries it independently. The derived `refresh_error.cause` (one release) joins those causes with `"; "` | producer and classifier `llm-limits.sh`; consumer `hammerspoon/llm-limits.lua`; guards `tests/test_llm_limits.sh`, `tests/e2e_surfaces.sh`, and `tests/test_consistency.sh` |
| u | Late review threshold | `3` × the base cell's recorded median duration, with a `120`s (`120000`ms) floor. Elapsed runs from the run's `started_epoch`, except for a CHUNKED cell (`chunks[cell] == [read, total]`, `total > 1`) carrying a positive integer `chunk_started[cell]`, which is timed from that stamp — the pass now running — since the median prices one pass; a document without the key keeps the run clock. The bench reports no late review of its own: the statusline and the task-row renderer are the only sites that judge one, against the `expected` map the bench's store writes into the progress file | expected map `../review-bench/share/rbench/store.py`; reader/coloring `bin/statusline.sh`; per-group red of a review task row `bin/subagent-statusline.sh`; live-progress and task-row prose `docs/statusline-contract.md` |
| v | Account data age | `as_of` is the oldest valid timestamp among `five_hour`, `weekly`, and `fable` windows whose `used_pct` is numeric; `stale_seconds` is its non-negative age. Absent and null-valued windows do not participate, and an account with no data-carrying window has neither field. Vendor aggregate age applies the same rule to its hoisted windows. Metadata with separate freshness retains its own timestamp: Codex reset credits use their capture time. | primary producer `llm-limits.sh` `data_as_of`/`set_data_age`; independent routing freshness `bin/worker-pick` `account_as_of`; consumers `llm-limits.sh` table/plain renderers and `hammerspoon/llm-limits.lua` |
| x | Claude account existence | an account exists when ANY claudeb store knows it: `tokens/<name>`, `limits/<name>.json`, or the `~/.claude-profiles/<name>` profile directory; `main` and `-` are reserved non-accounts on every surface. `bin/claudeb` `account_names` is the single enumerator (status, enable/disable, warm, probes, and pin validation all consult it); the limits menu lists strictly from the vendor cache, which those probes populate for the same set; account birth (add, first-launch profile create) and death (remove) fire the announce hooks so the cache follows without a manual refresh | `bin/claudeb` `account_names`/`account_exists` and the `main`/`-` filter; `llm-limits.sh` claude-section `main`/`-` skip; `share/account-announce.sh` `announce_account_added`/`announce_account_removed` wired into `bin/claudeb`, `bin/codexb`, `bin/geminib` |
| y | One limits view for every surface | bucket display semantics are defined once: a bucket is `expired` when its reset epoch is real (≥ `31536000`) and ≤ now, and a reset over `86400`s past is dropped outright — the collector nulls the field and `limits_reset_text` prints `-` — because the window it named has rolled over unseen and every surface reads the field as a schedule; the drop is read AFTER expiry, so a dead reading never loses its `expired` verdict by losing its date, `stale` per row a's thresholds (or auth expired / cached origin), and its `effective` value is `0` when expired, else the raw pct; a text cell shows the EFFECTIVE value with `~` (stale) and `!` (expired) markers, a bare `-` when unmeasured; reset cells and age text share one formatter; per-account age is the oldest measured bucket (row v). Every surface renders these semantics from the same defs or from fields the collector computed with them — no surface re-derives its own. Any probe/warm that rewrites snapshots announces a passive collect (`announce_limits_probed`) so the merged cache and menu move with the snapshots; the collector suppresses announces in its own children (`LLM_LIMITS_ANNOUNCE_SUPPRESS`) to avoid recursion and double writes; a pressed `r` in the status TUI carries `CLAUDEB_WARM_USER_EXPLICIT=true` so the robot-refresh guard (row `f`) cannot silently eat a user-explicit refresh | definitions `share/limits-view.sh` (`LIMITS_VIEW_JQ`: `limits_bucket_expired`/`limits_reset_ancient`/`limits_bucket_stale`/`limits_effective_pct`/`limits_pct_text`/`limits_reset_text`/`limits_age_text`); consumers `bin/claudeb` `account_data`+renderers and `llm-limits.sh` `meta`/`mark`/table/plain; `hammerspoon/llm-limits.lua` renders the collector's `effective_pct`/`stale`/`expired` fields only; `bin/statusline.sh` judges its raw account snapshot (`5h`/`wk`) with `limits_bucket_expired`/`limits_bucket_stale`/`limits_effective_pct`/`limits_reset_ancient` and renders the `fb` row off the collector's fields exactly as the menu does (dim = stale or expired, value = effective); live parity guard `tests/e2e_surfaces.sh` store-consistency check; announce plumbing `share/account-announce.sh` + `bin/claudeb` `probe_accounts`/warm dispatch + `llm-limits.sh` child env |
| aa | Hammerspoon launchd agent identity | label `com.egor.hammerspoon`, wrapper `/Users/egorloy/.local/libexec/hammerspoon`, log `/Users/egorloy/Library/Logs/com.egor.hammerspoon.log`; renaming any of these must move every site together or the env guard's ownership probe and log cap silently miss | hammerspoon repo `launchd/com.egor.hammerspoon.plist` (read at `${HS_ROOT:-$HOME/.hammerspoon}`) `Label`/`ProgramArguments`/`StandardOutPath`/`StandardErrorPath`; `hammerspoon/config/env_guard.lua` `launchctl print` probe and log-cap path; prose `docs/DIAGNOSTICS.md` |
| ae | Account pin ownership | the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`, `grok_profile`) is Egor's own override above the pool, and a session moves it only after he names it: naming an account to work on is not naming the pin. The permission is a pin grant of scope `account` in claude-setup's words store (`${WORDS_DIR:-$HOME/.cache/claude/words}/<session_id>/grant.pin`, `ttl:30m`), which its `hooks/word-intake.sh` writes when his words name the pin, touching with it the empty marker `<state_dir>/pin-grants/pin` the command door reads — younger than `1800`s unblocks, a stale or absent one blocks; the file doors read that marker only when the words library is missing, and a grant for this chat's own pin (row `cm`) never opens them — and it opens BOTH directions, since a pin he set is not a session's to clear either. Three doors refuse independently and fail open: the command door inside `worker_model_pin_account`, which every spelling of `claudeb\|codexb\|geminib\|grokb use` reaches, and — because a door on one way to the file is a door around the other — an `Edit\|Write` door and a `Bash` door for a session writing `~/.claude/worker-model` by hand. What the file doors deny is the PIN, not the file: the same file carries `worker=`, `*_model=` and `*_effort=`, which `/worker` rewrites directly, so Edit/Write leaving every `*_profile=` line as it found it passes ungated. Bash `pin_untouched_write` allows only whole-command `sed -i '' 's/^<key>=.*/<key>=<value>/' <path>` or `<var>=<path>; sed 's/^<key>=.*/<key>=<value>/' "$<var>" > "$<var>.tmp.$$" && mv -f "$<var>.tmp.$$" "$<var>"`. Keys are `worker`, `codex_effort`, `claudeb_effort`, `gemini_effort`, `grok_effort`; values match `[a-z0-9]+`, variables are plain shell identifiers, and path is `~/.claude/worker-model`, `$HOME/.claude/worker-model`, or the expanded absolute path. No additional statements, flags, expressions or append redirections qualify; model substitutions require a pin grant through Bash. Every door compares the resolved file, never the path text — `//`, a `..` hop and `~` all name the one pin. The command door applies only to a session (`CLAUDECODE` in the environment) writing the REAL file — the menubar shells out from Hammerspoon carrying neither, and a fixture named through `WORKER_PICK_CONFIG_FILE` is a test's own. Reading the pin is never gated. A wall never deletes the pin: a run-observed wall record skips it until the reset, then it leads again; dead auth lapses the query and leaves the line standing. Limits at 100% do not skip it. | `share/worker-model.sh` `WORKER_MODEL_PIN_TTL_MIN`/`worker_model_pin_grant`/`worker_model_canonical_path`/`worker_model_pin_allowed` and its refusal in `worker_model_pin_account`; the four vendor CLIs' `pin_account` wrappers; claude-setup `hooks/word-intake.sh` (writer); `bin/worker-pin-gate.sh` `write` and `bash` (readers, through `word_gate_allow`); registrations in `~/.claude/settings.json`; guards `tests/test_worker_pin_gate.sh`, `tests/test_worker_pick.sh`, `tests/test_claudeb.sh` |
| ah | The statusline speaks the gate's verdict | Debt is this chat's own unread lines, and the unit is the session, never «chat + folder». `review-debt <session>` prints ONE line, `STATUS=closed|open|unknown LINES=n FILES=n FIX=n WHY=ledger|gap|run|nobase|err|none [BOUND=n]`, and the gate relays it unchanged or answers `WHY=err` itself. The renderer turns that line into one form and never a second opinion: closed → nothing, open with lines → `N`, open with fixes only → a dim `fix N`, an unknown → the known number followed by a dim `?<why>` (`WHY=ledger` → `~BOUND ?ledger`), a line it cannot parse → `?err`; the autonomy mark rides in front of any of them. No session total, no other chat's count, and no number invented over an answer nobody could read. | `../review-bench/share/rbench/debt.py`; `../claude-setup/hooks/review-flow-gate.sh`; `bin/statusline.sh`; both consistency suites |
| ai | Usage wall record | `${WORKER_STATS_DIR:-${CLAUDEB_DIR:-$HOME/.claude-profiles/.claudeb}/worker-stats}/walls.jsonl` plus, for OpenCode, the served-call stamp `<same dir>/opencode-seen/<account>`. JSONL records share fields `side`, `account`, `bucket`, `detected_at`, `reset_at`, and `window`. `reset_at` and `window` are optional. OpenCode keeps `bucket: "general"` for every quota window, so any active wall blocks that key for every request; `window` is one of `5-hour`, `weekly`, or `monthly` when known. The file holds RAW observations and nothing else: a row says one request was refused, never that the refusal still stands, which is row `al`'s question. It has two writers in two languages — `bin/opencode-go` at the 429 it sees (a `jq` append, so a probe or a worker walls the plan as visibly as a bench cell) and `../review-bench/bin/review-bench` for the `opencode` and `agy` sides — and TWO rules they must spell alike. The first is the ceiling: a `reset_at` the provider stated is capped once, by the writer, to the horizon its own window can reach (`5-hour` 21600s, `weekly` 691200s, `monthly` 2764800s, unnamed 604800s), so a garbled date cannot retire an account until someone notices by hand, and every reader takes the recorded value as final rather than re-clamping it. The second is the grammar that reads the stated horizon out of the refusal body, because the gateway states it in wordings both writers must accept whole: abbreviated units (`hr`/`hrs`, `min`/`mins`, `sec`/`secs`) alongside the spelled-out `second|minute|hour|day|week`, an optional space between number and unit, and COMPOUND values (`Resets in 9hr 30min`, `Resets in 2 hours 30 minutes`) whose components are SUMMED rather than read down to the first one. The unit alternation is longest-first and ends on a word boundary, so both `3 mins` and `3 minutes` are consumed whole rather than `min` matching inside the longer spelling and the remainder of the word cutting the run of components short. A body stating no reset still yields no `reset_at` — none is invented — and the two grammars are pinned against one shared corpus of real 429 bodies, so a wording taught to one side alone fails the guard. NO writer ever retires an OpenCode row by the clock — `../review-bench/bin/review-bench`'s compaction may collapse superseded duplicates into one row per account and window (newest detection, longest horizon) but never drops the account — and no reader may treat a row as spent because its `reset_at` passed: the gateway dates a reset to the day, so a window is routinely shut hours past the horizon it named. The one counter-evidence is a completion the plan SERVED — a 2xx from `/models` or any other path is not one, since the plan answers those while completions stay walled — and `bin/opencode-go` records it by writing that account's `opencode-seen/<account>` stamp, which row `al` reads against the rows. Both writes are single-file and independent, so no reader-then-writer window exists in which a 429 landing beside them could be discarded. The account a row is filed under is that plan's keychain service verbatim: `opencode-go` for the default profile and `opencode-go-<profile>` otherwise. `../review-bench/bin/review-bench` reads the same rows for its own launch question through its own clock horizon (`WALL_WINDOW_MAX_TTL_S`) and is not bound by this row's retirement rule — it asks whether a cell may be launched now, not whether the plan is spent — but the served stamp binds BOTH readers, and row `al` spells the one comparison they share | writers `bin/opencode-go` (`wall_row`/`record_call`/`record_wall`/`mark_served`) and `../review-bench/share/rbench/accounts.py` (`mark_walled`, `WALL_WINDOW_MAX_TTL_S`); pool reader `../review-bench/share/rbench/accounts.py`; display reader `llm-limits.sh` (row `al`); guard `tests/test_consistency.sh` |
| aj | Per-vendor role switches | a role is a per-vendor wall over the pool, spelled `<vendor>_<role>=off` in `~/.claude/worker-model` for `claudeb`/`codex`/`gemini`/`grok` × `workers`/`reviewers`. Every one of the four is a key the writer accepts and all three routers read; review-bench staffs all four, so `grok_reviewers` walls its `gr` cells the way `gemini_reviewers` walls its `agy` ones. The literal `off` is the only veto and an ABSENT key is the open state, so opening a role deletes the line rather than inventing an `=on` spelling; a hand-edited duplicate resolves FIRST-wins on every reader, or the menu shows one value while the routers follow another. `share/worker-model.sh` `worker_model_set_role <vendor> <role> on\|off` is the one writer — under the pin's own lock (row `ae`), and refusing a session (`CLAUDECODE`) outright, since closing a role redirects every worker and rater after it. The ladder is **pin > roles > pool** (`docs/routing-contract.md` Roles): a usable vendor pin answers over `<vendor>_workers=off` the way it answers over pool exclusion, while a reviewers query never sees a pin, so `<vendor>_reviewers=off` is final. A pause (row `bp`) stands above that whole ladder and is the one switch the pin does not answer over: under it the role lines stay in the file exactly as written and say nothing at all, since a parked vendor renders no segment for them to word. The refusals are parsed contracts: `worker-pick: <vendor> is switched off for <role>` at exit `3`, which `worker-run` and `review-bench` both match to word their own, and `worker-pick: --role only means something with --account` at exit `2`. A workers wall reports `OUTCOME: <VENDOR>_UNAVAILABLE` and never `_USAGE_LIMIT` — a switch Egor flipped is not quota to hunt — and it holds over an explicit `--account`, a resumed session and the pin fallback alike, the vendor pin being the one override. | writer `share/worker-model.sh` `worker_model_set_role`; readers `bin/worker-pick` `role_off`/`--role` flag/`query_role_wall` and the per-vendor `*_role_off` flags, `bin/worker-run` `PICKED_ROLE_OFF` plus its own `${vendor}_workers` wall, `../review-bench/share/rbench/accounts.py` `reviewers_role_off`/`role_closed_note` and its `--role reviewers` query, `hammerspoon/llm-limits.lua` `WORKER_MODEL_PREFIX`/`WORKER_ROLES`/`readWorkerModel` with the For workers / For reviewers items writing through `M.setWorkerRole`/`WORKER_ROLE_SCRIPT`; prose `docs/routing-contract.md` Roles, `~/.claude/commands/worker.md` (keys `/worker` preserves and never writes) |
| ak | Auto-refresh vendor roster | the daemon refreshes exactly `claude`, `codex`, `gemini`, `grok`, `opencode`, spelled once in `live_vendors`, which the seed, the tick loop and the state validator all read — so a state file written before a vendor existed migrates into the default rung rather than failing validation, and a paused vendor (row `bp`) leaves all three at once. A vendor whose store row lists no accounts names none to revive where its base profile is not a fixed account — Gemini's is deletable and Grok's `main` is the real `~/.grok`, which carries no login — so `main` is never invented for those two. An account whose stored auth is `expired` is refreshable work on Grok alone, and a blocked account everywhere else: the Grok CLI rotates its own access token as a side effect of any authenticated subcommand, and that touch lives in the collector's deliberate refresh (`llm-limits.sh --refresh-account grok/<account>`, and the vendor-wide `grok`) so the heartbeat tick and the menu's Hard refresh share one path — the daemon itself never runs the vendor CLI for grok and stamps nothing for it. There the account gets exactly one `grokb <account> exec models`, bounded by `LLM_LIMITS_GROK_TOUCH_TIMEOUT` (`30`s), when its `auth.json` expiry has passed or its last poll answered 401, and only the poll that follows writes an auth verdict, since the CLI prints `You are not authenticated.` while rotating and its exit status therefore states nothing; the passive all-vendor collection stays a plain read. The confined-run export is the one other caller of that same rotation: `grokb export-token` touches the CLI once when the saved session has less than `--min-seconds` left, with `GROK_AUTH_EARLY_INVALIDATION_SECS` set to those seconds — left at its default 300 the CLI keeps a token with 17 minutes left, and the last half hour of every 6-hour token refused every grok cell — then re-reads `auth.json`, so a read-only export refuses only where the rotation bought too little time — never over a token the account's own refresh material could have renewed. `grokb` finds the `grok` binary through PATH, then the newest `~/.nvm/versions/node/*/bin/grok`, then `~/.local/bin` and `/opt/homebrew/bin`: the heartbeat runs under launchd, whose PATH carries no nvm, and a bare name there is a touch that silently never happens. Claude's expired token is the opposite and stays unrefreshable here, because only an interactive session rotates it (`docs/EXIT-PLAN.md`). The poller says `expired` only where it saw a `refresh_token` beside the rejected key, and `needs_login` otherwise; the daemon reads that verdict and never `auth.json`. Four of them refresh by reading a free usage endpoint; `opencode` has none, so its cadence is INVERTED and no other vendor may be given that shape: with no walled account in `~/.llm-limits.json` it is dormant — it sends nothing, and that state stays true for free because every real call records its own outcome (row `ai`) — and only a standing wall is probed, one `opencode-go wall-check` completion per walled account, which the gateway answers with a free 429 while the window is shut. The daemon never passes `--probe-clear`: that flag belongs to the explicit `Hard refresh` of row `al`, and on a heartbeat it would spend a completion per clear account per tick. It asks the collector, never the wall record: a second implementation of whether a wall stands (row `al`) would spend the plan on its own say-so. The probe's answer is read as one of three states and never two: `walled` (a refusal this probe itself recorded), `served` (a completion the plan answered) and `dormant` (nothing was sent at all, the wall having gone between the read and the request) — counting the third as a reopening would report a leg recovered on a request nobody made. The tick lock is handed back for the duration of the probe, because a wedged gateway must not hold the other four vendors' ticks behind it. A probe that answered re-collects, or the row it just retired keeps standing on every surface until the next tick. The stated reset is day-granular and therefore a hint: inside `OPENCODE_TIGHTEN_WITHIN_S` (`3600`s) of it, or past it, the existing ladder tightens and keeps probing, because only a served request retires a wall. No surface may invent a percentage for this vendor | `bin/llm-refresh` `normalized_state` seed, the `for vendor in` tick loop, the state validator, `opencode_tick`/`opencode_walls_json`/`opencode_probe`/`opencode_next_interval`; probe `bin/opencode-go` `wall-check` and `wall_check_all`; grok touch `llm-limits.sh` `grok_token_expired`/`grok_touch_expired` and `bin/grokb` `refresh_access_token` (`tests/test_grokb_export.sh`), binary `bin/grokb` `resolve_grok_bin`; guard `tests/test_consistency.sh` |
| al | OpenCode rows in the limits store | the Go plan publishes no usage endpoint — `usage`, `limits`, `me` and `account` all 404 — so `.vendors.opencode` carries no percentage and never will, and whether a recorded wall (row `ai`) still stands is computed HERE, once, by `llm-limits.sh`. One row per profile listed in `~/.config/opencode-go/profiles` (absent file = the single default profile `-`), in file order: `{account, walled, windows: [{window, resets_at}], as_of, stale_seconds}`. `account` is the PROFILE, mapped to the wall record's account name by row `ai`'s formula; `window` is `5h`/`wk`/`mo`, or `?` for a pre-window record, and a named window suppresses `?` beside it; `resets_at` is the recorded horizon as an ISO-8601 instant, `null` when the gateway never stated one, and it is DISPLAY only — a horizon already past retires nothing. The rule is one comparison and holds no clock: a window stands while the account carries a wall row `detected_at` no earlier than (`>=`) its served stamp, so a refusal outlives its own stated reset and only a completion the plan served ever ends it — and a tie goes to the wall, because only a standing wall is ever probed by the scheduled refresh, so a same-second pair read as served would freeze a walled account clean forever. That comparison has TWO implementations and they must stay spelled alike: `llm-limits.sh`'s `.detected_at >= $served` for every surface, and `../review-bench/share/rbench/accounts.py`'s `read_wall_rows`, which drops an account's rows below its stamp BEFORE aggregating a standing wall — the same order, or a stale long-horizon row shadows the 429 recorded after the completion and answers for the account anyway. A missing or unparsable stamp is no evidence and keeps every row, so an account nobody ever served stays walled on both. The bench answering this question its own way is what refused a whole OpenCode leg for a day (2026-08-15) on an account whose plan was serving other models fine. `as_of` is that served stamp alone — a 429 is not evidence the account is alive, and dating the row by one would show a walled account getting fresher the more often it refuses — so an account never served carries no age at all. The vendor is assigned LAST in the collector pipeline, past every pass that reads a percentage — staleness, expiry, `usable_now`, and the empty-vendor verdict that decides the exit code — because each of those would be inventing a reading; `--plain` and `--table` skip it for the same reason. Every other surface RENDERS these rows and re-derives nothing: `hammerspoon/llm-limits.lua` reads them like any vendor's, `bin/llm-refresh` picks its probe targets from them, and `opencode-go wall-check` asks them before spending a completion — unless `--probe-clear` makes the asking itself the point: the menubar's `Hard refresh` runs `wall-check --all --probe-clear`, which walks the roster file instead of the walled rows and spends one small completion on every clear account, since `as_of` is the served stamp and nothing but a served completion moves it. Anything scheduled (row `ak`) stays walled-only | producer `llm-limits.sh` `opencode_profiles`/`opencode_seen_tsv`/the `opencode=` jq; consumers `hammerspoon/llm-limits.lua` `appendOpenCode`, `bin/llm-refresh` `opencode_walls_json`, `bin/opencode-go` `wall_check`/`wall_check_all`, `../review-bench/share/rbench/accounts.py` `opencode_served_at`/`read_wall_rows`; guards `tests/test_consistency.sh` and `../review-bench/tests/test_review_bench.sh` |
| am | Worker files reach the launching chat | a worker a chat spawned IS that chat, so the files it authored are that session's own work for review coverage — the alternative is a run of hundreds of worker-authored files priced as the one edit the chat made by hand (Egor, 2026-08-17). The carrier is the RUN RECORD, not a report anyone read: at launch `worker-run` writes `<run-dir>/launcher` holding the `CLAUDE_CODE_SESSION_ID` that spawned it (nothing when there is none, or when the id is not a plain name), and after EVERY attempt — a walled one, a failed one, and one that never reached its workdir included — `<run-dir>/files`, whose first line is `WORKDIR: <dir>` and whose remaining lines are that run's paths, workdir-relative or absolute when they fall outside it. File discovery has ONE source of truth for every vendor: the repository content difference between the launch snapshot (`dirty-before-shas`, `head-before`) and the end snapshot (`dirty-after-shas`, `head-after`). Both use `git status --porcelain --no-renames -uall -z` and `git hash-object -w` for dirty paths; clean paths resolve against that side's HEAD, with changes between the two HEAD trees included in the candidate set. A path is a CANDIDATE exactly when its before/after blobs differ: shell-only writes, additions, modifications and deletions all count, while a touch or a rewrite to identical content does not. A snapshot detects change and never its author, so the run is LISTED for the candidates its own transcript also names; the rest stand in `files-note`, as a co-tenant's paths where the transcript answered for the whole run and as unattributed ones where it could not, because "the snapshot stands" is the claim that handed a run eight files for the two it wrote (live 2026-09-11). Reroutes retain the original launch snapshot and refresh the end snapshot, so the final list describes the net change over the whole run. A co-tenant edit during overlapping runs is attributed to every run whose transcript names it. `UNKNOWN: <reason>` is written when the repository snapshots cannot be established, and `PARTIAL: <reason>` where they stand but the transcript listing does not, so the narrowing could not run and the changed paths are named nowhere; a transcript naming writes OUTSIDE the snapshotted repository adds its own `PARTIAL:` row pointing at `files-note`, which holds one `UNKNOWN:` line per such path, since no content baseline was ever recorded for them. Paths containing tabs or newlines cannot fit the existing row contract and make the snapshot unknown. Non-git workdirs cannot supply a snapshot. Vendor transcript parsers answer for AUTHORSHIP and nothing else: they narrow the snapshot's candidates to this run's own, feed `files-note`, `WORKDIR-ESCAPE` diagnostics and the live watchdog, and can never add a path the snapshot does not hold. Reports expose the cross-check as `RUN-FILES-NOTE:`. A record is always written, including when the snapshot is unknown. Beside that list stands `<run-dir>/worker-session`, the run's OWN session ids one per line — EVERY vendor and not claudeb alone, since grok loads this machine's hooks out of `~/.claude/settings.json` for Claude compatibility and journals under its own session id (live: grok sessions against chat 35c155ac…, 2026-09-03), a codex or agy id reaching a journal is the same case one relay deeper, and an id that reaches no journal costs a reader nothing because this pairing is only ever consulted about an id some row already carries — written only where the vendor's output names one and never guessed at, and unioned across attempts for the reason the paths are. Beside them stands `<run-dir>/workdir-escape`, written only where the run's own calls named NO path inside its workdir — the silent agy failure of working in the first `--add-dir` instead (live 2026-08-24), which every report says out loud and which is not the ordinary case of a run that also wrote outside its workdir. Beside both stands `<run-dir>/dirty`, the same snapshot-diff paths spelled against the repository top under a `WORKDIR:` line, written only when no launcher owns the run; an owned run attributes every discovered path through `files` and `produced`. `<run-dir>/dirty-before` keeps its newline-separated, repository-relative path format for legacy consumers and live activity. Both `dirty-before-shas` and `dirty-after-shas` contain `<blob-sha>\t<path>` rows, repository-relative, with `-` for an absent or unhashable path. Beside that floor stands `<run-dir>/head-before`, the COMMIT the workdir's repository stood on when the run was launched — absent in an unborn repository, and never restamped by a reroute, since an attempt's floor is the run's floor — and at the run's end `<run-dir>/produced`, the CONTENT this run answers for, spelled as the links a debt reader walks (`../review-bench/docs/review-contract.md` §Debt): newline-separated rows `<prev>\t<cur>\t<path>`, the path spelled against the WORKDIR the way the listing's rows are and absolute where it falls outside it, `prev`/`cur` a blob sha or `-` for a side holding no content. Both sides are hashed with `git hash-object -w` — the floor's shas as well as the record's — so the blobs stand in the workdir repository's object store the reader diffs them out of (row `ao`). Only paths in the snapshot diff may have rows: one edit row for each uncommitted transition or a committed rewrite of preexisting dirt — `prev` its `dirty-before-shas` sha where it was already dirty, HEAD's blob at `head-before` where it was not, `-` where neither answers, and no row at all where the two sides are equal — and the commit transitions for those same paths (`git log --raw -m --no-renames --no-abbrev --reverse` over `head-before..head-after`, or a tree diff for an initial commit or a non-descendant HEAD), those carrying a fourth field `commit`, which is the one distinction the reader needs: a merge or a cherry-pick lands blobs their author already produced, and only the FIRST row naming a `cur` owns it (row `ao`). Written for a run whose `launcher` names a chat and whose workdir is a git working tree, and written even when it holds no row at all — its PRESENCE is the run answering for its own content, its absence is what sends the sweep back to `files` plus `dirty-before-shas` — and never rehashed afterwards: for snapshot records `claim` accepts only paths in the recorded diff and reads the frozen endpoint blobs; legacy records retain their append behavior, since what stands there was measured when the run ended and a fresh pass would date a co-tenant's later edit to this run. `prev` is measured from the run's START throughout, so a path the run committed and then edited again leaves an uncommitted row the chain cannot join: one ORPHAN link, which is this model's signal that a record is missing rather than a wrong owner. Snapshot attribution closes the historical shell-only blind spot even when the worker's hooks never journaled an edit. Anonymous `dirty` rows remain evidence about content owned by nobody: `../review-bench/share/rbench/store.py` `run_dirty_paths` adds them to the debt universe, never to `run_record_claims`. Old incomplete records retain their legacy dirt and claim semantics. Finished snapshot records are read only after `exit_code`, and the `journaled` marker retires the listing alone. The run's own listing is part of that universe too, for the same reason: a path a worker named that no journal has swept and no artifact ever held was in no count, no `debt --list` and no `--debt` scope until some later commit swept the run. A record whose `WORKDIR:` sits in a CHECKOUT OF THE FAMILY — the in-repo worktree convention `<repo>/.claude/worktrees/<name>` first — is spelled against that checkout's own top and so names the same repository-relative files the asking checkout holds, which is the family rule `review-bench debt` reads every artifact under (`../review-bench/docs/review-contract.md` §Debt): a worker a chat launched in its worktree is that chat's own work in the main checkout too. Neither reading takes a record whose workdir is a working tree of its own that is NOT of this family, since those paths read as this repository's by spelling alone while its git does not track one of them. The worker-session mapping also preserves ownership of historical shell edits journaled under the worker's id. `review-flow-gate.sh` folds the commit-journal entries owned by those ids into the launcher's pending set, priced whether or not the listing was ever swept — the `journaled` marker retires the LISTING, and nothing ever renames what the worker journaled — and keyed only where no run of this chat is still writing under that session, `--resume` handing one id to several runs; never for a run another chat launched, whose launcher answers for it. `share/chat_names.py` `worker_session_launchers` reads the same file — one walk, since `../review-bench/bin/review-bench` and the chat-name resolver of row `aw` fold a worker to its launcher for two different questions and must not answer them differently, so `debt --session <launcher>` counts those paths as the launcher's: the launcher JOINS the record's authors and the worker id stays beside it, since the gate inside the live worker asks this same question under that id and answered `other` it could not settle the work it had just done. `commit-journal.sh` sweeps the store on EVERY tool call — every `*/launcher`, the launching chat read off the file itself — judges every record through ONE predicate, `rj_run_final` in `hooks/lib/review-journal.sh`, which `commit-report.sh` reads too so the two hooks cannot disagree about whether a run is over: an `exit_code` on record (the run's own statement that it is over, whoever launched it — held for a liveness probe instead, a co-tenant's finished record with no launch stamp reads `unknown` and parks for the stale release while nothing else names its files and the edit notice skips it for having an exit code), a supervisor `rj_run_liveness` reads DEAD, or the age release below. A run that is not final is left alone entirely — not swept, and NOT retired: a record retired for an unusable launcher name while its run may still be writing takes the edit notice's warning about those files with it. It skips a run with no `exit_code` whose supervisor pid is still alive (its list is not final) while sweeping one whose supervisor is GONE, since nothing will ever write that exit code and the list is as final as it will ever be, journals what it read under the LAUNCHER's session id — stamping the debt of a listed path even where it already matches HEAD, since the listing is itself the record of who wrote the file and a worker that committed in its own shell lists nothing else — and marks `<run-dir>/journaled` only over a record it actually read, a path the record format can never hold (a control character in its name) counting as read since no later call can change it. Three more answers are TERMINAL for the same reason — no later call can make them come out differently, and counted as unrecorded each re-swept its run on every tool call of every chat for as long as the record sat on disk: a launcher naming no usable session id, a listing line that no `WORKDIR:` and no `meta.json` workdir can anchor, and a record whose listing never arrives. Each is said once on stderr AND once to the model as `additionalContext` — a PostToolUse stderr on exit 0 is read by nobody, so the one reader who could review these files or hand them to whoever owns them never learned they exist — behind a per-key marker in `$HOME/.cache/claude/review-journal/`, the same scheme the library notes use, and then retired. A run whose launcher names nobody, one that ended without naming a file at all and one whose workdir sits in no repository are each retired unswept with a note instead: nothing is stamped under an empty session any more, because a row without an owner is a row `journal_row` refuses. The listing sweep declines to retire a run whose rows it could not record, since a record retired over a failed write leaves those paths owed by nobody with nothing ever looking at them again. The key and the delivery are the run's OWNER's, never the sweeping chat's: where the two differ the sentence waits in `<owner>.commit-journal.notes` for that chat's own next call to collect, since printed here it reaches a reader who can do nothing about it while the chat whose files are named nowhere never hears they exist. A record that will never gain a listing at all — an exit code on record, the supervisor gone, or a liveness nothing can ever establish released by age, and `files` never written — is retired unread, since there is nothing left to import and every reader of it holds that record's whole WORKDIR as pending work on the strength of the missing file. Retired is not resolved, and nothing inherits what the record never named: a run whose files were never named leaves no claim for a later commit to be read against, and the paths it wrote are nobody's until some record names them (row `ao`). A run of ANOTHER chat is swept on that same evidence and nothing weaker — dead, or an `unknown` record past `RUN_RECORD_STALE_AFTER` — whether or not it carries a launch stamp, since a `ps` that answers about no process at all keeps a record unknown on every call there will ever be — because the launching chat may never take another tool call and its dead worker's files would then belong to nobody for good; live and unknown foreign runs are left alone, and the `journaled` marker settles two sweepers racing over one record. `review-flow-gate.sh` reads the same records for its pending set alone: the paths of a run still going, since nothing else prices them while it has no `exit_code`, and those of a finished run the journal has not swept yet (a worker that finished between the chat's last tool call and its commit reaches the commit through no journal entry at all), plus the journal entries its `worker-session` ids own — folded ahead of the `journaled` marker, which retires the LISTING alone. It names no run to anybody and never refuses a commit (`../review-bench/docs/review-contract.md`). Legacy incomplete listings can still be completed with `worker-run claim <run-id> --paths <p>… [--complete]`: `wait` prints their `UNNAMED:` paths, with a count and record path beyond the display cap. Only the launching chat may claim a finished run, and every path must resolve inside its workdir repository; validation is all-or-nothing. Claims remove only named paths from legacy `dirty`, and `--complete` retires the legacy `PARTIAL:`/`UNKNOWN:` caveat. For snapshot records the recorded diff is authoritative and claims cannot add other paths. `../claude-setup/hooks/worker-edit-guard.sh` remains unchanged; shell writes no longer require transcript discovery for attribution. The three readers spell the store one way, `WORKER_RUN_DIR` over `$HOME/.cache/claude-worker-runs`, or one of them answers about a directory nobody writes. A change to this machinery is reviewed with the `parallel-sessions` lens (`review-bench lens list`): its defects appear only beside another working chat, which a default review never imagines | producer `bin/worker-run` `persist_run_files`/`snapshot_workdir`/`snapshot_changed_paths`/`compute_snapshot_files`/`compute_run_files`/`transcript_crosscheck`/`record_worker_session`/`record_workdir_dirt`/`record_produced`/`produced_edit_rows`/`produced_commit_rows`/`produced_spelling`/`record_workdir_escape`/`workdir_escape_line`/`unnamed_line`/`claim_run` and the `launcher` and `dirty-before`/`dirty-before-shas`/`head-before` writes in `start_run`; consumers `../claude-setup/hooks/commit-journal.sh` `journal_worker_runs`, `../claude-setup/hooks/review-flow-gate.sh` `scan_run_records`, `../review-bench/share/rbench/{store,round,debt}.py` `run_record_claims`/`run_record_paths`/`run_dirty_paths`/`blind_fix_runs`/`tree_universe` and `share/chat_names.py` `worker_run_root`/`worker_run_launchers`/`worker_session_launchers`; `store.launch_chain` is the one launch-chain walk — `owners_of` and `caller_chat` both read it, so a nested worker's own review-bench calls answer under the launching chat. The worker-to-launcher FOLD happens at WRITE time (row `ao`, `rj_append_owned`), so no debt number depends on a run record surviving and `worker-run`'s 7-day prune of them is a retention setting and nothing else; policy `../review-bench/docs/review-contract.md`; guards `tests/test_worker_run.sh`, `../claude-setup/tests/test_worker_edit_guard.sh`, `../claude-setup/tests/test_commit_journal.sh`, `../claude-setup/tests/test_review_flow_gate.sh`, `../review-bench/tests/test_review_bench.sh` and `tests/test_consistency.sh` |
| an | Launching-chat pid walk | the harness registers a live chat as `<registry>/<pid>.json` with a string `.sessionId`, registry `~/.claude/sessions` (`../review-bench/share/rbench/store.py` takes `REVIEW_BENCH_SESSION_DIR` in its place for tests). A run is that chat's grandchild several execs down, so the launcher is found by walking `ps -o ppid=` up from the process's own pid, at most `15` hops and stopping at pid 1, taking the first entry whose `.sessionId` is a string and naming nobody where none is. TWO implementations, in two languages and at two moments: `../review-bench/bin/review-bench` walks ONCE at run start and records the answer as the progress document's `session`, because the chain is alive only then — a backgrounded run outlives its launcher and reparents to pid 1 — and `bin/statusline.sh` walks at render time ONLY for a document written before the writer recorded one. Which is also the precedence every reader owes: the recorded `session` first, the walk as the fallback, or a run the render attributes to nobody is dropped from the statusline of the very chat that started it | writer `../review-bench/share/rbench/store.py` `walk_launching_session`/`SESSION_WALK_HOPS`/`SESSION_REGISTRY_DIR_ENV`; reader `bin/statusline.sh` `review_run_session`/`review_run_owner`; segment row `docs/statusline-contract.md`; guards `tests/test_consistency.sh`, `../review-bench/tests/test_review_bench.sh`, `tests/test_statusline_hooks.sh` |
| ar | Worker run liveness identity | a worker run record's `meta.json` stamps the supervisor pid beside `pid_started_at`, the launch instant of that pid, written ONCE and never restamped — unlike `started_at`, which `bin/worker-run` moves on every walled reroute to budget `run_overdue` per attempt. Liveness is the pid's own age (`ps -o etime=`, never `kill -0`, which reads a foreign-owned live process as dead via EPERM) against `pid_started_at` within `RJ_PID_SLACK=30` seconds; a record with no `pid_started_at` answers `unknown` in the hooks and keeps the legacy signal probe in `worker-run` (its runs predate the field and must not start reading dead), never a verdict computed from the restampable `started_at`. Pid `0` is refused as a pid by BOTH readers before any probe: it is the pre-launch placeholder `worker-run` stamps before the supervisor exists, `ps -p 0` answers nothing while pid 1 answers, and read as a pid it says a run that has not STARTED is gone — which retires its record and sends everything it later writes to no journal, no debt row and no notice. An empty `ps` answer is read as death only once a probe of a pid that must exist (`ps -p $$ -o etime=`) shows `ps` can answer at all — the same bytes otherwise mean a sandboxed or failing `ps`, and both readers act on death destructively. `worker-run`'s own readers — `wait`, `report`, `busy_accounts` — go through the same test, or a chat is told to wait forever for a run the hooks have already retired. A bench run's `<run-dir>/delegated` stamp is read by the same pid rule (`../review-bench/share/rbench/round.py` `pid_still_running`, the one place the Python side spells it — asked by `triage_delegated` of a `delegated` stamp, by `live_worker_run` of a run record's own `pid`/`pid_started_at`, which is how the settle demand stays silent while a worker of the asking chat is still writing in the repository, and by `panel_still_running` of `<run-dir>/panel.pid`, which is how `review-bench wait` tells a panel that is working from one that died without handing anything over), with ONE earlier form: `claimed <session> <epoch>`, which `../claude-setup/hooks/worker-limit-gate.sh` writes at the Agent spawn for every `review-bench record <id>` the brief (or the brief file it names) carries — a worker takes about a minute to reach `worker-run`, whose pid stamp then overwrites the claim, and the Stop gate fired in that minute. A claim is live for `DELEGATED_CLAIM_SECONDS = 600` from its epoch and dead after, exactly as a gone pid; the hook never writes over a pid stamp | writers `bin/worker-run` (jq `.pid_started_at = $began` at supervisor launch); readers `bin/worker-run` `supervisor_running`/`PID_START_SLACK`, `../claude-setup/hooks/lib/review-journal.sh` `rj_run_liveness`/`RJ_PID_SLACK`; guards `tests/test_consistency.sh` and `tests/test_worker_run.sh` |
| av | Doctor snapshot compatibility | `<state_dir>/doctor-snapshot.json` holds `as_of`, `anomalies` (class → count), `total` (their sum) and `rows` (class → its first `DOCTOR_SNAPSHOT_ROWS = 12` detail rows of `{id, age, age_s, where, label, session}`, newest first by `age_s` with unknown ages last, the same rows the terminal report prints; `session` is the one field carrying a session id, read only by the menu's copy-open-command click, and no displayed field does — a chat is named). The menu takes class names from the snapshot's `anomalies` keys and hardcodes none. Doctor reports live panel progress directly. The doctor submenu is three sections of one menu — `Review` (the classes, `Rescan now`), `Weather: …` (`bin/llm-weather`, row `cq`) and `Gemini` (the review Flash pin, row `cs`, and nothing else — its version read off the `label` geminib's family cache carries, the slug shape only as a fallback); the parent appends ` · rescanning` while a rescan runs, and action rows read disabled `rescanning…` or `refreshing…`. | `../review-bench/share/rbench/debt.py` `doctor_snapshot_document`; `hammerspoon/llm-limits.lua` `readDoctorSnapshot`/`appendDoctor` |
| aw | One resolver names every chat | A chat surfaces under the name Claude Code gave it — the `ai-title` event its transcript records, or a `custom-title` Egor typed over that — and under nothing else. The `name` in `~/.claude-profiles/<profile>/sessions/<pid>.json` marked `nameSource: "derived"` is a placeholder the harness makes up from the working directory (`arbostar-frs-frontend-59`): he has never seen one, so it is refused and a chat holding nothing else surfaces as the first 8 characters of its session id. A record the harness did NOT derive carries a real name and is read. A worker run is not a chat — it surfaces as the chat that LAUNCHED it, off the run records of row `am`, and stays an id where two chats resumed one worker session or no record names a launcher at all, because a name of its own answers a question about a conversation with the errand it sent. Every surface that prints a session id prints that name beside it — a bare id is a chat Egor cannot find in his own window list — in one of two spellings, both `../review-bench/share/rbench/store.py`'s and both reading nothing of their own: `chat_display`, the name where the surface prints no id, and `chat_suffix`, ` (<name>)` after an id the reader needs machine-readable. This is DISPLAY only: session ids stay the on-disk grammar of every journal, artifact and receipt, and nothing here renames a stored key. Session-name collisions do not affect file discovery: row `am` attributes snapshot differences through each run's own `launcher`, independently of the transcript and session-name fold. The transcript ROOTS are this reading too — `~/.claude/projects` plus every `~/.claude-profiles/*/projects`, `CHAT_NAME_ROOTS` over both — because every chat on this machine runs under a profile, and a consumer globbing the default root for itself reads all of them as gone. A second reader of the title events, of the session records or of the launcher walk is how a name he has never seen reaches a surface that swears it is his | one implementation, `share/chat_names.py` (`chat_name`/`chat_label`, `NAME_PATTERNS`, `store_names`/`DERIVED_NAME_SOURCE`, `SHORT_ID`, `worker_run_launchers`, `transcript_roots`/`transcript_path`, env `CHAT_NAME_ROOTS`/`CHAT_NAMES_CACHE`); review-bench imports it from this checkout (`bin/review-bench` puts `../llm-legs/share` on `sys.path`) and keeps no copy; consumers `bin/chat-find` (`resolved_names`, `report_recent`) — and `bin/chats` through its JSON — `../review-bench/share/rbench/{store,debt,report}.py` (`chat_display`/`chat_suffix`), and `bin/chat-name`, the one-line entry point the shell surfaces that hold no Python go through (`../claude-setup/hooks/stop.d/notice-chat-names.sh`, which names every id this turn printed as `<original title> (<short id>)` and drops in silence the ones the resolver cannot name); none of them may read those stores itself; guards `tests/test_chat_names.sh`, `tests/test_chat_find.sh`, `../review-bench/tests/test_review_bench.sh`, `tests/test_consistency.sh` |
| ay | Sanctioned headless launchers | a vendor launched as a bare headless CLI call from a chat's Bash (`claude -p`, `claudeb … -p`, `codex exec`, `codexb … exec`, `gemini -p`, `geminib … --print`, `agy … --print`, `opencode run`, `grok … -p`, `grokb … --prompt-file`, `claudegpt p <acct> -p` — Claude Code print-running on a Codex account, which `claudeb?` cannot reach) is a worker nobody can see: no `worker-run` record, no statusline tag, no journal ownership, no pool refusal, no limit signature, no stall watch. Every headless run reaches a launcher that owns it, and the list is one list — `worker-run`, `review-bench`, `llm-limits`, `claudeb revive`, `claudeb warm`, `claude-session-driver`, `opencode-go`, `light-research`, plus the OWNED launchers, sanctioned only in the hand that owns them: `worker-run start|wait` inside a relay agent, `codex-image`/`gemini-image`/`grok-image` inside `image-gen` and nowhere else. The gate denies a command spelling a bare launch unless the same command names one of those launchers; it collapses quoted text into one word, then strips quotes and backslashes, then matches the vendor word only in command position — that order is what keeps a separator inside a quote (`echo "x; codex exec"`) from opening a command position the shell never had (string start or after `;`, `|`, `&`, `(`, a backtick or a newline, past any env assignments and the wrapper words `env`/`command`/`exec`/`nohup`/`nice`/`time`/`xargs`), so `'claude' -p`, `X="a b" claude -p` and `env claude -p` are denied while `mkdir claude -p` or a path operand is not; it fails open on its own errors. Interactive launches — no `-p`/`--print`/`--prompt`, no `exec`, no `run` — are the user and are never gated | policy `docs/routing-contract.md` § Sanctioned launchers; enforcement `bin/worker-launch-gate.sh` `LAUNCH_RES`/`SANCTIONED_RE`/`OWNED_RUN_RE`/`OWNED_IMAGE_RE`; registration in `~/.claude/settings.json`; guard `../claude-setup/tests/test_worker_launch_gate.sh` (the hook is registered in that repo, so its suite lives beside the registration) |
| bc | Gemini main's removal marker | ONE file, `~/.llm-limits-gemini.json.removed` — main's legacy cache path with `.removed` appended (`LLM_LIMITS_GEMINI_CACHE` moves the cache and the marker with it; `LLM_LIMITS_GEMINI_REMOVED` overrides the marker alone). `geminib remove main` and the menubar's `--gemini-remove` are the same operation on the same file, and its presence means main is ABSENT from the roster — no row, not even a removed one — undone only by deleting the file, never self-cleared by a later refresh. The per-account directory `~/.llm-limits-gemini/<name>.json.removed` holds NAMED accounts' markers and never main's | resolved once in `share/gemini-accounts.sh` `gemini_removal_marker`, which `bin/geminib` (`remove`, `gemini_main_removed`), `llm-limits.sh` (`gemini_legacy_removed`, `gemini_account_marker`) and `bin/worker-run` all reach through the shared module — neither script may spell the path itself |
| bd | Anchors store resolver | the review anchors store is ONE file per git FAMILY — `<git-common-dir>/review-anchors.json` — and every side resolves that directory with the SAME command: `rev-parse --path-format=absolute --git-common-dir`, spelled literally in `../review-bench/share/rbench/store.py` `git_common_dir`, in `../claude-setup/hooks/lib/review-journal.sh` `rj_store_of` and in `bin/statusline.sh` `journal_dir`, and as `rev-parse --absolute-git-dir --git-common-dir` in `../review-bench/bin/review-anchors`, which is the only WRITER. Per-worktree git dirs gave one project two ledgers: a settlement recorded from the main checkout cleared 33 paths while 12 stayed owed in a worktree of it, and the statusline and `review-bench debt` answered from different files (Egor, 2026-08-26). The file name is canonical once per side — `STORE = "review-anchors.json"` in `review-anchors`, `RJ_STORE_FILE=$common/review-anchors.json` in `review-journal.sh` — and no other file of either repository's `hooks/`, `bin/` or `share/` may spell it at all, since a private path is exactly how a second store grows back. Every write goes through one lock beside the file, `review-anchors.lock` held with `flock`, because two writers rewriting one JSON document lose whichever landed first | `../review-bench/bin/review-anchors`; `../claude-setup/hooks/lib/review-journal.sh` `rj_store_of`; `bin/statusline.sh` `journal_dir`; policy `../review-bench/docs/review-anchors-contract.md`; guards `tests/test_consistency.sh` and `../review-bench/tests/test_review_bench.sh` |
| bf | Chat repository index | the file is `$HOME/.cache/claude/review-journal/<session>.repos` — one absolute repository top per line, the same spelling a `.heads` row carries, no duplicates, written only for a session id that is a plain name (`''|.|..|*[!A-Za-z0-9._-]*` is refused) — and it is swept by NOTHING: the 120-minute `find` in `rj_snapshot_heads` matches `<session>.heads` and `<session>.*.heads` only, and outliving the calls that wrote it is the whole of what this file adds over them. It is what the one-panel rule enumerates candidates from beside the snapshots, and what `review-flow-gate.sh debt-total` sums the chat's debt over — the WHOLE list, with no per-render cap, plus the rendered repository the caller names. What it sums is `review-bench debt --total` per repository (row `ah`), never a per-tree count: the total is the chat's owed links over every tree of each family — every worktree, every branch tip, every stash entry — and the repository the caller stands in answers for all of them, so the same link reached through two members is not added twice. A member that cannot answer, or answers `unknown`, makes the total `unknown` rather than a number short by that repository. Worker repositories reach this index when `commit-journal.sh` imports the snapshot-derived `produced` rows of row `am`; shell-only edits use the same journal append and repository registration as other edits. So a suffix that drifts on either side leaves a repository invisible to `debt_owed_repos` and the chat launches a panel per repository | writer `hooks/lib/review-journal.sh` `rj_session_repos`/`rj_register_repo`, called at every journal append under an owner (`hooks/commit-journal.sh` `journal_row`, the one door every row of either journal is written through, and `rj_append_owned` itself for a worker's launcher); reader `../review-bench/share/rbench/store.py` `CHAT_REPOS_SUFFIX` in `chat_call_repos`; prose `../review-bench/docs/review-contract.md` §Debt one-panel paragraph |
| bh | Resume-timer Hammerspoon entry point | `ClaudeContinue.startTimerFor(id, minutes, customMessage, targetTty)` — `bin/claude-resume-timer` constructs the call positionally (a 2-arg and a 4-arg form), and the guard reads the provider fail-closed at the install path `${HS_ROOT:-$HOME/.hammerspoon}/claude_continue.lua`, which stays valid when the Lua moves repositories | provider `claude_continue.lua` (hammerspoon repo); caller `bin/claude-resume-timer`; guard `tests/test_consistency.sh` |
| bk | Grok worker knobs | Required defaults are `grok_model=auto`, `grok_effort=high`. `auto` is not a model NAME — it means whichever model that account defaults to — so nothing may substitute a version for it and no surface prints the word: `worker-run` omits `-m` entirely. The table effort union is `high|xhigh` for both model spellings; an effort outside it is REFUSED (`OUTCOME: EFFORT_REFUSED`, exit `4` before account resolution) and never clamped, since clamping spends the run at an effort the brief did not ask for | accepted knobs/default-writing semantics in `~/.claude/commands/worker.md`; routing defaults in `bin/worker-pick` (`gr_model`/`gr_effort`); launch defaults and the effort validator in `bin/worker-run`; pending-tag and live-tag fallbacks in `bin/worker-spawn-hook.sh` and `bin/worker-tag-hook.sh` |
| bj | Cache-TTL track line | The statusline learns which account a reply went through and how long its cache lives, and the chat picker reads that verdict back off the same file, so both the line's field ORDER and the bucket key it is derived from are shared. `$STATUSLINE_CACHE_DIR` (default `$HOME/.cache/claude-statusline`) `/cache-ttl-track-<session-id>` holds one space-separated line: version word `v2`, then `<assist_ts> <acct> <learned_upto> <ttl> <model> <uuid> <scan_bytes> <account_seen_upto> <account_seen>` — a reader takes the account from field 2 and the STAMP of the reply it was attributed to from field 1, counting the version word as 0 and naming both positions once (`bin/chats` `TRACK_TS`/`TRACK_ACCOUNT`), and `?` in the account field means the writer could not attribute the reply, which is no account at all. Those two accounts are not one: field 2 is the account the STAMPED reply went through, field 9 only the account that session was running under when it last looked, so a reader after the cache holder takes field 2. The two sides stamp DIFFERENT replies — the writer the newest of any kind, the picker's listing the newest one that SPOKE — so a track standing at or past the listing's reply is read as later knowledge of the same chat, its stamp the point the lifetime counts from, and one standing behind it attributes nothing. The cache lifetime itself comes from the reply's own `cache_creation` bucket keys, matched as `ephemeral_<n><m\|h>_` in both languages so a new bucket needs no edit. For a gateway chat field 2 is the Codex account (`CLAUDEGPT_ACCOUNT`), and the lifetime is the nominal `GATEWAY_CACHE_TTL` the listing gives the row, since gateway replies carry no bucket | writer `bin/statusline.sh` (`printf 'v2 %s %s %s %s %s %s %s %s %s\n'`); reader `bin/chats` `warm_account`; field order pinned writer-to-reader by `tests/test_chats.sh`; bucket key `bin/statusline.sh` jq `capture("ephemeral_(?<n>[0-9]+)(?<u>[mh])_")` and `bin/chat-find` `ttl_bucket`; prose `docs/statusline-contract.md` Persistence row |
| bi | Chat-switch cancel entry point | `ClaudeChatSwitch.cancel()` — no arguments; `hammerspoon/llm-limits.lua` reaches it through `_G` behind `and` guards, so a drifted signature degrades silently instead of erroring; the guard reads the provider fail-closed at `${HS_ROOT:-$HOME/.hammerspoon}/claude_chat_switch.lua` | provider `claude_chat_switch.lua` (hammerspoon repo); caller `hammerspoon/llm-limits.lua`; guard `tests/test_consistency.sh` |
| bl | Daily budget formula | the one metric that ranks accounts and vendors is the weekly budget per remaining day, defined once in `share/limits-view.sh` as `limits_daily_budget(effective_pct; limits_days_remaining(resets_at; now))`. Remaining days are floored at `0.25`, so a window minutes from its reset cannot read as unbounded budget; a reset the view refuses to print (placeholder or ancient, row `y`) answers `null` days, which the budget replaces with a neutral `7`-day window rather than that floor, so a garbage row never ranks first. No consumer may re-derive either number. The clamp is its own def, `limits_budget_days`, because a surface may print the two halves of the division — `bin/worker-pick`'s account rows read `<n>%/d ×<d>d`, and a divisor spelled a second time there would name a window the rate beside it was never paced over | defs `limits_days_remaining`, `limits_daily_budget` and `limits_budget_days` in `share/limits-view.sh`; consumers `bin/worker-pick` (the `−budget` rank key of row `g` and the vendor order under `auto`), `bin/codexb` `pick_account`, `llm-limits.sh` shield reconcile (row `bn`); prose `docs/routing-contract.md`; guards `tests/test_limits_view_budget.sh` and `tests/test_consistency.sh` |
| bm | Worker claims ledger | usage data lags a run by minutes, so a caller about to spend an answer records the selected account, and for `WORKER_CLAIMS_TTL` seconds (default `600`) that account ranks behind every unclaimed one (row `g`). `worker-run` and `light-research` ask with `worker-pick --account <vendor> --claim`; `codex-image`, `gemini-image`, and `grok-image` pick without `--claim`, validate the selected profile, then call the same `worker_claims_record` writer so an unusable profile does not consume a claim. It is a rank key, never a wall — a claimed account is still the answer when nothing else is selectable, and a query that cannot read the claims directory answers as if there were none rather than refusing to route. One empty marker file per claim, whose mtime is the entire state; nothing releases a claim explicitly. The human table, a reporting surface, never claims | `share/worker-claims.sh` is the TTL's and the layout's only home (`worker_claims_ttl`/`_record`/`_fresh`/`_prune`); `bin/worker-pick` `--claim`; claiming callers `bin/worker-run`, `bin/light-research`, `bin/codex-image`, `bin/gemini-image`, `bin/grok-image`; prose `docs/routing-contract.md`; guard `tests/test_worker_claims.sh` |
| bo | Reset consumable | The count is `reset_credits`, its expiry `reset_credits_expires_at`, and grok, codex and claude publish both. Every surface that renders the count at all renders it with the glyph `↻` and no vendor name attached: a vendor either published a count or it did not. The router (`bin/worker-pick`) renders none — it ranks on the budget alone, and a count printed beside a row reads as one that ranked it. The account row carries the count and the usual data age and nothing else; the expiry belongs to the submenu action that spends it, `Redeem usage reset · <when>`, worded and laid out identically for every vendor — the only per-vendor difference is whether a redeem RPC exists behind it. Each vendor's transport is spelled once: grok's `prod_mc_billing.ConsumerUiSvc` (path `/prod_mc_billing.ConsumerUiSvc/<Method>`, `GetRemainingResets` read, `RedeemReset` write) in `share/grok_resets.py`, codex's app-server JSON-RPC session in `share/codex_appserver.py` (`account/rateLimits/read`, `account/rateLimitResetCredit/consume`), claude's `cedar_ember` program in `share/claude_resets.py` (`GET /api/oauth/usage?cedar_ember=1` read — `bin/claudeb` asks the same URL for the snapshot — and `POST /api/organizations/<organizationUuid>/reset_rate_limits` write). All three writes spend a one-per-period consumable, so `bin/llm-reset-redeem` is their only caller and a menu click behind a confirmation is that script's only caller | field names in `grok-quota.py`, `codex-quota.py` and `bin/claudeb` `merge_usage`; `↻` in `llm-limits.sh` (`def credits` and the two table branches, none of them vendor-gated), `hammerspoon/llm-limits.lua` (`resetSuffix`), and its absence from `bin/worker-pick`; the redeem label in `hammerspoon/llm-limits.lua` `redeemResetItem` and `tests/llm_limits_renderer_harness.lua`; the grok service path and both method names in `share/grok_resets.py` alone, the codex session in `share/codex_appserver.py` alone (`codex-quota.py` and `bin/llm-reset-redeem` import it); the claude reset path and program name in `share/claude_resets.py` alone; the vendors with a redeem backend in `hammerspoon/llm-limits.lua` `RESET_REDEEM_VENDORS` |
| bn | Main-account shield | ranking no longer holds a base account back (row `g`), the pool does: a vendor's base account — the current claude profile, codex `main`; gemini and grok have none — leaves the worker pool once its daily budget falls below `WORKER_POOL_SHIELD_PER_DAY` (`3` %/day), recorded as `<pool_dir>/shielded/<account>` holding the weekly reset epoch and reconciled on every collect. Enabling the account by hand converts that marker to `<pool_dir>/shield-override/<account>` carrying the same epoch, which stops matching once the week rolls over. To `worker-pick` a shielded account is an ordinary pool exclusion with no rule of its own | constant and markers `share/worker-pool.sh` (`worker_pool_shield_set`/`_clear`/`_active`/`_override`, `worker_pool_override_current`/`_clear`); reconcile `llm-limits.sh` `reconcile_vendor_shields` and `apply_vendor_shield_state`; enable-override `bin/claudeb` and `bin/codexb`; prose `docs/routing-contract.md`; guard `tests/test_worker_pool_shield.sh` |
| bp | Per-vendor pause | a pause PARKS a vendor for months, spelled `<vendor>_paused=on` in `~/.claude/worker-model` for `claudeb`/`codex`/`gemini`/`grok`/`opencode` — `opencode` included, though no worker-pick leg staffs it, because review-bench does. The literal `on` is the only veto and an ABSENT key is the running state, so resuming DELETES the line rather than inventing an `=off` spelling; a hand-edited duplicate resolves FIRST-wins, exactly as the role keys do (row `aj`). The one writer is `share/worker-model.sh` `worker_model_set_paused <vendor> on\|off`, under the pin's own lock (row `ae`) and refusing a session (`CLAUDECODE`) outright, since parking a vendor takes it out of every router at once. **A parked vendor does not exist for the infrastructure**: not a wall, not a switch, not an `off`/`paused` word anywhere — no store entry, no NEXT segment, no vendor line, no roster slot and no skipped record. The mechanism is ABSENCE, the same interface a leg this machine never installed leaves: the collectors omit `vendors.<key>` from `~/.llm-limits.json` (`llm-limits.sh` `delpaths`, last of all its passes), and `bin/worker-pick` renders a vendor the store carries no row for as nothing at all. `worker-pick` reads the switch directly as well, because the collector only drops the vendor on its next run: a snapshot written before the pause must not keep it on screen. Roles are left exactly as they were. **Pause > pin** — the one place in the ladder where the pin does not answer over a switch: the pin line stays in the file for the day the vendor comes back, is not cleared and is not mentioned, and the pinned account drops out of the DATA-age reckoning with it. `worker=<paused vendor>` falls back to `auto` SILENTLY — a pause is a decision made after the toggle, and a warning on every run would be noise. The refusal is a parsed contract: `worker-pick: <vendor> is paused (<vendor>_paused=on in ~/.claude/worker-model)` at exit `3`, worded identically by the bench's `paused_note`. `--refresh-account` on a parked vendor is refused rather than quietly skipped, and Resume re-collects it on the next run. | writer `share/worker-model.sh` `worker_model_set_paused`; collectors `llm-limits.sh` `pause_config_vendor`/`paused_vendors`/`vendor_paused`/`delpaths` and `bin/llm-refresh`; router `bin/worker-pick` `vendor_paused`/`vendor_known` and the `--account` refusal; menu `hammerspoon/llm-limits.lua` Pause/Resume items; bench `../review-bench/share/rbench/accounts.py` `worker_model_switch`/`vendor_paused`/`paused_note`/`refuse_paused_sides`/`drop_paused_specs` and `../review-bench/share/rbench/verify.py` `default_verifier`; prose `docs/routing-contract.md` Pause |
| bq | Allowed worker models | `share/worker-model.sh` `worker_model_table` is the source for models and per-model efforts. Defaults: claudeb `opus`, codex `astra` (the newest slug of the family `codexb models` lists, row `cv`), gemini the newest Flash family `geminib families` prints, the table's first gemini row since `pro` is emitted last whatever its version (also every other slug the list prints, row `cr`), grok `auto` (the CLI's own default, plus every slug `grokb models` prints, row `cu`). Opus defaults to `high`; brief efforts are `high`, `xhigh`, and word efforts are `low`, `medium`, `max`. Additional models claudeb `fable`, codex `sol` and gemini `pro` require Egor's explicit word in this chat. Each model accepts only the union of its brief and word effort columns; word efforts require his ask as orchestrator policy. Gemini is the one vendor whose efforts are not refused but raised to `high` (row `i`). Neither Codex model permits `max`. Effort precedence: `--effort` (brief `EFFORT:`) > `<vendor>_effort` > resolved model's table default. Models outside the table yield `OUTCOME: MODEL_REFUSED`; efforts outside the union yield `OUTCOME: EFFORT_REFUSED`. Both exit `4` before account resolution, run directory creation or quota spend; neither walls an account or triggers rerouting. `/worker` refuses to store values outside the table | table and helpers `share/worker-model.sh`; launch validation `bin/worker-run`; write gate `bin/worker-pin-gate.sh`; policy `share/worker-policy.md`; `docs/routing-contract.md` Models; `~/.claude/commands/worker.md` |
| br | Instruction-file classes and the one span | two hooks answer for the files an LLM re-reads across sessions, and the moment they classify a path differently one door denies what the other reports. So the class table is spelled ONCE, `instruction_write_class` in `share/instruction-files.sh`, and it answers exactly four ways: `always` for `CLAUDE.md`/`CLAUDE.local.md` (denied whatever the byte delta, inside Egor's autonomy span as much as outside it), `debt` for `review-debt-ignore` (denied always, its own reason), `span` for every other `.md`/`.markdown` under the class directories (the extensions are one list too, `INSTRUCTION_MD_EXTENSIONS`, which the class table and the enumerating `find` both read — a second spelling leaves a file one door speaks for and the other never watches), and NOTHING for `settings.json` — no gate speaks for it, since nothing re-reads it into a context window and the harness rewrites it on every model or permission-mode switch, while it stays watched as the one guarded file git cannot give back. WHAT THE TRIPWIRE WATCHES is that enumeration plus a bounded tail, `instruction_ranked_*` in the same module: the `always` class speaks for any project `CLAUDE.md`/`CLAUDE.local.md` and "every project on the disk" cannot be enumerated, so tokenmap's `read-rates.json` — the index both gates already quote prices from — is cut at `INSTRUCTION_RANKED_MAX` by monthly reads, memory files and `.claude/worktrees/` copies excluded, deduplicated against the enumeration by RESOLVED TARGET (`~/.claude/CLAUDE.md` is a symlink onto the ranking's own first row, and one file under two names reports every change to it twice), cached in `ranked.txt` with a `#<version>` first line and re-cut at session start alone. A path the session-start re-cut brings in is recorded silently, never reported ADDED: nobody added it, the index moved. The cache is never authority, though, and neither is a baseline: a path a baseline watches stays watched while it exists whatever `ranked.txt` says later, a missing or empty baseline is itself an event (`baseline-missing`), and SessionStart compares against the newest baseline on disk (`changed-between-sessions`). The enumeration itself is every `.md`/`.markdown` under `~/.claude` at any depth (`projects/` pruned: transcripts and the model-written memory files) plus, in the payload's repository, `CLAUDE.md`, `CLAUDE.local.md`, `SKILL.md` and every markdown under a `.claude/` directory (`instruction_repo_files`); names are matched case-folded, since the volume folds them. DELIVERY is split in the same spirit — the hook writes `events.jsonl` and pokes, `hammerspoon/instruction-watch.lua` renders and writes `receipts/<id>`, and the three claims stay distinct (`sent: attempted` = the call was made, a receipt = the module read the record, `alerted: true` = a line went on screen; none of them says Egor saw it). The class directories are one list too, `_instruction_class_dirs`: `~/.claude/{docs,agents,instructions,skills,skills-on-demand,rules,commands}`, depth-unbounded and followed through their symlinks, and a directory the gate refuses writes to while the tripwire does not watch it is the one hole neither half can report (`commands` was exactly that). `bin/instruction-write-gate.sh` (PreToolUse Bash) and `bin/instruction-watch.sh` (PostToolUse/SessionStart) both ASK those two functions and carry no class list, no basename set and no directory loop of their own. WHERE A COMMAND LEAVES ITS BYTES is one parse, `instruction_write_targets` in the same module: two parses of one line is how a `.bak` sibling of a guarded name was a write to one door and not the other. No hook spells a redirection operator class, a copy-verb alternation or a destination-verb alternation of its own; the gate filters the parse's ROWS — the redirect, tee, copy (cp, mv, ln, install, rsync, `git checkout`/`restore`, `git apply`/`stash pop` landings), dd and patch rows, refusing outright (exit 2) a `refuse` row whose name holds a tab or a newline, every one of them judged whole, its own file's class against its own `trunc`/`append` mode, since a class read off the first row while the shrink shape was read off any row let a compound grow one guarded file on a later row's ability to shrink another; the tripwire reads no command at all and attributes by the clock — the bytes are a call's when their mtime lies inside the `inflight/<sid>` window both gates open at PreToolUse and no other session's; no mark or two windows is never a revert. Egor's autonomy span has ONE definition as well — `rj_autonomous` in `../claude-setup/hooks/lib/review-journal.sh`, over `words_span_on` in its `hooks/lib/words.sh` (the phrase and what ends the span live in `hooks/lib/word-families.json`, read by `hooks/word-intake.sh`) — reached through `instruction_autonomous` in the shared module and nowhere else: no llm-legs file names `rj_autonomous` a second time or spells the phrase, or a door would answer differently from the one beside it. What the span changes is one thing only: a `span`-class write that cannot grow the file passes the gate, and growth this session's own call produced is put back by the tripwire; `always` and `debt` are outside it entirely. | Two doors outside that pair read the same parse rather than a matcher of their own: `bin/worker-pin-gate.sh`'s Bash mode, which takes EVERY row kind (a copy over the account pin and a runtime that opens it move it as surely as a redirection does) and adds only deletion, the one write shape the parse does not model; and claude-setup `hooks/review-flow-gate.sh`, which asks `instruction_shell_scan` alone to tell a review LAUNCH from the word standing in a heredoc body, a grep pattern or a quoted sentence, names this module by absolute path (`REVIEW_FLOW_SHELL_PARSE` overrides it) the way that repository's other hooks name `bin/worker-pick`, and says so inside its own refusal when it cannot read it rather than falling back in silence. Suites: `bash tests/test_instruction_gate.sh`, `bash tests/test_worker_pin_gate.sh`, `bash tests/test_consistency.sh`, claude-setup `bash tests/test_review_flow_gate.sh` |
| bs | Refresh error list | Canonical vendor field `refresh_errors` is `[{account: string\|null, class, cause, at}]`. Classification lives once in `llm-limits.sh` `classify_cause` (`402 payment required`, `workspace deactivated` (a 402 whose body carries `deactivated_workspace` — dead auth, not a usage wall), `401/403 auth`, `usage limit reached`, `429 rate limit`, `5xx server error`, `timeout`, `login needed`, `not refreshed (<why>)`, `helper missing`, `refresh disabled`, else `refresh failed`). A spent window and an endpoint refusing the read are DIFFERENT classes and the wall is tested first, because `bin/llm-refresh` loosens its whole refresh ladder off the second one: pushback is a VERDICT about the read (`429`, `too many requests`, `rate limit exceeded`, `rate-limited`, `RESOURCE_EXHAUSTED`, spelled once as `REFRESH_PUSHBACK_RE` and matching the 429 branch here) and never the NAME of what is read — the codex usage RPC words every failure `failed to fetch codex rate limits: <real cause>`, so a bare `rate limit` match made an auth-dead account and a 402 workspace read as rate limits and walked codex 30→45→60 (2026-09-02..2026-09-07). The account's own wall (`usage limit`, `quota exceeded`/`exhausted`/`reached`, `out of credits`, `credits depleted`, `rate_limit_reached`, spelled once as `REFRESH_WALL_RE` and as `codex_usage_wall` in `../review-bench`) VETOES a pushback verdict on the same line, including a 429 whose body restates the wall, and is journaled as `usage wall, not endpoint pushback: <excerpt>` rather than loosening anything. Lua renders one `⚠ <account\|vendor>: <class> · <age>` row per entry (account rows under that account, vendor-wide under the vendor header, never between blocks); raw cause is the submenu. An entry whose account is not on the vendor roster is not rendered and is dropped at the next store write. `refresh_error` `{cause,at}` is derived from the list for one release (`bin/llm-refresh` still reads it) and the two shapes are pinned here | producer `llm-limits.sh` `classify_cause`; renderer `hammerspoon/llm-limits.lua`; reader `bin/llm-refresh` `recorded_errors`, `REFRESH_PUSHBACK_RE`/`REFRESH_WALL_RE`/`pushback_hit`/`wall_hit`; bench `../review-bench/share/rbench/accounts.py` `codex_usage_wall`; guards `tests/test_llm_limits.sh`, `tests/test_llm_refresh.sh`, `tests/llm_limits_renderer_harness.lua`, `tests/test_consistency.sh` |
| bt | Native agent types | One owner: `bin/worker-spawn-hook.sh` alone decides which agent types may spawn, on EVERY parent session, a model-less or unreadable transcript included. Hooks on one `PreToolUse(Agent)` call run side by side and any deny outranks every allow, so a second native list in another gate is a rule nobody can follow. Workers are unified: every run that edits, reviews, verifies or scans is a relay worker through `worker-run` (`RELAY_TYPES`), and a NATIVE agent type runs on the SESSION's model instead. The native allowlist is exactly `fork review-waiter light-research image-gen` (`NATIVE_ALLOWLIST`): `fork` runs only on Egor's word, `review-waiter` only blocks on `review-bench wait` so the run has a task row, `light-research` and `image-gen` are relay wrappers. Every other type — `Explore`, `Plan`, `general-purpose`, `claude-code-guide`, `claude`, `statusline-setup`, a typeless spawn, anything custom — is DENIED with the ask to use a relay worker (worker-run) instead; the deny carries NO retry stamp, since a one-shot escape is a rule a model walks through by calling twice. `bin/worker-limit-gate.sh` judges no native type: its only non-worker rule is `image-gen`'s older session-account rule with its one retry, judged only on an orchestrator session — Fable and the `claudegpt` gateway aliases, `claude-fable-*` and `anthropic.ccr.*`, one predicate (`orchestrator_model`) and no second literal, because a gateway chat is Claude Code on an OpenAI subscription (`docs/claudegpt.md`) | the lists live once in `bin/worker-spawn-hook.sh` (`RELAY_TYPES`, `NATIVE_ALLOWLIST`), the session-model predicate once in `bin/worker-limit-gate.sh` (`orchestrator_model`); prose in `docs/routing-contract.md` "Sanctioned launchers"; guards `tests/test_statusline_hooks.sh`, `../claude-setup/tests/test_worker_limit_gate.sh` and `tests/test_consistency.sh` |
| bu | Worker run deadlines | A run ends on INACTIVITY first and on the wall clock only as a last resort. `WORKER_RUN_IDLE_S` (default `1800`s) is measured over everything the run writes — `out`, `err`, `log`, the session transcript row `bv` resolves, and the newest edit under the workdir, which is the same reading `LAST-EDIT` prints, so a status row calling a run active cannot sit beside a watchdog killing it (`workdir_activity_stamp`, the one definition both read) — and a worker counts as active the moment a tool RETURNS, so the number only has to clear the longest single legitimate tool call; `0` disables it. An idle kill takes POSITIVE evidence of inactivity: every vendor but claudeb streams into `out` while it works, so silence there is evidence, while claudeb writes `out` once at exit and a claudeb run whose transcript was never located and which has touched no file emits no live signal at all — that run is left to the ceiling instead of being killed for being unobservable (live 2026-09-04: exit 143 at thirty minutes over a worker whose `LAST-EDIT` was 59s). Either watchdog names itself in `wait`/`report` (`KILLED: idle watchdog …` / `KILLED: deadline …`), since a bare 143 reads as a vendor fault. A THIRD ender is a signal: the supervisor traps TERM/INT, stops the vendor child's whole process group (launched through `setsid` so one signal takes the CLI and not only the wrapper), records `KILLED: signal <n> …` and never reroutes — killed and left alone, the child ran to completion while its record never got an exit code (live 2026-09-04). `WORKER_RUN_DEADLINE` (default `21600`s) is the ceiling for a CLI that wedges while still writing, and gemini's own `--print-timeout 360m` follows it rather than standing on its own number. Neither is a work cap: the old 170-minute ceiling killed two real runs mid-edit (2026-09-02, 2026-09-04), and a killed run hands nothing back | `bin/worker-run` (`IDLE`, `DEADLINE`, `activity_fingerprint`, `workdir_activity_stamp`, `killed_line`, `run_with_deadline`, the gemini `--print-timeout`); guard `tests/test_worker_run.sh` |
| bv | Launched brief vs recorded brief | `worker-run` hands the vendor `<run-dir>/brief.launch` — the caller's brief with `BRIEF_PREAMBLE` APPENDED — and records `<run-dir>/brief` as the input, byte for byte, because `report`, RESUME and ATTACH quote that one back and a preamble growing on every hop is a brief nobody wrote. Appended, never prepended: the brief's own first line is load-bearing (`RESUME <id>:`, `ATTACH <id>:`, read by the relay agents and by `worker-limit-gate.sh`). The preamble is the test-loop rule, spelled ONCE in `worker-run` and never repeated in a brief. A brief whose first line is blank is refused at `start` (exit 4), since that line is what every reader identifies the run by. Each LAUNCH appends a `RUN-TOKEN: <run-id>-a<attempt>` line of its own and rewrites `brief.launch`, so the effort-flag retry and every walled reroute carry a token nobody else has. A claudeb run's session id is resolved from the profile transcript whose first user record CARRIES THAT TOKEN — `<profiles-root>/<account>/projects/<cwd-slug>/<session>.jsonl`, newest first, no older than the attempt's own `brief.launch`, given up on after ten minutes — and written to `<run-dir>/session` / `<run-dir>/session-file` while the run is alive, so `session_id` prefers `out` and falls back to it and a killed run still reports `SESSION:`. Brief text is NOT an identity: every profile writes into the one transcript tree, a co-tenant run may carry the same brief word for word, and a relaunch would otherwise adopt the transcript of the attempt it just abandoned (a run launched before the token existed still matches on its text, prefix fallback included, and an empty first line never becomes that prefix) | `bin/worker-run` (`BRIEF_PREAMBLE`, `brief.launch` composition, `claudeb_discover_session`, `first_user_text`, `record_session_file`, `session_id`); consumer prose `share/worker-policy.md`; guard `tests/test_worker_run.sh` |
| bw | Codex deactivated-workspace wall wording | Codex's live failure carries both `Payment Required` and `deactivated_workspace`. The run classifier and refresh-error classifier keep both spellings: the former treats the failed relay account as walled, while the latter explains the stored refresh failure as workspace deactivation | `bin/worker-run` `has_limit_signature`; `llm-limits.sh` `classify_cause`; guard `tests/test_consistency.sh` |
| bx | A spent SuperGrok plan is one wording in both repositories | what retires a grok account is the PERSISTENT wording alone — `hit the rate limit for your plan`, `hit the credit limit for your plan`, `subscription:free-usage-exhausted`, `run out of credits`, `you['’]ve reached your free Grok Build usage limit for now`, HTTP `402` — and never a status code, since grok answers 429 and every 5xx for capacity weather too. Two independent readers ask it: `bin/worker-run` (`GROK_LIMIT_RE`), which reroutes a brief, and `../review-bench/share/rbench/accounts.py` (`GROK_WALL_RE`), which retires the account for the panel. Drifted apart, one of them takes the whole leg out of every panel for a day over weather the other shrugs at | `bin/worker-run` `GROK_LIMIT_RE`; `../review-bench/share/rbench/accounts.py` `GROK_WALL_RE`; guard `tests/test_consistency.sh` |
| by | Codex out-of-credits wall wording agrees across relay and bench | `out of credits` retires a Codex account in both the implementation relay and a review panel. It is a persistent workspace wall, not provider weather, and neither reader may leave the other retrying or reusing the account | `bin/worker-run` `has_limit_signature`; `../review-bench/share/rbench/accounts.py` `codex_usage_wall`; guard `tests/test_consistency.sh` |
| bz | Run-observed worker wall records | `${XDG_CACHE_HOME:-$HOME/.cache}/claude-worker-runs/walls/<vendor>-<account>` holds two unix-epoch lines: the reset of a wall `worker-run` observed (unparseable RESET → now+3600) and the write time. An unexpired record is a USAGE wall for `worker-pick` (`WALLED` flag, excluded from NEXT) until llm-limits holds a reading of that account newer than the write with both windows under 100%: the record stands in for the collector, never overrides it, and `worker-pick` lapses it then (`worker_walls_lapse_if_read`); a one-line record stays a wall until its reset. A met wall on a pinned account also removes that one name from the file the pin came from — `~/.claude/worker-model` or the chat pin file of row `ck` (`worker_model_clear_walled_pin`); a `*` pin is removed only once every pool account it covers holds an unexpired record; `worker-pick` clears a still-present pin once when it finds an unexpired record. Stale usage data never skips or clears a pin. The reader ignores an expired record and may delete it. `WORKER_WALLS_DIR` relocates the directory for tests, never the live path. Distinct from the OpenCode JSONL of row `ai`. | writer `bin/worker-run` (`record_run_wall` via `share/worker-walls.sh`); clearer `share/worker-model.sh` `worker_model_clear_walled_pin`; reader `bin/worker-pick` (`worker_walls_fresh`, `clear_observed_pins`); guard `tests/test_consistency.sh` |
| ca | Pin key shape and single parser | `<vendor>_profile=<name>[,<name>...]` in `~/.claude/worker-model` is a comma-separated set of pinned account names (order in the list is irrelevant; the rank vector orders the tier), or `<vendor>_profile=*` — the vendor pin, standing alone and never beside a name — which `worker_model_pins` expands to every account the limits store carries for that vendor that the pool admits (`worker_model_pool_accounts`). `worker_model_pin_scope <vendor>` answers `account`, `vendor` or `none`; worker-pick ranks tier 0 (account), 1 (vendor), 2 (unpinned). ONE parser and ONE writer: `share/worker-model.sh` `worker_model_pins` / `worker_model_pin_add` / `worker_model_pin_remove` / `worker_model_pin_first`. Adding a name over `*`, or `*` over names, replaces the line. Remove drops the key when the list empties. Every `*_profile` reader goes through that parser. `bin/statusline.sh` is excepted while another edit owns it. | `share/worker-model.sh`; `bin/worker-pick` `pins_json`/`pin_scope`/`next_cells`; vendor CLIs `use`/`--unpin`/`--clear`; `hammerspoon/llm-limits.lua` pin set; guard `tests/test_consistency.sh` |
| cb | Codex quota kick cadence | next probe allowed `600`s after a probe that ran, `1800`s after one that failed | `bin/statusline.sh` `codex_quota_kick` holds both as `local ok_after=<n> fail_after=<n>` — the deadline stamp is written from them and nothing else may hardcode either; `docs/statusline-contract.md` "Codex quota kick" prose (`+600s` / `+1800s`); `docs/DIAGNOSTICS.md` symptom row, whose triage rule reads a stamp against them. This row is NOT row a: the probe interval only has to stay under `LIMITS_STALE_FIVE_HOUR` so the cell never dims, and the backoff equalling the `llm-refresh` base cadence is a coincidence — changing a staleness threshold does not move either number |
| cc | Gateway model aliases and their labels | `claudegpt`'s two aliases are `sol` and `astra`, the model ids Claude Code records for them are `anthropic.ccr.sol` and `anthropic.ccr.astra`, and every column naming one shows `Sol` / `Astra` — never the raw id, which no other surface prints either | `bin/statusline.sh` maps the two ids to the two labels in its own `case` (the statusline is rendered inside the chat and reads them off the payload); `share/chat_resume.py` `GATEWAY_LABELS` is where the pair lives for everything that reads a TRANSCRIPT — `bin/chats` `model_label` and `bin/chat-find` `short_model` take their column from it and spell no id of their own, reopen/launch lines are spelled once in `share/chat_resume.py` (`resume`, `switch`, `launch`); `claude_chat_switch.lua` requires the resolved launcher and spells no fallback (guard: `tests/test_consistency.sh`); and `gateway_alias` turns the same id back into the `--model` argument the reopen command carries; `bin/claudegpt` `FAMILIES` names the same two aliases on the launcher side and resolves each through `codexb models --family` (row `cv`); prose `docs/claudegpt.md` |
| cd | Codex main's removal marker | ONE file, `~/.llm-limits-codex.json.removed` — main's legacy cache path with `.removed` appended (`LLM_LIMITS_CODEX_CACHE` moves the cache and the marker with it; `LLM_LIMITS_CODEX_REMOVED` overrides the marker alone). `codexb remove main` and the menubar's `--codex-remove` are the same operation on the same file, and its presence means main is ABSENT from the roster — no row, not even a removed one — undone only by deleting the file, never self-cleared by a later refresh. Named codex accounts are removed by deleting their profile directory and have NO marker, so `codex_removal_marker <named>` fails rather than naming a path | resolved once in `share/codex-accounts.sh` `codex_removal_marker`, which `bin/codexb` (`remove`, `account_names`, `account_exists`, `pick`, `run`), `llm-limits.sh` (`codex_legacy_removed`, `--codex-remove`, the roster filter), `bin/worker-run` and `bin/statusline.sh` all reach through the shared module — no script may spell the path itself |
| ce | Gateway context window and its derived cuts | `872000` is the ONE literal, `bin/claudegpt`'s `CONTEXT_WINDOW`, which the launcher hands the router (`--context-window` on every alias) and the chat (`CLAUDE_CODE_MAX_CONTEXT_TOKENS` and `CLAUDE_CODE_AUTO_COMPACT_WINDOW`, both set to it and both removed again before `worker-run` starts a vendor session). Every cut BELOW it is derived and no site may spell one: Claude Code's autocompact trigger is the window less its `20000` output reserve and `13000` summary tokens, and `../claude-setup/hooks/context-nudge.sh` reads `CLAUDE_CODE_AUTO_COMPACT_WINDOW` out of the environment and subtracts its own `RESERVE` of `33000` — the two reserves added up — so both land on `839000` at this window and the nudge speaks at the cut Claude Code actually compacts at, by construction rather than by coincidence. The statusline's `ctx` cell is a percentage of the window the harness payload carries and holds no constant either, so raising the window moves all three and only `CONTEXT_WINDOW` is edited | literal `bin/claudegpt` `CONTEXT_WINDOW`; prose `docs/claudegpt.md` "Thresholds" (`872000` and the derived `839,000`); env reader `../claude-setup/hooks/context-nudge.sh` `RESERVE` (never edited from here — it is the fallback ceiling for every non-gateway chat too); guard `tests/test_consistency.sh`, which recomputes `872000 - 33000` against the doc |
| cf | The `anthropic.ccr.` model-id prefix | ONE prefix names a gateway session model, and five sites spell it because each reads or writes a different carrier: `bin/claudegpt` `menu_settings` WRITES the two ids into the chat's own `/model` picker (the launcher's aliases themselves, `FAMILIES`, carry no prefix — the router prepends it), `bin/worker-limit-gate.sh` `orchestrator_model` reads the model of the SESSION (`anthropic.ccr.*` binds the orchestrator doctrine, row `bt`), `share/chat_resume.py` `GATEWAY_PREFIX` reads a TRANSCRIPT and is the only home for every surface that does (`bin/chats`, `bin/chat-find`, the chat-link hooks — none spells an id, row `cc`), `~/.hammerspoon/claude_compact.lua` `validFullModel` reads the `/model` RETURN id a compact has to restore, and `bin/statusline.sh` reads the harness PAYLOAD in its label `case`. A new surface reads one of those five carriers or it has no business with the prefix | `bin/claudegpt`, `bin/worker-limit-gate.sh`, `share/chat_resume.py`, `~/.hammerspoon/claude_compact.lua` (its own repository, cited and never edited from here), `bin/statusline.sh`; guard `tests/test_consistency.sh` greps each |
| cg | Report queue root | `${XDG_CACHE_HOME:-$HOME/.cache}/claude-reports` | `bin/report-bus` `ROOT`; `docs/report-bus.md`; `docs/DIAGNOSTICS.md` report symptom row; guard `tests/test_consistency.sh` |
| ch | Report worker/subagent skip predicate | `CLAUDEB_WORKER=1`, nonempty `agent_id`, `transcript_path` containing `/subagents/`; `agent_type` in `codex-worker`, `claudeb-worker`, `gemini-worker`, `grok-worker`, `light-worker`, `image-gen`, `light-research` | `bin/report-bus` `skip_flush`; `../claude-setup/hooks/stop.d/ask-run-unfinished.sh` environment, agent and transcript markers; `../claude-setup/hooks/worker-tag-hook.sh` agent-type list; `docs/report-bus.md`; guard `tests/test_consistency.sh` |
| ci | Interactive account status | ONE module, `share/account-status-tui.sh`, owns rendering, keys, sorting, async refresh and the chat-role worker-pick query for both `claudeb status` and `codexb status` on a TTY; each entrypoint lists ONE vendor's accounts (`claudeb` the Anthropic ones, `codexb` the OpenAI ones) and the star and initial selection apply within those rows, otherwise the first one; Enter launches the selected profile through the vendor adapter — `claudeb profile <name>` for Claude (native `claude`, Anthropic models), `claudegpt p <name> --model astra` for Codex (Claude Code on that OpenAI account; every other `codexb` verb stays native Codex); each non-TTY renderer stays vendor-owned | adapters `bin/claudeb` and `bin/codexb`; guards `tests/test_account_status_tui.sh`, `tests/test_claudeb.sh`, `tests/test_codexb.sh`, and `tests/test_consistency.sh` (no vendor-local key loop or renderer) |
| cj | Alias a SWITCH onto an OpenAI account opens on | `astra` — the `codexb status` picker's Enter and "Switch chat to this" onto a Codex row name it explicitly, whatever the old chat, its stamp or the launcher's own default said; a bare `sol`/`astra` on the command line overrides it, a jump onto a Claude account is untouched, and an ordinary REOPEN is not a switch: `resume_argv` and a bare `switch_argv`/`launch_argv` keep the alias the chat was launched with | `share/chat_resume.py` `GATEWAY_SWITCH_ALIAS`, applied in `main`'s `switch`/`launch` modes (the "Switch chat to this" CLI) and never inside the library helpers; `bin/codexb` `codexb_status_model`; `docs/claudegpt.md`; guards `tests/test_claudegpt.py`, `tests/test_claude_chat_switch.sh`, `tests/test_codexb.sh`, `tests/test_consistency.sh` |
| ck | A worker run carries the review round it fixes | a brief whose first line — or second, after a `RESUME <id>:`/`ATTACH <id>:` line — is `ROUND: <run-id>` names the review-bench round the run fixes; the id matches `REVIEW_ROUND_RE='^[0-9]{8}T[0-9]{6}Z-[0-9a-f]{7}(-[0-9]+)?$'` and names a bench on record (`<worker-stats>/benches/<id>`), else the launch is refused with one line naming the shape. `worker-run start --round <id>` names the same round on the command line and wins over the header, which stays the fallback. With neither, the whole brief text is scanned for round-shaped whole tokens and the launch is bound to the one that is both on record and still has a confirmed finding left (`review-bench fix <id> --print` prints a rule; a settled round prints nothing, an unknown one fails the launch) — exactly one such id binds and prints one line, two or more refuse the launch and ask for a `ROUND:` line. The prose scan is a door of the `workers` role alone: a `--role research` run reads a round's logs and edits nothing, so its prose binds no round, and a `--round`/`ROUND:` on a research launch is refused. Whichever door it came through the run stores it as `review_round` in its `meta.json`, beside `round_source` (`flag` · `header` · `brief-text`) and hands it to the anchors store at the run's end (`review-anchors run-fold --round <id>`), which is what closes the round on the paths the run touched | `bin/worker-run` `review_round_ok`/`brief_review_round`/`brief_text_rounds`/`fold_run_anchors`; `../review-bench/bin/review-anchors` `run-fold`, `../review-bench/share/rbench/round.py` `round_brief_line`; both consistency suites |
| cl | Chat pin file | `${CHAT_PINS_DIR:-$HOME/.cache/claude-chat-pins}/<session_id>`, the session id being `CLAUDE_CODE_SESSION_ID` (an id holding `/` or `..` names no file). Same `<vendor>_profile=<name>\|*` lines as row `ca`. Beside the pin line the file may carry `<vendor>_fast=on`, written and cleared by the same `chat-pin` call (`grok-fast`/`codex-fast`) and read by `worker_model_chat_fast`: it makes a WORKERS run of that vendor fast (grok takes `worker_model_grok_fast_sibling`, codex the priority tier) and the statusline appends `⚡` to the pin label. Existing and non-empty, it REPLACES the global pin tier for that session: every `*_profile=` line of `~/.claude/worker-model` is ignored there; absent, empty or no session id means the global pins. Resolved for routing ONLY by `worker_model_pin_file` in `share/worker-model.sh`; the default path is spelled twice more, each pinned verbatim: `bin/worker-pin-gate.sh` `chat_pins_dir` (the write gate over the directory) and the `bin/statusline.sh` `pin` segment (reads this session's file only); a met wall (`worker_model_clear_walled_pin`) edits whichever file the pin came from. The Agent hooks export `CLAUDE_CODE_SESSION_ID` from their stdin `.session_id` before reading a pin or asking `worker-pick`; the collector reads the grok pin with the id blanked, since the store is every chat's. | `share/worker-model.sh` `worker_model_chat_pin_file`/`worker_model_pin_file`/`worker_model_pinned_account`; `bin/worker-limit-gate.sh`, `bin/worker-spawn-hook.sh` session export; `llm-limits.sh` `grok_pin`; `bin/worker-pin-gate.sh` `chat_pins_dir`; `bin/statusline.sh` `pin` segment; guards `tests/test_worker_pick.sh`, `tests/test_worker_run.sh`, `tests/test_consistency.sh` |
| cm | Chat pin grant | `${WORDS_DIR:-$HOME/.cache/claude/words}/<session_id>/grant.pin` — the pin grant claude-setup's `hooks/word-intake.sh` writes when his words put this chat's workers on a vendor, an account or `auto`; fresh within `ttl:30m`, it lets `chat-pin` write this chat's pin file for the exact target it holds, or `auto` when it holds a vendor. Without the words library `chat-pin` reads the older `<state_dir>/pin-grants/chat-<session_id>`, which nothing writes any more. | `bin/chat-pin` `granted` (reader, `words_grant_target`); `share/worker-model.sh` `worker_model_chat_pin_grant` (the fallback's path); claude-setup `hooks/word-intake.sh` (writer); guards `tests/test_chat_pin.sh`, `tests/test_consistency.sh` |
| cn | Gemini capacity fallback | One mechanism for every Gemini consumer, in `bin/geminib`'s launch path, so no worker, research leg or review cell carries one of its own. A headless launch (`--print`) naming a chain family always gets a `--log-file` — the caller's when it passed one, else `${GEMINIB_CACHE_DIR:-~/.cache/geminib}/logs/<ts>-<pid>.log` prepended to the arguments — and the log is watched once a second for `No capacity available for model`. A relaunch re-feeds the brief from scratch, so it happens only while the attempt's log segment holds no model step (`streamGenerateContent`, the same step mark `bin/gemini-weather` counts): once one exists the watcher stands down and agy's own retry keeps the attempt; if that attempt then fails with the phrase in its segment the family is marked, stderr says `geminib: capacity <family> after N steps, not relaunched` and the run exits non-zero. A signal to the wrapper (TERM/INT) is forwarded to agy's group, waited on for up to 3 s (under worker-run's 5 s KILL escalation) and the group is KILLed on exit if still alive. Before any step, first sight ends that attempt (TERM then KILL to agy's own process group), touches the marker `${GEMINIB_CACHE_DIR:-~/.cache/geminib}/capacity/<family>` (the research sandbox allows writes to that cache) and relaunches the identical command one family down the chain `gemini-3.8-flash gemini-3.7-flash gemini-3.6-flash` at the SAME effort suffix; the last family gets no watcher, so agy's own 503 retries run and its status is the run's. A family whose marker is younger than `GEMINIB_CAPACITY_HOLD_S` (default 600) is skipped before the first launch and tried again once the hold lapses — the whole of “back up when it is served again”. `GEMINIB_CAPACITY_FALLBACK=0` disables it; nothing else is configurable. Stderr contract, the only way a caller learns what was served: `geminib: capacity fallback <old-slug> -> <new-slug>` per 503, `geminib: capacity hold <old-slug> -> <new-slug>` per hold skip, and `geminib: model <slug>` last, on every launch that named a model | `bin/geminib` (`launch_agy`, `capacity_*`); consumers `bin/worker-run` gemini leg, `share/light-research.sh`, `bin/gemini-image`, and the review-bench cells that call `geminib profile <acct>` |
| co | Gemini model weather cache | ONE file, `${GEMINI_WEATHER_DIR:-~/.cache/gemini-weather}/latest.json`, written only by `bin/gemini-weather`: `valid_until` (epoch; readers treat the document as absent from then on) and `window_min` and `families[]` of `{family, label, short, state, cut}` with `state` one of `ok`, `slow`, `starved`, `no-data`. Every threshold lives in `bin/gemini-weather` alone — `HEALTHY_STEP_S = 3`, `SLOW_FACTOR = 3` (slow = median step at or above 9 s), starved = a 503 line or a geminib hold marker (row `cn`) inside the window, default `DEFAULT_WINDOW_MIN = 60` — so no reader re-derives a state. CUT follows RUNS and counts panel-killed `rater_runs[]` with `finished_at` inside the window from `<WORKER_STATS_DIR>/benches/*/meta.json`, defaulting to `<CLAUDEB_DIR>/worker-stats`; family uses `served_model`, otherwise `agy-flash38`/`agy-flash37`/`agy-flash36`/`agy-pro`. Positive counts render `cut ×N` after s/step and never affect states. The cache has NO menu reader: `bin/gemini-weather` is a terminal tool, the menu launches it nowhere, and the weather table's `theirs` column is `bin/llm-weather`'s own count for every vendor (row `cq`) — two collectors over two windows on one row is what that column used to be | `bin/gemini-weather` `write_cache`/`collect`; readers `bin/gemini-probe` (`state_of`, `log_summary`) and `bin/llm-weather` (`family_of`, `geminib_cache_dir`, as a sibling module — never this cache); prose `docs/DIAGNOSTICS.md` |
| cp | Gemini review cell lifetime | A Gemini cell ends at the later of the slowest non-Gemini cell finishing plus `GEMINI_CELL_GRACE_S = 60` and its own launch plus `REVIEW_TIERS[tier]["budget_min"] * 60`; tierless panels use T0, weather reruns retain the panel tier, and Gemini-only panels keep the vendor timeout | `../review-bench/share/rbench/launch.py` `GeminiLifetime`; `../review-bench/docs/review-contract.md`; `tests/test_consistency.sh` |
| cq | LLM leg weather cache | ONE file, `${LLM_WEATHER_DIR:-~/.cache/llm-weather}/latest.json`, written atomically only by `bin/llm-weather`: `as_of` (epoch), `window_h` (default 24), `trend_d` (7), `worst` (`<model> <top class> ×<count>` for the top two models with bad legs joined by ` · `, `""` when clean) and `models[]` of `{model, surfaces, legs, bad, classes, trend, origins, incidents}` — `surfaces` ⊆ `review`,`worker`; `classes` only non-zero counts of `walled`, `escaped`, `stalled`, `cap`, `failed`, `slow`; `trend` `up`, `down` or `""`; `incidents[]` newest first, at most 15, of `{age_s, age, surface, project, class, detail, origin, ref}` with `ref` the only id-like field. The failure vocabulary and whose failure each word is are copied from review-bench `share/rbench/panel.py` (`FAILURE_REASONS`, `FAILURE_ORIGIN`) word for word: `origin` is `ours` (review-bench's own mechanics) or `theirs` (the provider's weather) on a `failed` leg and `""` on every other class — `cap` stays a class of its own and never carries one — and `origins` counts those words per model beside `classes`, never instead of them. Models sort bad desc, legs desc, name. The menu renders `theirs` as its own column and subtracts it from `failed`, and a model gets a row only with `bad > 0`; it mixes in no second collector. EVERY surface takes the same pipeline: a failed worker leg runs the same vocabulary over its `result` and the last 8 KB of its `err`, so it carries an origin exactly as a review leg does, and keeps `exit N` with no origin only when nothing matches; a walled leg of either surface carries none. The vocabulary covers geminib's two capacity spellings (`No capacity available for model`, `geminib: capacity `) under the word `capacity`, origin `theirs`. A review leg's model is its `served_model` resolved through geminib's own family list (`${GEMINIB_CACHE_DIR:-~/.cache/geminib}/models.json`, row `cr`) — a Flash version this file never heard of needs no code change — and the rater's own name without the cache. Every threshold (slow = 2× the 7 d median of the CLEAN legs of the same surface and model with ≥ 5 of them — a capped or failed leg's duration is the kill's, not the model's, and would hide slowness behind the caps — falling back to every leg of the group with a duration when the clean legs are fewer than that, since no median at all hides a model slow on every leg it finishes, and only a clean leg is ever marked slow; trend at 2×/0.5× with ≥ 3 bad legs) lives in `bin/llm-weather` alone, which resolves the bench store as `${WORKER_STATS_DIR:-${CLAUDEB_DIR:-~/.claude-profiles/.claudeb}/worker-stats}` like every other reader | `bin/llm-weather` `collect`/`write_cache`; guard `tests/test_llm_weather.sh`; prose `docs/DIAGNOSTICS.md` |
| cr | Gemini model families | ONE list: `geminib families [--json] [--refresh]` prints `family<TAB>slug<TAB>agy-id-prefix<TAB>label` newest version first (`--json`: objects `{family, slug, agy_prefix, label}`). Source `${GEMINIB_CACHE_DIR:-~/.cache/geminib}/models.json` `{fetched_at, attempted_at, families[]}`, refreshed from `agy models` when it holds no families, when both stamps are older than 24 h (`families_ttl_s=86400`) or on `--refresh`; a failed or empty refresh stamps `attempted_at`, keeps the old families and says `geminib: agy models failed; using the families cached at <UTC>` on stderr; with no cached families the built-in last-known list `families_builtin` answers — the ONLY family literal in the production files of llm-legs, review-bench and claude-setup (experiment-tagged lines and review-bench's legacy rater renames aside). The parse rule lives in geminib alone: an `agy models` line `gemini-<M>.<N>-(flash\|pro)[-high\|-medium\|-low]<TAB><label>` gives family = id without effort, label = label without ` (<Effort>)`, slug `flash<M><N>` or `pro` (the newest Pro owns it). Effort suffixes are the callers'. Shell readers call `geminib families`; python readers read the JSON and run `geminib families --json` once when it holds none; the legacy model name `flash` means the newest Flash on the list | `bin/geminib` `families_*`, `capacity_flash_chain`; readers `share/worker-model.sh` `worker_model_gemini_families`/`worker_model_gemini_family`, `bin/worker-run`, `bin/worker-spawn-hook.sh`, `bin/worker-tag-hook.sh`, `bin/gemini-image`, `ask_gemini.sh`, `bin/gemini-weather` `geminib_families`, `bin/gemini-probe`, `../review-bench/share/rbench/catalog.py` `gemini_families`; `tests/test_geminib_families.sh`; literal and format pins in `tests/test_consistency.sh` |
| cs | Gemini review Flash pin | ONE file, `${GEMINIB_CACHE_DIR:-~/.cache/geminib}/review-flash`, holding one Flash slug of the family list (row `cr`) and written only by `geminib review-flash [<slug>\|--clear]` — a slug the list does not print as a Flash family is refused with one stderr line, the write is atomic, and `geminib review-flash` with no argument prints `<slug><TAB>pinned\|default`, the file's slug when it has one and the newest Flash otherwise. Read by exactly two consumers: `../review-bench/share/rbench/catalog.py`, where it picks `GEMINI_FLASH_CELL`, the Flash of T0 and T1 roster cells only (`FLASH_PIN_TIERS`) — T2 and T3 cells and `GEMINI_VERIFIER` always run the newest Flash — and, when it pins a family the list does not hold, keeps that cell on the newest Flash and refuses in `flash_cell` for a T0/T1 run, naming the file, so only a run staffing a pinned Gemini cell stops while every importer of rbench (`review-debt`, the report, the hooks) carries on; and the Hammerspoon `LLM doctor` submenu's `review flash T0–T1: <label>[ · pinned]` row, which reads the file and the Flash families of geminib's `models.json` cache (row `cr`) as files — never a shell-out, a menu build runs on every open — lists them plus `newest (default)`, writes only through `geminib review-flash`, and renders `review flash T0–T1: unavailable` without the cache. Absent or empty means the newest Flash. The workers and the research leg never read it: they stay on the first gemini row of `worker_model_table` | `bin/geminib` `review_flash`; `hammerspoon/llm-limits.lua` `reviewFlash`/`reviewFlashChoices`/`M.setReviewFlash`; `../review-bench/share/rbench/catalog.py` `review_flash_pin`; `../review-bench/docs/review-contract.md`; `tests/test_geminib_families.sh`, `tests/llm_limits_renderer_harness.lua`, `../review-bench/tests/test_compositions.sh`; pins in `tests/test_consistency.sh` |
| ct | Web search capability | ONE table, `share/web-search.sh` `web_search_table`, giving per vendor the argv that turns live web search ON and the argv that turns it OFF: `-` where the vendor's CLI already stands in that state (claudeb, gemini and grok search by default, so only codex has to be asked with `-c web_search=live`) and `!` where the vendor has no switch at all. A cell is split into argv words, so a flag taking a LIST takes it comma-joined as ask_claude.sh spells it — `A,B` and never `A B`, whose second word would reach `claude -p` as the prompt. A state the vendor cannot reach is REFUSED at launch when it was asked for, naming the vendors that can reach it in that direction (an unreachable ON, and an unreachable OFF such as gemini's); only a default nobody asked for launches anyway, and `web_search` in `meta.json` then records the state the run really got. Every entry point resolves through the table and none spells a vendor flag of its own: `worker-run start <vendor>` and `start light` stay opt-in (`--web-search`, or a `WEB:` header line of the brief — key and state case-insensitive, read only from the contiguous header block at the very top, and a `WEB:` line the parse cannot reach there is refused rather than dropped), `--no-web-search` asks for the other direction, a flag contradicting the header refuses the launch, `--role research` is on by default and `WEB: off` turns it off, and each resume supervisor rebuilds the same arguments from `meta.json` — where an absent `web_search` key resolves to the CLI's own default state rather than to off. `worker-run start` and `worker-run report` both name the resolved state as `WEB: on\|off`, `bin/light-research` prints it beside `ACCOUNT:` and for a run still going, resolves the state ONCE from the caller's prompt header and passes it as a flag instead of re-embedding the header, and its citation checker counts an `http(s)://` citation (scheme in any case) as a `LINKS:` count instead of failing it as an unresolvable path | `share/web-search.sh`; `bin/worker-run` `start_run`, `supervise_claudeb`, `supervise_codex`, `supervise_gemini`, `supervise_grok`; `share/light-research.sh` `supervise_gemini_research`, `research_citation_check` and `research_citation_body`; `bin/light-research` `collect_run`/`land_answer`/`units_finish`; `share/worker-policy.md`; guards `tests/test_worker_run.sh`, `tests/test_light_research.sh`, pins in `tests/test_consistency.sh` |
| cu | Grok model list | ONE list: `grokb models [--json] [--refresh|--cached]` prints `slug<TAB>default(yes\|no)<TAB>label` in the CLI's own order, which is newest first (`--json`: objects `{slug, default, label}`; the label is the slug as printed, since grok prettifies nothing). Source `${GROKB_CACHE_DIR:-~/.cache/grokb}/models.json` `{fetched_at, attempted_at, default, models[]}`, refreshed by running `grok models` inside each signed-in profile `grokb list` shows, in that order, until one answers with a parseable list — the list is account-independent, and an unauthenticated profile answers with an older default — when the cache holds no models, when both stamps are older than 24 h (`models_ttl_s=86400`) or on `--refresh`; only when EVERY signed-in profile fails is `attempted_at` stamped, and then the old models stand and stderr says `grokb: grok models failed; using the models cached at <UTC>`; with no cached models the built-in last-known list `models_builtin` answers and says so, `grokb: grok models failed; no cache; using the built-in list`. Neither `--cached` nor `GROKB_MODELS_NO_FETCH=1` ever asks the CLI — cache, else the built-in list — and both hooks export the variable, because an expired cache on the interactive path would put a 30 s bounded `grok models` in front of a keystroke and every concurrent hook would start its own; `worker-run`'s launch path keeps fetching. The parse rule lives in grokb alone: inside the `Available models:` block a line `* <slug>` or `- <slug>` is a model and `(default)`, or the `Default model:` line when the block marks none, is the default. It is NOT the quota cache `~/.llm-limits-grok.json`, and nothing here reads the claudeb store. Consumers: the table's grok rows (row `bq`), emitted behind the `auto` row that stays the vendor default, and `worker_model_grok_label` — the ONE rule that collapses `auto` and the marked default to the vendor word `grok` while every other slug labels itself, read by the launch line and both hooks — and `worker_model_grok_launch_model`, the ONE resolution of the slug a `chat-pin grok-fast` chat launches (the `-fast` sibling replaces the marked default and `auto` alone, on a WORKERS leg alone), read by `worker-run start` and by the spawn hook's task row so the row can never name a model the run does not use. `../review-bench/share/rbench/catalog.py` `grok_models`/`grok_cell_token` reads the same cache and shells out to `grokb models --json` only when it holds none, naming every review cell off a slug (`grok-4.7` → `grok47`) and staffing the marked default (review-bench row `bl`) | `bin/grokb` `models_*`/`show_models`; readers `share/worker-model.sh` `worker_model_grok_models`/`worker_model_grok_default`/`worker_model_grok_label`, `bin/worker-run`, `bin/worker-tag-hook.sh`, `bin/worker-spawn-hook.sh`, `../review-bench/share/rbench/catalog.py` `grok_models`; `bin/worker-run`, `bin/worker-spawn-hook.sh`; `tests/test_grokb_models.sh`; fixture `tests/fixtures/grokb-models.{json,sh}`, which every suite reaching the list sources so no test can fetch; literal and format pins in `tests/test_consistency.sh` |
| cv | Codex model list and the family word | ONE list: `codexb models [--json] [--all] [--family <word>]` prints `slug<TAB>family<TAB>version<TAB>label<TAB>default(y\|n)<TAB>efforts` from the newest `models_cache.json` (by its `fetched_at`) across `~/.codex` and every codexb profile home — the file the Codex CLI itself refreshes; codexb never fetches. A slug is `gpt-<version>-<family>`; a slug with no word after the version (`gpt-5.5`) or none at all is its own family (`worker_model_codex_split`, the one rule). Families are ordered by the priority of their newest member, newest version first inside one; the default is the lowest-priority listed slug; hidden slugs appear only under `--all`; `--json` adds `priority`, `visibility`, `source` (`cache` with `cache` path, or `builtin`). A missing or unparsable cache falls back to `models_builtin` and says `codexb: no readable models_cache.json; using the built-in list`. `--family <word>` prints the newest LISTED slug of that family and gives back any slug it knows; an unknown word exits 1 naming the families. The worker table keys codex on the family word (`astra`, `sol`, row `bq`); a full slug is allowed through its family and launches unchanged — a deliberate pin. Every codex launch resolves the word through `worker_model_codex_slug`: `worker-run` records the word in meta.json `.model` and the slug in `.model_id` and the supervisor launches `.model_id`; `codexb profile` defaults to the resolved table default; `bin/claudegpt` resolves `FAMILIES`. Surfaces (task rows, statusline, worker-pick, menu, worker-stats) print the word | `bin/codexb` `models_*`/`show_models`; `share/worker-model.sh` `worker_model_codex_split`/`worker_model_codex_family`/`worker_model_codex_slug`/`worker_model_row_key`; `bin/worker-run`, `bin/claudegpt`; `../review-bench/share/rbench/catalog.py` `CODEX_CELLS`; `tests/test_codexb_models.sh`; fixture `tests/fixtures/codexb-models.{json,sh}` (`CODEXB_MODELS_CACHE`, whose astra is `gpt-6.1-astra`), sourced by every suite that launches codex; pins in `tests/test_consistency.sh` |
