# Shared invariants

Some values are duplicated across scripts written in different languages (bash,
jq, Lua, prose) because each implementation is independent — no shared library
spans `bin/claudeb`, `bin/statusline.sh`, `bin/worker-pick`, `llm-limits.sh`, the
Hammerspoon renderer, and the docs. When such a value drifts in one place, the
system misbehaves silently. `tests/test_consistency.sh` re-extracts each value
from the live files and fails if any site disagrees with the canonical value
below. This table is the canonical source; the test names it in every failure.

| # | Invariant | Canonical value | Implementation sites |
|---|---|---|---|
| a | Staleness / dim thresholds | five_hour `1800`s, weekly `21600`s, fable `21600`s | the numbers live once in `share/limits-view.sh` (`LIMITS_STALE_FIVE_HOUR`/`_WEEKLY`/`_FABLE`); `bin/claudeb` and `llm-limits.sh` pass them into jq as `$thr5`/`$thrw`/`$thrf` and must carry no local literal; `bin/statusline.sh` dim logic (`now - h5_as_of > 1800`, `now - wk_as_of > 21600`, `now - limits_mtime > 21600`); `docs/statusline-contract.md` usage-table prose (`as_of` >1800s / >21600s, mtime >21600s); `hammerspoon/llm-limits.lua` consumes the collector's `.stale` flag rather than a local threshold (`bucket.stale == true`) — it must stay flag-driven, never hardcode a number |
| b | Keychain service formula | `Claude Code-credentials-` + first 8 hex of `shasum -a 256` of the profile path | `bin/claudeb` `keychain_service`; `llm-limits.sh` `claude_subscription_type`; `bin/claude-session-driver` `keychain_service` (Python `hashlib.sha256(...).hexdigest()[:8]`, same prefix) |
| c | worker-pick cache line format | `cx<mark><acct>·<model>·<eff> cb<~|@><acct>·<model>·<eff> gx<mark><acct>·<model>·<eff>` (producer printf `cx%s%s·<model>·%s %s·%s·%s gx%s%s·%s·%s`; Codex model comes from `~/.codex/config.toml`, and each vendor account is dynamic) | producer `bin/worker-pick` `write_cache`; consumer `bin/statusline.sh` reads `worker-pick.line.<acct>` verbatim |
| d | Weather HTTP classes | `000`, `429`, `5xx` (`5[0-9][0-9]`) are weather — they never write an auth verdict (`expired`/`revoked`/`auth-failed`) | `bin/claudeb` `probe_weather_failed` case pattern; `bin/claudeb` `oauth_refresh` outcome classification |
| e | OAuth token-endpoint 429 cooldown | `900 * 2^(max(1, strikes)-1)`, capped at `14400`s (4h); a longer vendor `Retry-After` wins | `bin/claudeb` `oauth_backoff_until`; `llm-limits.sh` `claude_stale_cause` |
| f | Token-freeze file semantics | file at `<store>/token-freeze` present = freeze active; a numeric `until` in the past → inactive (auto-resume); a missing / non-numeric `until` → active while the file exists. The active freeze blocks robot paths only; `CLAUDEB_WARM_USER_EXPLICIT=true` exempts the full user invocation and marks its journal entries with `"user":true` | `bin/claudeb` `token_freeze_active` plus user guards and `token_journal`; `llm-limits.sh` `token_freeze_active_at` plus the user-explicit stale-cause guard |
| g | Codex/Gemini base-profile priority | literal profile `main` never outranks a usable non-main account of the same vendor that spends no more quota: in `worker-pick` it is the tiebreak after the spending bucket and the five-hour reading (`docs/routing-contract.md` rule 2), in `codexb` it sorts last outright. Display-only account lines sort the selected account first, then the remaining candidates, then pool members that cannot serve, then everything unreachable, preserving selection order within each band | `bin/worker-pick` shared `rank` and `display_band` definitions; `bin/codexb` `pick_account` ranking; `share/worker-policy.md` vendor-fit prose |
| h | Antigravity review cell invocation mapping | Every cell omits `--effort`. `agy-pro-low` → `--model gemini-3.1-pro-low`; `agy-pro-high` → `--model "Gemini 3.1 Pro (High)"` because v1.1.9 still serves the high slug as Flash; `agy-flash37-<effort>` → `--model gemini-3.7-flash-<effort>`; `agy-flash36-<effort>` → `--model gemini-3.6-flash-<effort>`; `agy-flash35-<effort>` → `--model gemini-3.5-flash-<effort>`. A resolved label that disagrees with the requested cell is recorded as `model_mismatch` and warned about; every Antigravity cell is a `-skill` cell — diff-only agy review is refused in `parse_rater` | `bin/review-bench` `AGY_EFFORTS`, `AGY_MODEL_IDS`, and `agy_model_id`; `bin/worker-run` knob-to-agy model case for worker-supported models; `docs/DIAGNOSTICS.md` review-bench system-map entry |
| i | Gemini worker knobs | Required defaults are `gemini_model=pro`, `gemini_effort=high`; valid pairs are pro high/low, flash36 high/medium/low, and flash35 high/medium/low (bare `flash` aliases `flash36`). Invocation follows invariant h: effort is encoded in the versioned slug except the `pro` high label workaround, and no pair passes `--effort` | accepted knobs/default-writing semantics in `~/.claude/commands/worker.md`; routing defaults in `bin/worker-pick`; statusline reads and renders both knobs independently; policy pointer in `share/worker-policy.md` |
| j | Worker spawn pressure gate | `worker-pick --account <vendor>` owns selection: its selected account is allowed below the hard `100`% wall and warned from `85`%; exit `3` and an effective `100`% account are blocked. `95`% is the protective block only when worker-pick is unavailable or fails, and that fallback is always reported | shared `~/.claude/hooks/worker-limit-gate.sh` for claudeb, Codex, and Gemini worker spawns; `bin/worker-pick` account query; `~/.claude/settings.json` has one Agent registration |
| k | Worker account resolution | resolution lives only in `worker-run`: an explicit `--account` (relays forward the brief's `ACCOUNT: <name>` line) wins and never reroutes; otherwise `worker-pick --account claudeb\|codex\|gemini`; exit `3` aborts the launch with `OUTCOME: <VENDOR>_USAGE_LIMIT`, or with `OUTCOME: <VENDOR>_UNAVAILABLE` when the picker reports an empty worker pool rather than a wall; an account outside the pool is refused outright unless it is the vendor pin (row q); only an unavailable/failed query falls back to the vendor pin (recorded as pinned), then `main` for Codex/Gemini, and the resolved account is visible in the printed `TAG:` line. On a mid-run usage wall an unpinned run re-resolves with the burnt accounts excluded and relaunches, reporting each hop as a `REROUTE:` line and rewriting the tag; `OUTCOME: <VENDOR>_USAGE_LIMIT` means the whole pool walled. Claude refuses when its fallback pin is empty because `claudeb` requires an explicit profile | `~/.claude/commands/worker.md` pin toggles; `bin/worker-pick` pin routing; `bin/worker-spawn-hook.sh` pending tag; `bin/worker-run` account resolution; `~/.claude/agents/claudeb-worker.md`, `codex-worker.md`, and `gemini-worker.md` `--account` forwarding |
| l | Gemini quota group matching | case-insensitive group label contains `gemini`; exact display-name equality is forbidden | `llm-limits.sh` raw group collection/validation; `bin/worker-pick` normalized account eligibility |
| m | Gemini profile discovery and HOME mapping | account `main` maps to the process base HOME; named accounts are sorted profile directories and map to `<profiles-dir>/<name>` | `share/gemini-accounts.sh`, sourced by `bin/geminib` and `llm-limits.sh` |
| n | Weekly bucket provenance | rate-limit response headers carry NO weekly percentage, so no writer may stamp `origin: "headers"` on `seven_day`; every reader treats such a bucket as unknown (rendered `?`/`-`, never a number) | writers: `bin/claudeb` `merge_headers` (5h only), `bin/statusline.sh` `rl_merge` stamps `origin: "session"` and drops a cached header-origin week; readers: `bin/claudeb` `account_data`, `llm-limits.sh` both claudeb-store and single-account snapshot reads, `bin/statusline.sh` render gate, `bin/worker-pick` cache normalization |
| o | Claude rotation usability presence | `llm-limits.sh` always emits boolean `rotation.usable.general`, `rotation.usable.fable`, and `blocked == (rotation.usable.general \| not)` for every emitted Claude store account, including incomplete, disabled, and unusable snapshots; consumers may rely on those keys being present. `bin/worker-pick` defaults a missing rotation key to `true` only for compatibility with pre-change caches. Two documented weak spots, both fail-open on `fable` and both bounded by the worker's own `CLAUDEB_USAGE_LIMIT` reroute: the Pro-plan gate needs `claude_subscription_type`, which returns an empty string when the keychain is locked or absent (unknown plan is then treated as non-Pro). | producer: `llm-limits.sh` Claude account projection; permissive fallback: `bin/worker-pick` |
| p | Reserved profile names | the same set in both guards, so a name that cannot be a profile cannot be offered as a switch target either: `help add enable disable remove use accounts status st profile p run warm token-upkeep`, plus `main` and any name starting with `.` or containing `/` | `bin/claudeb` `reserved_name` (with `main` checked separately by `add_account`); `bin/claude-chat-switch` `is_reserved` |
| q | Worker-pool membership | one mechanism for all three vendors, so a toggle means the same thing everywhere: the excluded names live one per line in `<profiles-dir>/.<tool>/disabled` (`.claudeb`, `.codexb`, `.geminib`), an absent file means everything is in, and the dot-directory keeps the file outside the profile enumerators' `*` glob. Read and written only through `share/worker-pool.sh`, which also owns the per-vendor directory formula (`worker_pool_dir`, used by the three CLIs, `worker-run`, `codex-image` and `llm-limits.sh`; Claude reaches it as its store root `CLAUDEB_DIR`) and the wall itself (`worker_pool_refuse_headless`). Exclusion IS unreachability for every headless run — `claudeb … -p`, `codexb <name> exec`, `geminib profile <name> --print`, and any `worker-run start` naming the account — however the account is named; the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`) is the one override. An interactive launch is the user, never a worker, and is never gated, which is why every member including the last one may go out: an empty pool means no worker may run, and `worker-pick` says so in its own words (`every <vendor> account is out of the worker pool`) so `worker-run` reports `OUTCOME: <VENDOR>_UNAVAILABLE` instead of a usage limit. One rule the helper itself enforces: a file that exists but cannot be read means "everything is out" for readers and is never rewritten by writers | `share/worker-pool.sh`; `bin/claudeb`, `bin/codexb`, `bin/geminib` (`is_disabled`/`set_disabled`, `enable`/`disable`); `llm-limits.sh` (`enabled` per account, `ROT off`); `bin/worker-run` (the wall on `--account` and the empty-pool outcome); `bin/codex-image` (the same wall, since image runs launch `codex` directly); `bin/worker-pick` (excluded from every vendor's eligible set but not its pin resolution; per-vendor empty-pool reason); `hammerspoon/llm-limits.lua` ("In worker pool" checkbox on every account row, sole rows included) |
| r | Claude leg selection announcement | the stderr literal `claudeb: worker-pick selected <name>` is a parsed contract, not free prose: claudeb prints it on every routed headless run, `ask_claude.sh` seds it into the audit row's `account` field, and the legs test stub reproduces it byte-identically | `bin/claudeb` headless routing announcement; `ask_claude.sh` audit account extraction; `tests/test_legs_routing.sh` claudeb stub |
| s | User-entry refresh classification | `needs_user_entry:true` is the structured marker for a refresh outcome that requires human account entry; a vendor error carries it only when every `"; "`-joined entry has that class, and the affected account carries it independently. Every composite cause, including multiple Claude auth failures, uses `"; "` between entries; individual stale-cause text must never contain that separator | producer and classifier `llm-limits.sh`; consumer `hammerspoon/llm-limits.lua`; guards `tests/test_llm_limits.sh`, `tests/e2e_surfaces.sh`, and `tests/test_consistency.sh` |
| t | Review receipt path and schema | `<state_dir>/receipts/<repoName>__<repoHash>.json`, directly under `receipts`, where `repoName` is the basename returned by the repository identity rule and `repoHash` is the first 8 lowercase hex characters of SHA-1 over the canonical full repository path; one JSON object with string fields `repo`, `tree`, `commit`, `run_id`, and `ts`, non-negative integer `errored`, an optional positive integer `panel` naming how many cells the run launched (absent on receipts written before it existed), and `tree` is the reviewed commit's Git tree object. A lens run writes a sibling `<repoName>__<repoHash>__lens-<slug>.json` with an extra `lens` field and never the plain name, so a review run by a methodology the tool did not write can never be read as the repository's own. A scoped worktree run — one narrowed to named paths with `--paths` — writes a sibling `<repoName>__<repoHash>__scope-<slug>.json` with an extra `scope` array field, where `<slug>` is the first 8 lowercase hex characters of SHA-1 over its normalized paths joined with NUL; it likewise never writes or advances the plain name, so `receipt` cannot read a review of part of the tree as a review of the repository. The record is keyed on the repository and a chat is not, so no surface outside `bin/review-bench` reads it: what a chat owes is answered per session by the gate's verdict (row `ah`), and a badge rendered off this file would answer for reviews other chats ran | writer and reader `bin/review-bench` |
| u | Late review threshold | `3` × the base cell's recorded median duration, with a `120`s (`120000`ms) floor | writer/reporting `bin/review-bench`; reader/coloring `bin/statusline.sh`; live-progress prose `docs/statusline-contract.md` |
| v | Account data age | `as_of` is the oldest valid timestamp among `five_hour`, `weekly`, and `fable` windows whose `used_pct` is numeric; `stale_seconds` is its non-negative age. Absent and null-valued windows do not participate, and an account with no data-carrying window has neither field. Vendor aggregate age applies the same rule to its hoisted windows. Metadata with separate freshness retains its own timestamp: Codex reset credits use their capture time. | primary producer `llm-limits.sh` `data_as_of`/`set_data_age`; independent routing freshness `bin/worker-pick` `account_as_of`; consumers `llm-limits.sh` table/plain renderers and `hammerspoon/llm-limits.lua` |
| w | Owner-only review panels | tier `T3` and any tier's `--max` run only once Egor has named one himself; nothing in the tool ever proposes a panel, so a passing mention can never become what runs. The permission is an empty marker file `<state_dir>/review-grants/<panel>`, one per panel (`t3`, `max`), whose mtime is the whole state: a marker younger than `1800`s unblocks, nothing is spent, and a stale or absent one blocks. Both sides refuse independently and fail open on anything they cannot read. The tool side's terminal exemption is Egor's own shell only: a terminal on both ends AND no agent runtime in the environment (`CLAUDECODE`) | `bin/review-bench` `OWNER_TIERS`/`OWNER_GRANT_DIR`/`OWNER_GRANT_TTL_S`, `owner_named`, `owner_at_keyboard`, `guard_tier_owner`; `bin/review-owner-gate.sh` `prompt` (writer) and `bash` (reader) modes; registrations in `~/.claude/settings.json`; phrase mapping in `~/.claude/docs/review-tiers.md` |
| x | Claude account existence | an account exists when ANY claudeb store knows it: `tokens/<name>`, `limits/<name>.json`, or the `~/.claude-profiles/<name>` profile directory; `main` and `-` are reserved non-accounts on every surface. `bin/claudeb` `account_names` is the single enumerator (status, enable/disable, warm, probes, and pin validation all consult it); the limits menu lists strictly from the vendor cache, which those probes populate for the same set; account birth (add, first-launch profile create) and death (remove) fire the announce hooks so the cache follows without a manual refresh | `bin/claudeb` `account_names`/`account_exists` and the `main`/`-` filter; `llm-limits.sh` claude-section `main`/`-` skip; `share/account-announce.sh` `announce_account_added`/`announce_account_removed` wired into `bin/claudeb`, `bin/codexb`, `bin/geminib` |
| y | One limits view for every surface | bucket display semantics are defined once: a bucket is `expired` when its reset epoch is real (≥ `31536000`) and ≤ now, and a reset over `86400`s past is dropped outright — the collector nulls the field and `limits_reset_text` prints `-` — because the window it named has rolled over unseen and every surface reads the field as a schedule; the drop is read AFTER expiry, so a dead reading never loses its `expired` verdict by losing its date, `stale` per row a's thresholds (or auth expired / cached origin), and its `effective` value is `0` when expired, else the raw pct; a text cell shows the EFFECTIVE value with `~` (stale) and `!` (expired) markers, a bare `-` when unmeasured; reset cells and age text share one formatter; per-account age is the oldest measured bucket (row v). Every surface renders these semantics from the same defs or from fields the collector computed with them — no surface re-derives its own. Any probe/warm that rewrites snapshots announces a passive collect (`announce_limits_probed`) so the merged cache and menu move with the snapshots; the collector suppresses announces in its own children (`LLM_LIMITS_ANNOUNCE_SUPPRESS`) to avoid recursion and double writes; a pressed `r` in the status TUI carries `CLAUDEB_WARM_USER_EXPLICIT=true` so the token-freeze experiment cannot silently eat a user-explicit refresh | definitions `share/limits-view.sh` (`LIMITS_VIEW_JQ`: `limits_bucket_expired`/`limits_reset_ancient`/`limits_bucket_stale`/`limits_effective_pct`/`limits_pct_text`/`limits_reset_text`/`limits_age_text`); consumers `bin/claudeb` `account_data`+renderers and `llm-limits.sh` `meta`/`mark`/table/plain; `hammerspoon/llm-limits.lua` renders the collector's `effective_pct`/`stale`/`expired` fields only; live parity guard `tests/e2e_surfaces.sh` store-consistency check; announce plumbing `share/account-announce.sh` + `bin/claudeb` `probe_accounts`/warm dispatch + `llm-limits.sh` child env. `bin/statusline.sh` still carries its own dim thresholds (row a) and `used_pct` rendering — folding it in is the recorded follow-up, blocked only on the parallel session's uncommitted statusline work |
| z | Lens registry location | `*.md` directly under `lenses/` beside `bin/`, which `REVIEW_BENCH_LENS_DIR` overrides whole (tests only); a lens is identified by its frontmatter `name` slug, never by its filename | `bin/review-bench` `LENS_DIR`/`lens_dir()`; registration prose in the `review-bench` skill (`~/.claude/skills/review-bench/SKILL.md`) |
| aa | Hammerspoon launchd agent identity | label `com.egor.hammerspoon`, wrapper `/Users/egorloy/.local/libexec/hammerspoon`, log `/Users/egorloy/Library/Logs/com.egor.hammerspoon.log`; renaming any of these must move every site together or the env guard's ownership probe and log cap silently miss | `launchd/com.egor.hammerspoon.plist` `Label`/`ProgramArguments`/`StandardOutPath`/`StandardErrorPath`; `hammerspoon/config/env_guard.lua` `launchctl print` probe and log-cap path; prose `docs/DIAGNOSTICS.md` |
| ab | Review report frame | every report block a hook or the bench emits is its word centered in `=` to a total line width of `50` with one space each side, closed by a bare rule of `50` `=`; the generic shape consumers locate a block by is `^=+ [a-z]+ =+$` / `^={10,}$`, and the review consumers narrow the header to its own word (`^=+ review =+$`) so a commit or push report framed identically is never taken for one — the width and those single spaces are what all of it rests on | emitters: `bin/review-bench` `REPORT_FRAME_WIDTH`/`REPORT_FRAME_WORD`/`report_frame_header()` and `../claude-setup/hooks/commit-report.sh` `FRAME_WIDTH`/`frame_header`/`rule`; consumers, both of which PRINT the block they locate rather than asking the model for it: `../claude-setup/hooks/review-report-nudge.sh` and its Stop net `../claude-setup/hooks/review-report-delivery-gate.sh`, each keyed on `HEADER_SPAN_RE`/`FOOTER_SPAN_RE` |
| ae | Account pin ownership | the vendor pin (`claudeb_profile`, `codex_profile`, `gemini_profile`) is Egor's own override above the pool, and a session moves it only after he names it: naming an account to work on is not naming the pin. The permission is an empty marker file `<state_dir>/pin-grants/pin` whose mtime is the whole state — younger than `1800`s unblocks, nothing is spent, a stale or absent one blocks — and it opens BOTH directions, since a pin he set is not a session's to clear either. Three doors refuse independently and fail open: the command door inside `worker_model_pin_account`, which every spelling of `claudeb\|codexb\|geminib use` reaches, and — because a door on one way to the file is a door around the other — an `Edit\|Write` door and a `Bash` door for a session writing `~/.claude/worker-model` by hand. What the file doors deny is the PIN, not the file: the same file carries `worker=`, `*_model=` and `*_effort=`, which `/worker` rewrites directly, so a write leaving every `*_profile=` line as it found it passes ungated. Every door compares the resolved file, never the path text — `//`, a `..` hop and `~` all name the one pin. The command door applies only to a session (`CLAUDECODE` in the environment) writing the REAL file — the menubar shells out from Hammerspoon carrying neither, and a fixture named through `WORKER_PICK_CONFIG_FILE` is a test's own. Reading the pin is never gated. The one write no grant gates is the account ending its own pin: `worker-pick` removes a pin whose account hit its USAGE wall (`worker_model_clear_walled_pin`, the exact name it measured and only on data it calls fresh), because nobody chose that and a pin exists to be spent — every other lapse, dead auth included, leaves it standing for the window to reset | `share/worker-model.sh` `WORKER_MODEL_PIN_TTL_MIN`/`worker_model_pin_grant`/`worker_model_canonical_path`/`worker_model_pin_allowed` and its refusal in `worker_model_pin_account`; `bin/worker-pin-gate.sh` `prompt` (writer) plus `write` and `bash` (readers); registrations in `~/.claude/settings.json`; guard `tests/test_worker_pin_gate.sh` |
| af | Second-round verdict has one voice | what a triaged round earned — the three dials (`SECOND_REVIEW_P1S=2`, `SECOND_REVIEW_FINDINGS=8`, `WEAK_LINK_P1S=5`) AND the three-way wording, the weak-block verdict included, which sends the fixer to rethink the piece instead of reviewing it a second time — is decided only inside `review-flow-gate.sh`, and every other site asks it through `review-flow-gate.sh` `escalation-verdict <p1> <total>` (exit `0` with the fork, `1` for a closed round). Two numbers and nothing else: a caller handing the gate a repository or a stamp would be asking it to price the shared tree again, which is the whole class of question `docs/review-contract.md` deleted. That mode must answer from argv BEFORE the hook reads its payload, or a caller holding the pipe open hangs on it. Every speaker is handed the answer by `bin/review-bench` `escalation_verdict` and nowhere else: the `round:` row of every `review-bench record` report block, and the Stop gate that refuses to end a turn on an untriaged run. `bin/review-bench` spells the two round-size numbers a second time for one reason only — the waiver lock of row `ap`, which holds them equal — and spells neither `WEAK_LINK_P1S` nor any of the wording; a copy of those anywhere else is the failure this row exists to prevent | `../claude-setup/hooks/review-flow-gate.sh` dials plus the `escalation-verdict` dispatch; `bin/review-bench` `escalation_verdict` and the report `round:` row; `../claude-setup/hooks/review-report-gate.sh` Stop branch; guards `../claude-setup/tests/test_review_flow_gate.sh` and `tests/test_review_bench.sh` |
| ah | The statusline speaks the gate's verdict | one question, three implementations in a row, and a word renamed on any of them goes silent rather than loud. `review-bench debt --repo <top> [--session <sid>] [--paths <p>...]` prints exactly one line and always exits `0`: `none`, `debt <n> mine|other [locked]`, or `timed-out <run-id>`; `--list` prints the debt paths themselves instead of that line. `review-flow-gate.sh verdict <repo> [session]` is the only translator of those into a statusline line — `off`, `bright rev ● <n>`, `dim rev ● <n>` or `loud rev timeout` — and it asks about the whole repository with no paths, since debt outlives the commit that landed it. `loud` belongs to the watchdog alone, so red always means a hung review and nothing else, and `bright` is normal weight (this chat's own debt), never a second red. Like `escalation-verdict` (row `af`) it must answer from argv BEFORE the hook reads its payload, or a caller holding no pipe open hangs on it, and it is read-only by construction — it launches no panel and writes nothing, or a statusline render would spend a review. `bin/statusline.sh` renders that line verbatim, subject only to truncation and to the style word: it computes no class of its own. A second implementation of this question renders a label that disagrees with the gate that acts on it, and one of the two is always lying (Egor, 2026-08-09) | producer `bin/review-bench` `cmd_debt`/`repo_debt`; translator `../claude-setup/hooks/review-flow-gate.sh` `verdict_mode`/`review_debt`; consumer `bin/statusline.sh` `review_gate_verdict`/`review_verdict_line`; segment row `docs/statusline-contract.md`; guards `../claude-setup/tests/test_review_flow_gate.sh`, `tests/test_review_bench.sh` and `tests/test_statusline_hooks.sh` |
| ai | Usage wall record | `${WORKER_STATS_DIR:-${CLAUDEB_DIR:-$HOME/.claude-profiles/.claudeb}/worker-stats}/walls.jsonl` plus, for OpenCode, the served-call stamp `<same dir>/opencode-seen/<account>`. JSONL records share fields `side`, `account`, `bucket`, `detected_at`, `reset_at`, and `window`. `reset_at` and `window` are optional. OpenCode keeps `bucket: "general"` for every quota window, so any active wall blocks that key for every request; `window` is one of `5-hour`, `weekly`, or `monthly` when known. The file holds RAW observations and nothing else: a row says one request was refused, never that the refusal still stands, which is row `al`'s question. It has two writers in two languages — `bin/opencode-go` at the 429 it sees (a `jq` append, so a probe or a worker walls the plan as visibly as a bench cell) and `bin/review-bench` for the `opencode` and `agy` sides — and TWO rules they must spell alike. The first is the ceiling: a `reset_at` the provider stated is capped once, by the writer, to the horizon its own window can reach (`5-hour` 21600s, `weekly` 691200s, `monthly` 2764800s, unnamed 604800s), so a garbled date cannot retire an account until someone notices by hand, and every reader takes the recorded value as final rather than re-clamping it. The second is the grammar that reads the stated horizon out of the refusal body, because the gateway states it in wordings both writers must accept whole: abbreviated units (`hr`/`hrs`, `min`/`mins`, `sec`/`secs`) alongside the spelled-out `second|minute|hour|day|week`, an optional space between number and unit, and COMPOUND values (`Resets in 9hr 30min`, `Resets in 2 hours 30 minutes`) whose components are SUMMED rather than read down to the first one. The unit alternation is longest-first and ends on a word boundary, so both `3 mins` and `3 minutes` are consumed whole rather than `min` matching inside the longer spelling and the remainder of the word cutting the run of components short. A body stating no reset still yields no `reset_at` — none is invented — and the two grammars are pinned against one shared corpus of real 429 bodies, so a wording taught to one side alone fails the guard. NO writer ever retires an OpenCode row by the clock — `bin/review-bench`'s compaction may collapse superseded duplicates into one row per account and window (newest detection, longest horizon) but never drops the account — and no reader may treat a row as spent because its `reset_at` passed: the gateway dates a reset to the day, so a window is routinely shut hours past the horizon it named. The one counter-evidence is a completion the plan SERVED — a 2xx from `/models` or any other path is not one, since the plan answers those while completions stay walled — and `bin/opencode-go` records it by writing that account's `opencode-seen/<account>` stamp, which row `al` reads against the rows. Both writes are single-file and independent, so no reader-then-writer window exists in which a 429 landing beside them could be discarded. The account a row is filed under is that plan's keychain service verbatim: `opencode-go` for the default profile and `opencode-go-<profile>` otherwise. `bin/review-bench` reads the same rows for its own launch question through its own clock horizon (`WALL_WINDOW_MAX_TTL_S`) and is not bound by this row's retirement rule — it asks whether a cell may be launched now, not whether the plan is spent — but the served stamp binds BOTH readers, and row `al` spells the one comparison they share | writers `bin/opencode-go` (`wall_row`/`record_call`/`record_wall`/`mark_served`) and `bin/review-bench` (`mark_walled`, `WALL_WINDOW_MAX_TTL_S`); pool reader `bin/review-bench`; display reader `llm-limits.sh` (row `al`); guard `tests/test_consistency.sh` |
| aj | Per-vendor role switches | a role is a per-vendor wall over the pool, spelled `<vendor>_<role>=off` in `~/.claude/worker-model` for `claudeb`/`codex`/`gemini` × `workers`/`reviewers`. The literal `off` is the only veto and an ABSENT key is the open state, so opening a role deletes the line rather than inventing an `=on` spelling; a hand-edited duplicate resolves FIRST-wins on every reader, or the menu shows one value while the routers follow another. `share/worker-model.sh` `worker_model_set_role <vendor> <role> on\|off` is the one writer — under the pin's own lock (row `ae`), and refusing a session (`CLAUDECODE`) outright, since closing a role redirects every worker and rater after it. The ladder is **pin > roles > pool** (`docs/routing-contract.md` Roles): a usable vendor pin answers over `<vendor>_workers=off` the way it answers over pool exclusion, while a reviewers query never sees a pin, so `<vendor>_reviewers=off` is final. The refusals are parsed contracts: `worker-pick: <vendor> is switched off for <role>` at exit `3`, which `worker-run` and `review-bench` both match to word their own, and `worker-pick: --role only means something with --account` at exit `2`. A workers wall reports `OUTCOME: <VENDOR>_UNAVAILABLE` and never `_USAGE_LIMIT` — a switch Egor flipped is not quota to hunt — and it holds over an explicit `--account`, a resumed session and the pin fallback alike, the vendor pin being the one override. In the cache line (row `c`) a workers-off vendor arrives as its own shape, `cb⏸off`/`cx⏸off`/`gx⏸off`, distinct from every account name | writer `share/worker-model.sh` `worker_model_set_role`; readers `bin/worker-pick` `role_off`/`--role` flag/`query_role_wall` and the per-vendor `*_role_off` flags, `bin/worker-run` `PICKED_ROLE_OFF` plus its own `${vendor}_workers` wall, `bin/review-bench` `reviewers_role_off`/`role_closed_note` and its `--role reviewers` query, `hammerspoon/llm-limits.lua` `WORKER_MODEL_PREFIX`/`WORKER_ROLES`/`readWorkerModel` with the For workers / For reviewers items writing through `M.setWorkerRole`/`WORKER_ROLE_SCRIPT`; render `bin/statusline.sh` `⏸off`; prose `docs/routing-contract.md` Roles, `docs/statusline-contract.md` worker row, `~/.claude/commands/worker.md` (keys `/worker` preserves and never writes) |
| ak | Auto-refresh vendor roster | the daemon refreshes exactly `claude`, `codex`, `gemini`, `opencode`, and the four are spelled together in the seed, the tick loop and the state validator, so a state file written before a vendor existed migrates into the default rung rather than failing validation. Three of them refresh by reading a free usage endpoint; `opencode` has none, so its cadence is INVERTED and no other vendor may be given that shape: with no walled account in `~/.llm-limits.json` it is dormant — it sends nothing, and that state stays true for free because every real call records its own outcome (row `ai`) — and only a standing wall is probed, one `opencode-go wall-check` completion per walled account, which the gateway answers with a free 429 while the window is shut. The daemon never passes `--probe-clear`: that flag belongs to the explicit `Hard refresh` of row `al`, and on a heartbeat it would spend a completion per clear account per tick. It asks the collector, never the wall record: a second implementation of whether a wall stands (row `al`) would spend the plan on its own say-so. The probe's answer is read as one of three states and never two: `walled` (a refusal this probe itself recorded), `served` (a completion the plan answered) and `dormant` (nothing was sent at all, the wall having gone between the read and the request) — counting the third as a reopening would report a leg recovered on a request nobody made. The tick lock is handed back for the duration of the probe, because a wedged gateway must not hold the other three vendors' ticks behind it. A probe that answered re-collects, or the row it just retired keeps standing on every surface until the next tick. The stated reset is day-granular and therefore a hint: inside `OPENCODE_TIGHTEN_WITHIN_S` (`3600`s) of it, or past it, the existing ladder tightens and keeps probing, because only a served request retires a wall. No surface may invent a percentage for this vendor | `bin/llm-refresh` `normalized_state` seed, the `for vendor in` tick loop, the state validator, `opencode_tick`/`opencode_walls_json`/`opencode_probe`/`opencode_next_interval`; probe `bin/opencode-go` `wall-check` and `wall_check_all`; guard `tests/test_consistency.sh` |
| al | OpenCode rows in the limits store | the Go plan publishes no usage endpoint — `usage`, `limits`, `me` and `account` all 404 — so `.vendors.opencode` carries no percentage and never will, and whether a recorded wall (row `ai`) still stands is computed HERE, once, by `llm-limits.sh`. One row per profile listed in `~/.config/opencode-go/profiles` (absent file = the single default profile `-`), in file order: `{account, walled, windows: [{window, resets_at}], as_of, stale_seconds}`. `account` is the PROFILE, mapped to the wall record's account name by row `ai`'s formula; `window` is `5h`/`wk`/`mo`, or `?` for a pre-window record, and a named window suppresses `?` beside it; `resets_at` is the recorded horizon as an ISO-8601 instant, `null` when the gateway never stated one, and it is DISPLAY only — a horizon already past retires nothing. The rule is one comparison and holds no clock: a window stands while the account carries a wall row `detected_at` no earlier than (`>=`) its served stamp, so a refusal outlives its own stated reset and only a completion the plan served ever ends it — and a tie goes to the wall, because only a standing wall is ever probed by the scheduled refresh, so a same-second pair read as served would freeze a walled account clean forever. That comparison has TWO implementations and they must stay spelled alike: `llm-limits.sh`'s `.detected_at >= $served` for every surface, and `bin/review-bench`'s `read_wall_rows`, which drops an account's rows below its stamp BEFORE aggregating a standing wall — the same order, or a stale long-horizon row shadows the 429 recorded after the completion and answers for the account anyway. A missing or unparsable stamp is no evidence and keeps every row, so an account nobody ever served stays walled on both. The bench answering this question its own way is what refused a whole OpenCode leg for a day (2026-08-15) on an account whose plan was serving other models fine. `as_of` is that served stamp alone — a 429 is not evidence the account is alive, and dating the row by one would show a walled account getting fresher the more often it refuses — so an account never served carries no age at all. The vendor is assigned LAST in the collector pipeline, past every pass that reads a percentage — staleness, expiry, `usable_now`, and the empty-vendor verdict that decides the exit code — because each of those would be inventing a reading; `--plain` and `--table` skip it for the same reason. Every other surface RENDERS these rows and re-derives nothing: `hammerspoon/llm-limits.lua` reads them like any vendor's, `bin/llm-refresh` picks its probe targets from them, and `opencode-go wall-check` asks them before spending a completion — unless `--probe-clear` makes the asking itself the point: the menubar's `Hard refresh` runs `wall-check --all --probe-clear`, which walks the roster file instead of the walled rows and spends one small completion on every clear account, since `as_of` is the served stamp and nothing but a served completion moves it. Anything scheduled (row `ak`) stays walled-only | producer `llm-limits.sh` `opencode_profiles`/`opencode_seen_tsv`/the `opencode=` jq; consumers `hammerspoon/llm-limits.lua` `appendOpenCode`, `bin/llm-refresh` `opencode_walls_json`, `bin/opencode-go` `wall_check`/`wall_check_all`, `bin/review-bench` `opencode_served_at`/`read_wall_rows`; guards `tests/test_consistency.sh` and `tests/test_review_bench.sh` |
| am | Worker files reach the launching chat | a worker a chat spawned IS that chat, so the files it authored are that session's own work for review coverage — the alternative is a run of hundreds of worker-authored files priced as the one edit the chat made by hand (Egor, 2026-08-17). The carrier is the RUN RECORD, not a report anyone read: at launch `worker-run` writes `<run-dir>/launcher` holding the `CLAUDE_CODE_SESSION_ID` that spawned it (nothing when there is none, or when the id is not a plain name), and after EVERY attempt — a walled one, a failed one, and one that never reached its workdir included — `<run-dir>/files`, whose first line is `WORKDIR: <dir>` and whose remaining lines are that run's paths, workdir-relative or absolute when they fall outside it, unioned across attempts because rerouting restamps the run's start and takes a new session. `UNKNOWN: <reason>` replaces the paths when the list cannot be established at all (vendor keeps no per-file record, no workdir, transcript unreadable) and `PARTIAL: <reason>` stands beside real paths when the run also ran shell commands, whose edits no transcript records; a record is always written, because no record is what an unfinished run looks like and both readers take that absence for "this run wrote nothing". `commit-journal.sh` sweeps the store on EVERY tool call — `grep -l -x` over `*/launcher` for its own session id — skips a run with no `exit_code` whose supervisor pid is still alive (its list is not final) while sweeping one whose supervisor is GONE, since nothing will ever write that exit code and the list is as final as it will ever be, journals the rest under the current session id and marks `<run-dir>/journaled` only over a record it actually read. `review-flow-gate.sh` reads the same records: it folds the paths of a finished run the journal has not swept yet into its pending set (a worker that finished between the chat's last tool call and its commit reaches the commit through no journal entry at all), and names once each — carrying the record's own reason — every run that recorded no list, every run that recorded its editor calls only (`PARTIAL:`), every run still going, every run whose supervisor pid is gone, which it calls ABANDONED rather than running because a missing `exit_code` is not liveness — folding that run's own listing into the pending set, since the journal's sweep and this one are the same two states — and every run whose record names NO supervisor pid, which is neither: nothing was ever established about it, and calling it abandoned tells the chat to stop waiting for something that may still be writing. A record naming no tree at all is named wherever the chat is committing rather than filtered out of every repository. Runs are retired by a `<run-dir>/noticed` marker the gate stamps on the invocation that CONSUMES the notice — never by a clock over a HEAD any co-tenant moves, and never at arming time, which would drop the run out of the very note key the retry is compared against and block the retry the notice promised. Nothing here ever refuses a commit (`docs/review-contract.md`). A change to this machinery is reviewed with the `parallel-sessions` lens (`review-bench lens list`): its defects appear only beside another working chat, which a default review never imagines | producer `bin/worker-run` `persist_run_files`/`compute_run_files` and the `launcher` write in `start_run`; consumers `../claude-setup/hooks/commit-journal.sh` `journal_worker_runs` and `../claude-setup/hooks/review-flow-gate.sh` `scan_run_records`; policy `docs/review-contract.md`; guards `tests/test_worker_run.sh`, `../claude-setup/tests/test_commit_journal.sh`, `../claude-setup/tests/test_review_flow_gate.sh` and `tests/test_consistency.sh` |
| an | Launching-chat pid walk | the harness registers a live chat as `<registry>/<pid>.json` with a string `.sessionId`, registry `~/.claude/sessions` (`bin/review-bench` takes `REVIEW_BENCH_SESSION_DIR` in its place for tests). A run is that chat's grandchild several execs down, so the launcher is found by walking `ps -o ppid=` up from the process's own pid, at most `15` hops and stopping at pid 1, taking the first entry whose `.sessionId` is a string and naming nobody where none is. TWO implementations, in two languages and at two moments: `bin/review-bench` walks ONCE at run start and records the answer as the progress document's `session`, because the chain is alive only then — a backgrounded run outlives its launcher and reparents to pid 1 — and `bin/statusline.sh` walks at render time ONLY for a document written before the writer recorded one. Which is also the precedence every reader owes: the recorded `session` first, the walk as the fallback, or a run the render attributes to nobody is dropped from the statusline of the very chat that started it | writer `bin/review-bench` `walk_launching_session`/`SESSION_WALK_HOPS`/`SESSION_REGISTRY_DIR_ENV`; reader `bin/statusline.sh` `review_run_session`/`review_run_owner`; segment row `docs/statusline-contract.md`; guards `tests/test_consistency.sh`, `tests/test_review_bench.sh`, `tests/test_statusline_hooks.sh` |
| ao | Review debt journal | whose debt a commit landed is recorded in `<absolute-git-dir>/claude-review-debt`, in the SAME record format as the commit journal beside it (row `am`'s carrier): `<session-id>\t<epoch>\t<repo-relative-path>`, NUL-terminated, entries with no TAB owned by nobody. Only `review-flow-gate.sh` writes it — at the commit that goes through — and it rewrites rather than appends, dropping the entries of paths no longer in debt, since a journal nothing prunes grows a chat's name onto files it settled months ago. `review-bench debt` reads it and the commit journal together and asks nothing else about authorship | writer `../claude-setup/hooks/review-flow-gate.sh` `record_debt`; reader `bin/review-bench` `journal_entries`/`debt_authors`; policy `docs/review-contract.md`; guards `../claude-setup/tests/test_review_flow_gate.sh` and `tests/test_review_bench.sh` |
| ap | Second-round thresholds bind the waiver | the two numbers a round is priced on (`SECOND_REVIEW_P1S=2`, `SECOND_REVIEW_FINDINGS=8`) decide the fork's wording in `review-flow-gate.sh` (row `af`) AND whether debt reads `locked` in `review-bench`, so the same round cannot owe a second review in one voice and be waivable in the other. They are spelled in both files and must stay equal | `bin/review-bench` `SECOND_REVIEW_P1S`/`SECOND_REVIEW_FINDINGS`/`debt_locked`; `../claude-setup/hooks/review-flow-gate.sh` `SECOND_REVIEW_P1S`/`SECOND_REVIEW_FINDINGS`; guard `tests/test_consistency.sh` |
| aq | The waiver's placeholder reason | the commit gate hands a chat a paste-ready `review-bench waive --reason 'WHY THIS GOES UNREVIEWED' --paths ...` line, and a model that pastes it unedited records the placeholder as the decision — a waiver that answers the question with the question. `bin/review-bench` refuses that one reason by name, so the literal is spelled in both files and must stay equal: a gate that rewords its placeholder while the bench still refuses the old one reopens the hole silently, with the paste-ready command as the carrier | `bin/review-bench` `WAIVE_PLACEHOLDER_REASON` and `cmd_waive`; `../claude-setup/hooks/review-flow-gate.sh` `waive_cmd`; guards `tests/test_consistency.sh` and `tests/test_review_bench.sh` |
